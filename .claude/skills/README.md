# Skills — Kurulum

Bu klasördeki skill'lerin bir kısmı **gitignored** (üçüncü-parti repo clone'ları, repo şişmesin diye). Yeni geliştirici / yeni makinada kurulum:

## htmx-expert (gitignored)

`ercan-er/htmx-claude-skill` (MIT) — HTMX pattern + anti-pattern + swap stratejileri. M-13 Plan 04 Faz 1'de kuruldu.

```bash
cd .claude/skills
git clone --depth 1 https://github.com/ercan-er/htmx-claude-skill htmx-expert
```

## ui-ux-pro-max (tracked)

GitHub repo'dan kopyalandı, içeriği projede yaşıyor. Yeni install gerekmez.

## alpine-js (tracked, in-house)

ReportHub için yazılan custom skill (`SKILL.md` ~250 sat). Razor MVC + Alpine.js entegrasyon pattern'ları.

## Diğer (tracked, in-house)

`session-handoff/`, `plan-tracker/` — proje-özel skill'ler.
