# Oturum Günlüğü — 2026-05-14

## Ana Konu

Mosaik'in stratejik değerlendirmesi → VISION.md yön belgesi → 15 dokümanı VISION ile hizalama → Plan 34 (SOP + AI Danışman) onaylanması → diğer modüllerin stale plan tespiti. Bu oturum **belge altyapısı + plan disiplini** üzerine yoğunlaştı, kod yazımı yok (Plan 34 implementation yarın başlar).

## Tamamlananlar (12 commit)

1. **`8db6292` — Kalan 2 HIGH güvenlik fix** — Analizdeki 11 HIGH listesi sweep edildi, 7'si zaten kapalıydı (a68378c'de), 2 gerçekten açıktı:
   - `AdminController.Reports.cs:66`, `AdminController.cs:253`, `TestController.cs:39+72` — `_ = ex;` → `_logger.LogError` + SqlException ayrımı
   - `SmtpEmailService.cs:82` — bulk disabled `_logger.LogWarning` eklendi
   - TODO Code review backlog güncellendi (false-positive 7 madde işaretli)

2. **`a4ef6f7` — TODO stale-claim önlem (3 katman)** — Tespit-fix asimetrisi sistemik boşluk olarak teşhis edildi:
   - `.claude/rules/todo-verification.md` yeni kural ("TODO bilgi değil hipotez")
   - `session-start.sh` stale-claim detector (hardening commit + open HIGH → uyarı)
   - CLAUDE.md madde 12 + memory `feedback_todo_stale_claim.md`

3. **`3c6630a` — session-start kod sağlığı sinyalleri** — Hook artık anlık verir:
   - C# 500+ hard-limit ihlali (ContractsController 577, AiController 570)
   - JS 350+ hard-limit
   - Inline style toplam (3 oluşum)
   - Silent catch `_ = ex;` sayısı (0)
   - ARCHITECTURE_MAP refresh tarihi
   - Plan envanteri + stale plan uyarısı
   - TODO MEDIUM open
   - Journal stale uyarısı (1+ gün eski)

4. **`e4bd67a` — CLAUDE.md inline marker auto-refresh** — `refresh-arch-map.sh` extend edildi:
   - `<!-- AUTO:TEST_COUNT -->`, `MIGRATION_RANGE`, `MIGRATION_COUNT`, `CONTROLLERS`
   - **Gerçek:** Test 246 → 260, Migration "01_→27_" → "00_→56_" (57), Controller 8 → 16
   - CLAUDE.md sayıları artık canlı koddan generate

5. **`7858eb2` — Vendor lib + mockup temizlik + ADR-002** — 74,468 satır silindi:
   - `wwwroot/lib/bootstrap` (7.2 MB ölü) + `lib/jquery*` (~700 KB ölü) + `_ValidationScriptsPartial.cshtml` (kullanılmıyor)
   - `wwwroot/mockups/` + `design-mockups/` → `docs/mockups/` (production wwwroot temiz)
   - ADR-002 (modular-monolith) retrospektif yazımı + CLAUDE.md ADR index güncel

6. **`09f4721 + 6c7dfe1` — Skill audit + Plan stale-check + ADR-013** — 3 ayrı iş:
   - `web-component-design` skill silindi (kendisi "SKIP for Razor" diyordu)
   - `.claude/skills/README.md` skill index (23 aktif, 6 kategori)
   - `session-start.sh` plan stale-check (14 gün dokunulmamış uyarısı)
   - `plan-first.md` §6 stale plan disiplini
   - ADR-013 multi-DB topology (1 local + 3 remote DB + identity bridging + filter)

7. **`11d909b` — docs/VISION.md** — Mosaik ürün özellik vizyon belgesi (kullanıcı analizi ~2200 kelime + kalıcı hale getirildi):
   - Mevcut özellik %75 / vNext %40-50 iki olgunluk ayrımı
   - 7 modül değer sıralı liste (SOP > Comment > Form > Workflow Designer > Duyuru > KPI/OKR > Mesajlaşma)
   - **vNext kalbi: SOP + Comment + Workflow Designer** (~6-9 hafta)
   - Yapılmayacaklar listesi (KPI/OKR ayrı modülü, Mesajlaşma, ayrı Duyuru, own Form Builder)

8. **`e930ca8` — Tüm belgeleri VISION ile hizala** — 8 dosya:
   - test-discipline.md (yeni kural — "Build yeşil ≠ test yeşil")
   - Plan 16 VISION uyum başlığı + 14 plan durum tablosu
   - Plan 19 → archive (Plan 27 Faz C absorb)
   - ADR-014 (frontend stack — Vanilla + Alpine + htmx ertelenmiş)
   - ADR-015 (yeni modül ayrı assembly + mevcut 4 modülü 1/ay tempoda)
   - architecture.md Module ayrımı + Frontend stack + olgunluk
   - plans/README.md VISION cross-reference + yapılmayacaklar
   - CLAUDE.md madde 13 (test disiplini) + ADR index 15

9. **`b5e15f8 + 909c1b3 + 6a8ac7f` — Plan 34 (SOP) yazıldı + AI Danışman eklendi + onaylandı**:
   - 5 entity (SopDocument, SopVersion, SopReadReceipt, SopApprovalSubmission, SopAiConversation)
   - 7 faz (A-G), Faz F AI Danışman MVP (DocumentChatService pattern reuse + thumbs feedback + retention)
   - 12 açık soru → 12 varsayılan karar (kullanıcı kabul disiplini ile)
   - Toplam 34-52h (Faz G hariç), 2.5-3 hafta

## Mimari etkileri (ADR + Plan)

- **ADR-002 + ADR-015:** Mevcut 4 modül (OrgChart, Calendar, Contracts, Documents) 1/ay tempoda `Mosaik.Modules.<X>` ayrı csproj'a çıkacak (Haz→Eyl 2026 takvimi)
- **ADR-013:** Cross-DB topology + identity bridging belgelendi, Linked Server **yasak**, ConnString plain-text DB'de (admin-only + audit)
- **ADR-014:** Frontend stack net — Vanilla (250+ satır) + Alpine (`x-data` ≤20 satır) + htmx **ertelenmiş** (canlı kullanım 0)
- **VISION.md:** 3 modül yapılmayacak (KPI/OKR ayrı, Mesajlaşma, ayrı Duyuru), Form Builder integrate (own değil)

## Build / Test Durumu

- Build: 0 hata 0 uyarı
- Test: **337/337 geçti** (test-discipline.md kuralı kanıtlandı — kapatma öncesi koşturuldu)
- Smoke test: yapılmadı (kod değişikliği yok, belge ve config)

## Diğer modüllerin durumu (kullanıcı isteği)

Aktif planların gerçeklikle uyumu denetlendi. **6 plan stale + 1 duplike** bulundu:

| Plan | Durum dosyada | Canlı kodda | Tespit |
|---|---|---|---|
| 13 G09 readonly SP login | Taslak, onay bekliyor | Yok | Beklemede, sorun yok |
| 14 Filter prod-ready | Faz A+B+C ✅ | UserDataFilter canlı | Faz B implement durduruldu (kullanıcı 8 May) |
| **16.6 Modular monolith** | Taslak (auto-onay) | Mosaik.Modules.Circular var | **STALE — archive'a taşınmalı** |
| 16.7 Tag system | Taslak, 6 açık soru | Yok | Beklemede, sorun yok |
| **17 Tamim** | ✅ Tüm fazlar | Mosaik.Modules.Circular | **STALE — archive'a taşınmalı** |
| **21 Lookup table** | Taslak | LookupService canlı | **STALE — implementasyon var, plan kapatılmamış** |
| 22 Holidays/dates | Taslak | Yok | Beklemede, sorun yok |
| **23 Sidebar layout** | Taslak | Implementasyon var | **STALE — archive'a taşınmalı** |
| 24 sqlcli web UI | Taslak | D:/Dev/sqlcli ayrı proje | Mosaik içi YOK (plan'a göre olacaktı) — durum belirsiz |
| **25 Sözleşme** | Taslak | ContractsController + Obligations + Compliance canlı | **STALE — Faz A+B+C kısmen tamamlandı, plan güncellenmeli** |
| **25.1 Contract security** | Taslak | Kısmen | **STALE — durum güncellenmeli** |
| **26 OCR fallback** | Taslak | Plan 27 Faz A'ya absorb | **DUPLIKE — Plan 19 gibi superseded (Plan 27'ye not yazıldı ama Plan 26 hâlâ aktif klasörde)** |
| **27 Documents AI** | Taslak | AiController + DocumentsController + DocumentChatService canlı (Faz A+B kısmen) | **STALE — durum güncellenmeli** |
| 32 Scheduled Reports | Taslak, 6 açık soru | Plan 31 SMTP var ama caller yok | Beklemede |
| 34 SOP | ✅ Onaylandı (bugün) | Implementasyon yok | Faz A başlangıç noktası |

**Eksik / yapılması gereken:**

1. **Plan stale temizliği** (~30dk) — 6 plan'ı archive'a taşı veya durumu güncelle:
   - `16.6` → archive (Circular implementasyonu olarak hizmet etti)
   - `17 Tamim` → archive (tüm fazlar tamam)
   - `21 Lookup` → durumu güncelle ("Implement edildi, plan kapanış journal'ı yaz" veya archive)
   - `23 Sidebar` → archive (implementasyon canlı)
   - `25, 25.1, 27` → durumu "Faz A/B implement edildi, Faz C+ devam ediyor" diye güncelle
   - `26 OCR fallback` → archive (Plan 27 Faz A absorb)

2. **Plan 25 + 27 durum güncellemesi** — Hangi fazlar canlı, hangileri kaldı detay yazılmalı (Plan 17'nin yaptığı gibi).

3. **Plan 13 + 22 + 16.7** — Onay bekliyor durumda kalanlar, bu oturumdan sonra kullanıcının yeniden bakması lazım.

4. **Plan 24 (sqlcli web UI)** — D:/Dev/sqlcli ayrı projede yaşıyor. Plan'a göre Mosaik içine alınacaktı, durum belirsiz. Yeniden değerlendirme gerekli (sqlcli ayrı tool olarak kalsın mı?).

5. **Plan 32 (Scheduled Reports)** — Plan 35 Comment/Mention'dan ÖNCE bitirilmesi gerek (VISION uyarısı). 6 açık soru hâlâ açık.

6. **Faz B implement (Plan 14)** — IK FilterDefinition aktivasyonu, 3 OptionsQuery hazır. Plan 18B HR Sync sonrası açılır (kullanıcı 8 May "şimdilik durdur" demişti).

## Commit Durumu

- Uncommitted: 2 dosya (`docs/security-findings-2026-05-12.md`, `kod_toplayici.ps1`) — bu oturumda dokunulmadı, kullanıcının kendi işi olabilir
- Bu oturumda 12 yeni commit (8db6292 → 6a8ac7f)
- Pre-handoff compliance scan yapılmadı (kod değişikliği yok, sadece belge)

## Kararlar (bu oturumda kalıcı yazılan)

| Karar | Yer | Tarih |
|---|---|---|
| Test disiplini — testleri kapatmadan yap | `.claude/rules/test-discipline.md` + CLAUDE.md madde 13 | 2026-05-14 |
| TODO stale-claim disiplini | `.claude/rules/todo-verification.md` + hook | 2026-05-14 |
| VISION yön belgesi (vNext kalbi SOP+Comment+Workflow Designer) | `docs/VISION.md` | 2026-05-14 |
| Modüler monolit enforcement | ADR-015 | 2026-05-14 |
| Frontend stack ertelemesi (htmx) | ADR-014 | 2026-05-14 |
| Multi-DB topology + Linked Server yasak | ADR-013 | 2026-05-14 |
| Plan 34 (SOP + AI Danışman) onaylandı | `plans/34-...md` | 2026-05-14 |
| Plan 19 absorbe Plan 27'ye | `plans/archive/19-...superseded-by-plan27.md` | 2026-05-14 |
| `web-component-design` skill silindi | git rm | 2026-05-14 |

## Dikkat Edilmesi Gerekenler

1. **TODO stale-claim eğilimi sistemik.** Bu oturumda 7 madde stale çıktı (11 HIGH'tan), şimdi de 6 plan stale. Önlemler (hook + rule + memory) yerinde ama bunu sıklıkla denetlemek lazım — her oturum başında session-start hook uyarı veriyor.

2. **Belge tutarlılığı sürekli risk.** VISION + Plan 16 + TODO + CLAUDE.md + ADR'lar bütünleşik bir grafa döndü — bir tanesi değişince diğerlerini güncellemeyi unutma. Cross-reference disiplini.

3. **Test koşumu zorunluluk haline geldi.** Bu oturumda 337/337 geçti, commit message'a yazıldı. Yarın Plan 34 Faz A başlayınca her commit'te test çalıştırılmalı.

4. **Plan 34 implementation paralel ikincil işlerle yarışacak.** Plan 32 Scheduled Reports + Plan 18B HR Sync paralel ikincil olarak duruyor. Aynı anda 2+ feature açmak (3 paralel feature eşiği, session-protocol.md) — dikkat. Önce Plan 34 Faz A bit, sonra Plan 32 6 açık sorusu.

5. **AI provider chain** (Ollama → Gemini → z.ai) Plan 34 + Plan 25 + Plan 17 üçü için ortak. Birinde değişiklik diğerlerini etkiler — `FallbackLlmService` üst soyutlama, Mosaik.Core.AI'da.

## Yarına Başlangıç Noktası

**Birincil:** Plan 34 Faz A — `Mosaik.Modules.SOP` iskelet (4-6 saat)
1. `dotnet new classlib -n Mosaik.Modules.SOP -o Mosaik.Modules.SOP --framework net10.0`
2. `Mosaik.sln add Mosaik.Modules.SOP`
3. `<ProjectReference Include="../Mosaik.Core/Mosaik.Core.csproj" />`
4. `SopModule.cs` `IMosaikModule` impl (Circular'dan kopya-uyarla — ModuleKey="sop", DisplayName="Prosedürler", Icon="fas fa-clipboard-list", DisplayOrder=70)
5. `Mosaik.csproj`'a `<ProjectReference Include="../Mosaik.Modules.SOP/..." />`
6. `Database/03_AddSopAppModule.sql` (AppModules.SOP kaydı)
7. Build smoke + sidebar entry görünüm test
8. Commit: `feat(sop): Faz A modul iskelet (plan: 34)`

**İkincil (paralel ya da yerine):** Plan stale temizlik (~30 dk)
- 6 plan archive'a taşı veya durumu güncelle
- Plan 26 Plan 19 gibi superseded işaretle
- Plan 25 + 27 fiili durumu yansıt

**Üçüncül:** Plan 32 (Scheduled Reports) 6 açık sorusu — Comment/Mention'dan önce bitirilmesi gerek (Plan 31 SMTP caller).
