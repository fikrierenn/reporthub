# Oturum Günlüğü — 2026-05-23

## Ana Konu

Plan 34 (SOP/Prosedür Yönetimi) Faz A→D bütün stack implement edildi: modül iskeleti, entity+migration, admin CRUD+Quill+approval, user-facing "Prosedürlerim"+okundu disiplini. Bunun yanında Plan 45 (post-review hardening, 1 HIGH + 3 MEDIUM güvenlik fix) ve annyang voice command araştırma notu da çıktı.

## Tamamlananlar

### Pre-Faz A — D-03/D-04 commit'leri (kapanış)
- `39ccf1a` D-03 multi-provider vision + D-04 24 test (önceki gün uncommitted'dı)
- `0bc66ef` CLAUDE.md marker + journal 2026-05-22

### Plan 34 SOP — Faz A iskelet (`2c700be`)
- `Mosaik.Modules.SOP.csproj` (Razor SDK, net10.0, HtmlSanitizer + EF Core)
- `SopModule : IMosaikModule` (ModuleKey=sop, DisplayName=Prosedürler, Icon=fa-book-open, SortOrder=270)
- Areas/SOP/Controllers/HomeController + Views/Home/Index placeholder
- `Mosaik.sln` add + `Mosaik/Mosaik.csproj` ProjectReference
- `Mosaik/Database/71_SeedSopAppModule.sql` (idempotent AppModules INSERT, ModuleType=extension)
- DB doğrulandı: AppModules WHERE ModuleKey='sop' → 1 satır
- `d1fdeb5` chore ARCHITECTURE_MAP + plan Faz A check

### Plan 45 — Post-review hardening (`47c178a`)
Dış inceleme bulgularını doğrulandıktan sonra fix'lendi:
- **F-1 HIGH** — `Mosaik/Services/DataSourceManagementService.cs:95` `oldSnap`'tan `ds.ConnString` çıkarıldı (audit log read access olanlara connection string sızıntısı kapandı). Test: `DataSourceManagementServiceTests.DeleteAsync_AuditOldValues_DoesNotLeakConnString`.
- **F-2 MEDIUM** — `Mosaik/Services/Ai/MultiProviderVisionProvider.cs:140,195` provider HTTP body `AiSummaryResult.Error`'a embed edilmiyor; generic Türkçe mesaj + structured `_logger.LogError` (Status/Model/Body).
- **F-3 MEDIUM** — `Mosaik.Modules.Forms/Database/72_FormsTokenCascade.sql` — EF Cascade ile SQL FK uyumsuzluğu kapandı.
- **F-4 MEDIUM** — `Mosaik.Modules.Forms/Database/73_FormsDbInvariants.sql` — enum CHECK + UsedCount<=MaxUses + FormSubmissionFieldValues XOR.
- LOW-F5/F6/F7 TODO.md backlog'a düştü (budget race, inline JS CSP, büyük dosya).
- `plans/45-post-review-hardening-2026-05-22.md` yazıldı.

### Plan 34 Faz B — Entity + migration (`ac205ea`)
- 4 entity: `SopDocument` (FirmaId/Title/Category/DepartmentIds CSV/IsCompanyWide/OwnerUserId/ReadDeadlineDays/RequiresIKApproval/AiAdvisorEnabled), `SopVersion` (VersionNumber/ContentJson/PlainTextContent/EffectiveDate/SupersededDate/Status), `SopReadReceipt` (AssignedAt/ReadAt/ConfirmedAt/ReminderSentCount), `SopApprovalSubmission` (ApprovalRequestId soft FK, modül izolasyonu).
- `Mosaik.Modules.SOP/Database/01_CreateSopTables.sql` — 4 tablo + 5 CHECK + filtered index (`IX_SopReadReceipts_UserPending` WHERE ConfirmedAt IS NULL).
- `02_SeedSopCategories.sql` — `sopCategory` lookup + 6 değer (İK/Operasyon/IT/Finans/Kalite/Etik).
- `SopModule.ConfigureModelBuilder` doldu.

### Plan 34 Faz C — Admin CRUD (`3b10aff`)
- `SopService.cs`: CreateAsync/UpdateAsync/NewVersionAsync (HtmlSanitizer + PlainText derive)/MarkVersionApprovedAsync/ArchiveSupersededAsync (90 gün eşik).
- `SopApprovalService.cs`: ApprovalRequest direkt DB (Mosaik.Services.ApprovalService inject yasak — ADR-002 modül izolasyonu). 2-3 step (Yazan→DepYön→IK opsiyonel), Rejected→Draft, tüm Approved→MarkVersionApproved.
- `SopController.cs`: Index/Create/Edit/Details/NewVersion/Submit/Decide + AntiForgery + `[Authorize(Roles="admin,sop-editor")]`.
- 6 admin view: Index, Create, Edit (`_SopForm` partial DRY), Details (version history + status pill), NewVersion (Quill 2.0.2 CDN). `_ViewImports` + `_ViewStart`.
- `SopServiceTests.cs` — 10 test (version chain, sanitize <script>, supersede, archive sweep, validation).

### Plan 34 Faz D — User-facing + okundu (`d885ae0`)
- `MyController.cs`: Index (onlyPending toggle), Read (MarkRead), Confirm (POST).
- `SopReadReceiptService.cs`: AssignToUsersAsync idempotent, MarkReadAsync (once), ConfirmAsync (ConfirmedAt + ReadAt fallback), GetMyReceiptsAsync (bekleyen üstte), GetPendingDueAsync (Faz E Hangfire için).
- 2 user view: My/Index (status pill Bekliyor/Okundu/Onaylandı + filter-bar + countdown), My/Read (status pill + Alpine onay checkbox disabled-until-checked).
- ViewModels: `SopMyListItemViewModel` (DaysRemaining/IsOverdue computed), `SopMyDetailsViewModel`.
- `SopReadReceiptServiceTests.cs` — 10 test (assign idempotent, mark only once, confirm + readAt fallback, unassigned guard, due window 7/3 gün, confirmed excluded).

### Annyang araştırma (`c72abed`)
- Kullanıcı oturum içinde annyang URL paylaştı.
- WebFetch ile teknik özellik + KVKK risk analizi yapıldı.
- Karar: şu an entegrasyon yok (ses verisi browser→Google/Microsoft cloud, KVKK Plan 40 prereq).
- Geri açma koşulu: provider DPA + opt-in + KVKK DataElement envanter.
- Alternatif: Whisper.cpp WASM (yerel) veya kurumsal Whisper self-host.
- `docs/RESEARCH_ANNYANG_2026-05-22.md` yazıldı, TODO.md backlog satırı.

## Build / Test Durumu

- **Build:** 0 uyarı, 0 hata.
- **Test:** 432/432 yeşil (önceki 411 + 1 F-1 DataSource audit + 10 SOP service + 10 SOP receipt = 432).
- **Smoke test:** yapılmadı (kod yolu unit + integration test ile doğrulandı). Migration 71+72+73+SOP-01+02 production'da `dotnet run` ile sırayla uygulanır (Forms tabloları henüz DB'de yok — Plan 41 Faz 0 migration runner ile gelecek).

## Commit Durumu

- Bu oturumda commit sayısı: **10** (D-03/D-04 closure dahil).
- Uncommitted: **0** dosya.
- Yeni commit'ler:
  - `39ccf1a` feat(ai): D-03 + D-04
  - `0bc66ef` chore(docs): CLAUDE.md marker + journal
  - `2c700be` feat(sop): F-A iskelet (plan: 34)
  - `d1fdeb5` chore: ARCHITECTURE_MAP + Plan 34 Faz A check
  - `47c178a` fix(security): F-1..F-4 (plan: 45)
  - `ac205ea` feat(sop): F-B entity+migration (plan: 34)
  - `3b10aff` feat(sop): F-C admin CRUD (plan: 34)
  - `d885ae0` feat(sop): F-D user-facing (plan: 34)
  - `c72abed` docs(research): annyang
  - `d86323c` docs(map): ARCH_MAP refresh

## Yarım Kalan / Yarına Bırakılan İşler

### Plan 34 Faz E — Bildirim (4-6h)
- **S-20** Yeni SOP onaylanınca atanmış user'lara `INotificationService` push (Plan 17 Faz H altyapısı kullan)
- **S-21** Reminder Hangfire RecurringJob — `SopReadReceiptService.GetPendingDueAsync(7)` ve `GetPendingDueAsync(1)` her gün taranır, `ReminderSentCount` artırılır
- **S-22** Email caller (Plan 31 SMTP — Plan 32 caller hazır olduğunda, değilse skip)

### Plan 34 Faz F — AI Danışman (10-14h)
- **S-23** `SopAiConversation` entity + Migration `05_CreateSopAiConversation.sql`
- **S-24** `SopVersion.PlainTextContent` zaten Faz B'de cache var; Migration 01 NOT NULL'a alma opsiyonu
- **S-25** `SopAiAdvisorService` (FallbackLlmService chain + system prompt + 30K char context + audit)
- **S-26** `SopRateLimitGuard` (20 soru/saat/kullanıcı, admin bypass)
- **S-27** `_SopAiAdvisorDrawer.cshtml` Alpine drawer + KVKK uyarı banner + disclaimer
- **S-28** Feedback UI (thumbs-up/down + endpoint)
- **S-29** History retention RecurringJob (1 yıl)
- **S-30** `SopAiAdvisorServiceTests`

### Backlog (acil değil)
- LOW-F5 AI budget race (`Interlocked.Add` atomic) ~30dk
- LOW-F6 inline JS CSP refactor sweep ~4-6h
- LOW-F7 büyük dosya borcu (EditReportV2 1006 satır)
- Annyang POC (KVKK gating sonrası)

## Kararlar

- **ADR-002 modül izolasyonu güçlendirme:** Mosaik.Modules.SOP, Mosaik.Services.ApprovalService inject etmedi — ApprovalRequest/ApprovalStep entity'lerine direkt DbContext erişimi. Mosaik.Services.ApprovalService Mosaik.Core'a port edilirse SopApprovalService sadeleşir (Plan 16.5 follow-up). **Refactor şimdilik yapılmadı**, modül bağımsızlığı korunarak duplicate logic kabul edildi.
- **DepartmentIds CSV başlangıçta:** Plan 18B HR sync sonrası many-to-many'e refactor. UI'da basit text input + IsCompanyWide toggle (Plan 34 §10 madde 3 kararıyla tutarlı).
- **Icon `fa-book-open`:** Plan 34'te `fa-clipboard-list` denmişti ama Forms zaten bu icon'u kullanıyor — sidebar çakışmasını engellemek için `fa-book-open` seçildi.
- **Quill setup kopyalandı:** Plan 34 §10 madde 6 (modül izolasyonu için kabul edilen tekrar). `SopContentSanitizer.cs` Circular `QuillContentSanitizer.cs` birebir kopya. Mosaik.Core.Editor ortak base library Plan 16.5 follow-up.
- **PlainTextContent eager derive:** Faz F'de cache amaçlı, ama Faz B'de zaten `NewVersionAsync` içinde derive ediliyor (HTML→plain regex). Faz F sadece prompt'ta tüketecek.
- **Annyang kararı (reddedildi şu an):** KVKK Plan 40 + provider DPA olmadan entegre yok. Backlog'a not.

## Dikkat Edilmesi Gerekenler

- **Migration zinciri:** Mosaik.Modules.SOP/Database/01+02 ve Mosaik.Modules.Forms/Database/72+73 production'da uygulanmadı. `dotnet run` migration runner sırayla yapacak. Forms tabloları DB'de henüz yok (Plan 41 Faz 0 → Plan 45 F-3/F-4 → SOP 01/02 sırasıyla).
- **Test DbContext:** SopServiceTests + SopReadReceiptServiceTests `TestContext` (özel DbContext) kullandı; MosaikContext değil. Çünkü MosaikContext'i in-memory provider'a sokmak için Hangfire/SqlServer dep'lerini override etmek gerekiyor. `module.ConfigureModelBuilder(mb)` ile aynı schema kuruldu.
- **F-2 testler etkilenmedi:** vision tests `Assert.Contains("429"/"403", result.Error)` durum kodu hala mesajda var (`(HTTP 429)` format korundu).
- **SopController.CurrentFirmaId:** `User.FindFirstValue("FirmaId")` claim'i yoksa 0 döner — kullanıcı login'de FirmaId claim'inin set edildiğinden emin ol. (Plan 25 Faz B multi-firma kapsamı bu noktayı kapsamış mıydı, doğrulanmadı.)
- **SopApprovalService.SubmitAsync** SaveChanges 2 kez çağırıyor (ApprovalRequest.Id alabilmek için). Transaction wrapping yapılmadı; başarısız 2. SaveChanges → orphan Submission. Düşük risk, Faz E review.
- **Authorize Roles="admin,sop-editor"**: `sop-editor` rolü migration'da seed edilmedi (Plan 34 §10 madde 5'te söz var, ama Faz B'de atlandı). Test ortamında sadece admin login çalışır. Faz C+ rol seed migration 03 eklenmeli.

## Mimari Etkiler

- Yeni modül: `Mosaik.Modules.SOP` (4. extension modül — Circular, Forms, SOP).
- AppModules schema'ya yeni satır: `sop` (SortOrder=270, ModuleType=extension).
- 11 yeni controller olmadı (HomeController + SopController + MyController — Areas/SOP altında).
- `IMosaikModule` discovery ModuleLoader assembly-scan ile otomatik.
- Cross-modül abstraction kullanımı: `Mosaik.Core.Logging.IAuditLog`, `Mosaik.Core.Lookup.ILookupService`, `Mosaik.Core.Workflow.ApprovalRequest+ApprovalStep+ApprovalStatus`, `Mosaik.Core.Html.IContentSanitizer`, `Mosaik.Core.Module.IMosaikModule`.
- Mosaik.Services.ApprovalService **inject edilmedi** (modül izolasyon kararı yukarıda).

## Yarına Başlangıç Noktası

1. **Plan 34 Faz E başlat:** S-20 (INotificationService push, yeni SOP versiyonu Approved → `AssignToUsersAsync` + `INotificationService.CreateBulkAsync` "sop_published" event). Bağlantı: `Mosaik.Core/Notification/INotificationService.cs`.
2. **`sop-editor` rolü seed:** Migration `Mosaik.Modules.SOP/Database/03_SeedSopRoles.sql` (admin + sop-editor + sop-reader). Plan 34 §10 madde 5 kararıyla.
3. **Faz F'e geçmeden önce SmtpEmailService caller durumu doğrula:** Plan 31/32 ile SOP reminder email aktif olur mu? Olmazsa in-app notification yeterli.

## İlişkili

- Plan dosyaları: `plans/34-sop-prosedur-yonetimi.md` (Faz A+B+C+D ✅), `plans/45-post-review-hardening-2026-05-22.md` (✅ tüm fix'ler bitti)
- Research: `docs/RESEARCH_ANNYANG_2026-05-22.md`
- TODO.md güncellemeleri: 2026-05-22 dış inceleme follow-up LOW-F5/F6/F7 + annyang araştırma
- ADR ilişkileri: ADR-002 (modular monolith — ApprovalService inject yasağı), ADR-015 (yeni modül ayrı assembly)
