# Oturum Günlüğü — 2026-05-19

## Ana Konu
Önceki oturum sonu uncommitted iş (16 dosya) + handoff'tan kalan C-04 AI Wizard Faz 1 + multi-agent post-review (code-reviewer + silent-failure-hunter + security-reviewer + code-optimizer) bulgularının kapatılması. Üç dalga halinde HIGH → MEDIUM → derin audit MEDIUM hardening.

## Tamamlananlar

### Dalga 1 — Önceki oturumun temizliği (7 commit)
- `1880167` **fix(contracts): hafta sonu vade Pazartesi'ye kaydı** — ContractObligationGenerator.SafeDate + test align (2026 takvimi)
- `87b3e2a` **fix(reminder): daysLeft işaret düzeltme** — `-daysLeft` → `daysLeft` (zaten pozitif)
- `b3b105d` **fix(compliance): route binding + quarterly preview + KDV/Kurumlar/Geçici tarih düzeltme** — Migration 49 KDV 28, Kurumlar 30 Nis, Geçici DayOfMonth=17
- `533bfba` **feat(compliance): migration 60 — vergi/belediye/IK takvim paketleri** — Damga, KDV Tevkifat, GV 2. Taksit, SGK Prim, MTV, Emlak/Arazi, İlan Reklam
- `5a73fa3` **feat(ai): C-04 AI Wizard Faz 1** — AiController.Wizard.cs split + WizardExtractionService 8 kategori risk bayrağı + Create.cshtml drag-drop UI + components.css .risk-flag-list
- `a4f011b` **chore(hook): session-start turn-log injection** — cavemem-esinli son 5 tur özetini session start çıktısına ekle
- `7c9e2c2` **docs: TODO + journal 2026-05-17 + security findings**

### Dalga 2 — Multi-agent post-review HIGH (1 commit)
- `9f2af3e` **fix(security+reliability): 4 HIGH bulgu**
  - AiController.Wizard.cs:28 — `ReadAsync` → `ReadExactlyAsync` + EndOfStreamException (CA2022 da temizlendi)
  - WizardExtractionService.Start finally — temp PDF KVKK + disk-fill cleanup; AiController catch dalında da temizlik
  - DailyReminderJob — per-obligation try/catch izolasyon; FirmaIds parse foreach DIŞINDA (N+1 fix); AsNoTracking; MarkReminderSentAsync helper
  - Migration 60 idempotency — `IF NOT EXISTS WHERE PackageName AND Title` composite + BEGIN/END sarma

### Dalga 3 — MEDIUM bulguları (1 commit)
- `ce316d9` **fix(reliability): Turkey timezone + wizard polling temizliği**
  - Mosaik.Core/Domain/BusinessClock — Türkiye TZ helper (Windows + IANA fallback, ilk versiyonu statik)
  - DailyReminderJob + ComplianceController `today = BusinessClock.Today`
  - Create.cshtml `_resetJobState()` helper + 4xx/5xx polling kesimi (404 → süre doldu, diğer → sunucu hatası)

### Dalga 4 — Derin audit MEDIUM (2 commit)
- `14b7163` **fix(compliance+clock): ComplianceDueCalculator + IBusinessClock DI + TZ fail-fast**
  - Mosaik/Services/Compliance/ComplianceDueCalculator — saf vade hesabı (Q-due aylar [2,5,8,11], Monthly/Yearly weekend shift)
  - Mosaik.Tests/ComplianceDueCalculatorTests.cs — 9 yeni test (Geçici Vergi May/Ağu/Kas/Şub, KDV 28, Kurumlar 30 Nis, MTV 31 Oca)
  - BusinessClock → IBusinessClock + SystemBusinessClock; Program.cs ResolveTurkeyTimeZone fail-fast (UTC silent fallback kaldırıldı)
  - DailyReminderJob + ComplianceController DI ile clock alıyor
  - WizardExtractionService:55 path separator hardening (`+ DirectorySeparatorChar` → App_DataX silent kabul önlendi)
- `83e87e2` **feat(contracts): wizard alanları forma + migration 61**
  - Migration 61 — Contracts'a ContractValue/Currency/GoverningLaw/AutoRenewal/KvkkInvolved (idempotent guard) — sqlcli ile DB'ye uygulandı, 5 kolon doğrulandı
  - Contract + ContractCreateViewModel + Create.cshtml form alanları (2 input + 2 checkbox) + populate `check()` helper
  - Create.cshtml `upload()` başında pollTimer cleanup (file korunur)
  - components.css `.field-check` generic utility

### Mimari etkileri
- Yeni `Mosaik.Core/Domain/IBusinessClock` interface — tüm tarih-kıyaslama path'leri DI üzerinden.
- Yeni `Mosaik/Services/Compliance/` namespace.
- `Mosaik/Database/61_AddContractWizardFields.sql` — Contracts schema genişledi.
- ARCHITECTURE_MAP auto-refresh: test=272, migration 61, CLAUDE.md güncellendi.

## Build / Test Durumu
- Build: yeşil (0 hata, 0 uyarı — CA2022 da temizlendi)
- Test: **349/349 yeşil** (340 + 9 yeni ComplianceDueCalculator)
- Smoke test: server start OK + login screen render OK; Create form'unun DOM verify'ı admin şifresi bilinmediği için tarayıcıdan tamamlanmadı (kullanıcı manuel test edebilir)

## Commit Durumu
- 11 yeni commit (`e6d6bb9..83e87e2`)
- Uncommitted: 1 dosya (`kod_toplayici.ps1` — kullanıcı scripti, dokunulmuyor)
- 15 eşik altında

## Yarım Kalan / Yarına Bırakılan İşler

### Karar bekleyen (bu oturum bulguları)
- **N-1 notification idempotency stratejisi** — A strict (önce işaretle, sonra gönder), B at-least-once+retry (email fail → retry), C hibrit (in-app idempotent + email ayrı retry kuyruğu). Mevcut at-least-once benzeri ama email retry yok.
- **N-2 Compliance role gate** — `[Authorize(Roles="admin,finance")]` istenir mi, product onayı gerekli.
- **N-3 Wizard rate limit + temp TTL** — per-user concurrency cap (~2 eşzamanlı) + App_Data/wizard_temp/ TTL hosted service. Plan 33 Faz 4 ile birleştirilebilir.

### Plan 33 modül tamamlama (kalan)
- C-05 ContractsController partial split (~1h, **en hızlı**)
- C-01 DailyReminderJob Calendar+Compliance+Tamim genişletme (3-4h)
- C-02 Plan 22 Holidays entity (10-14h, ADR-016 öncesi)
- C-07 Plan 27 Faz A — ContractExtractionValidator + Stage 3 + 10 field (4-6h; wizard genişletme ile overlap)
- C-08 Plan 25.1 Faz 5+6 — Queue migration + admin inline + smoke (5-7h)

### ADR yazılmadı
- ADR-016 Calendar Unified Event Source
- ADR-017 Compliance Scope Sınırı
- ADR güncellemesi (006 UTC vs Turkey local — bu oturumda IBusinessClock pattern eklendi, ADR-006'ya bu kararı yansıt)

### Düşük öncelik
- Test coverage genişletme: WizardExtractionService + ComplianceController integration testleri
- ARCHITECTURE_MAP'e IBusinessClock + ComplianceDueCalculator pattern notu
- Notification idempotency tablosu/constraint (N-1 stratejisi netleştikten sonra)

## Kararlar
- **Quarterly compliance vade hesabı Türk vergi takvimi referans** — Q-due ayları [2,5,8,11]. Saf helper'a çıkarıldı + 9 test.
- **BusinessClock UTC silent fallback kaldırıldı** — TZ bulunamazsa Program.cs startup fail-fast (`InvalidOperationException`). Sebep: düzelttiği bug'ı sessizce geri getirebilirdi.
- **IBusinessClock DI üzerinden** — statik yerine. Test edilebilir + future-proof.
- **Wizard temp PDF cleanup ExecuteAsync finally** — KVKK retention + disk-fill koruması. Cleanup hosted service ayrı iş (N-3).
- **Multi-agent compliance scan oturum içi** — 4 paralel agent (code-reviewer + silent-failure-hunter + security-reviewer + code-optimizer). Tekrar etmek değerli.
- **Migration 61 DB'ye uygulandı** — sqlcli ile (5 ALTER TABLE ADD COLUMN). Mosaik DB profile = `mosaik`.

## Dikkat Edilmesi Gerekenler
- `BusinessClock` statik API hâlâ dosyada (`SystemBusinessClock.ResolveTurkeyTimeZone()` statik kalmalı çünkü Program.cs startup'ta çağrılıyor) — `IBusinessClock`/`SystemBusinessClock` instance API'si üzerinden tüketim default.
- Migration 61 idempotent — re-run güvenli ama dosyayı silme.
- AiController.Wizard.cs `WizardStatus` endpoint user-scoping yok (security agent flag etmişti, M sınıfı). jobId GUID enumerate edilirse başka user'ın sonucu görünebilir teorik olarak. Pratik risk düşük (10 dk TTL + GUID v4) ama ilerideki SOP/Comment AI mention için pattern düzeltilebilir.
- `kod_toplayici.ps1` untracked kalıyor (kullanıcı scripti).

## Yarına Başlangıç Noktası
1. **N-1/N-2/N-3 karar al** — özellikle N-1 idempotency stratejisi (15 dk konuşma yeterli). N-3 plan yazılmalı (Plan 33 Faz 4 D2 adayı).
2. **C-05 ContractsController partial split** (1h, en hızlı, sıcak başlangıç) — Files.cs + Ai.cs (zaten Wizard.cs split var, pattern referans).
3. **ADR-006 timezone notu** — IBusinessClock kararını ADR-006'ya yansıt (5-10 dk).
4. **C-01 DailyReminderJob genişletme** — Calendar event reminder + Compliance template reminder + Tamim reminder zaten ayrı (`TamimReminderJob`). Tek `IReminderJob` interface mi, ayrı job mı? Karar gerek (3-4h).
