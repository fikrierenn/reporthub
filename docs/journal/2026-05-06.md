# 2026-05-06 — M-10 Faz 6 Widget Binding Migrate

## Ana konu

ADR-007 Named Result Contract'ın **son fazı** (Faz 6) tamamlandı. PDKS Pano + Satış Pano widget'ları legacy `resultSet: N` int index binding'inden `result: "<contractName>"` named binding'e taşındı; `DashboardConfig.ResolveResultSet`'in 3. dal'ı (legacy index) ve `DashboardComponent.ResultSet` property'si tamamen kaldırıldı. Plan: `plans/archive/06-m10-faz6-widget-migrate.md`.

## Tamamlananlar

- **Migration 27** ([ReportPanel/Database/27_MigrateWidgetBinding.sql](ReportPanel/Database/27_MigrateWidgetBinding.sql)): cursor + JSON_MODIFY pattern. PDKS 15 + Satış 16 = 31 widget. Idempotent (WHERE clause `result IS NULL AND resultSet IS NOT NULL`). Doğrulama SELECT: PDKS Pano 15/15 NamedBinding, Satış Pano 16/16 NamedBinding, LegacyIndexOnly=0.
- **DashboardConfig.cs** ([Models/DashboardConfig.cs:34-58](ReportPanel/Models/DashboardConfig.cs#L34)): `ResolveResultSet` 3 dallı resolver → 2 dallı (named contract → "rsN" regex fallback). Legacy `comp.ResultSet.HasValue` dal silindi. `DashboardComponent.ResultSet` int? property tamamen kaldırıldı (eski JSON config'lerde `resultSet` field'ı varsa `JsonExtensionData` ile `Extra` dict'e düşer, sessiz ignore).
- **PlaceholderRenderer.cs** ([Services/Rendering/PlaceholderRenderer.cs:13-15](ReportPanel/Services/Rendering/PlaceholderRenderer.cs#L13)): bindInfo 3-yollu if/else → ternary (Result var/yok).
- **DashboardConfigValidator.cs** ([Services/DashboardConfigValidator.cs:212-225](ReportPanel/Services/DashboardConfigValidator.cs#L212)): legacy resultSet rules kaldırıldı; NoBinding artık **hard error** (önceden warning). V2 builder yeni widget'larda default `result: "rs0"` set ettiği için pratik etkisi yok; manuel manipülasyon / eski legacy import durumlarında save bloklanır.
- **Test güncellemeleri:**
  - `DashboardRendererTests`: 6× `ResultSet = N` → `Result = "rsN"`. `ResolveResultSet_falls_back_to_legacy_index_when_result_not_set` + `ResolveResultSet_returns_null_for_out_of_bounds_legacy_index` testleri silindi (artık dal yok). Yerine `ResolveResultSet_rsN_pattern_resolves_to_int_index` + `_returns_null_for_out_of_bounds_rsN_pattern` eklendi.
  - `DashboardConfigValidatorTests`: 14× `, "resultSet": N` → `, "result": "rsN"` (replace_all). `Validate_rejects_negative_widget_resultset` testi silindi. `Validate_warns_orphan_widget_no_binding` → `Validate_errors_widget_no_binding` (warning → hard error).
  - `DashboardConfigExtensionDataTests`: round-trip JSON'una binding eklendi (`"result": "rs0"`).
- **Sonuç:** Build yeşil, 228/228 test yeşil (önceki 229'dan -1: silinen legacy testi).
- **Plan arşivi:** `plans/06-m10-faz6-widget-migrate.md` → `plans/archive/`.

## Build / Test Durumu

- Build: yeşil (`dotnet build` 0 hata 0 uyarı)
- Test: **228/228 yeşil**
- Smoke (browser):
  - PDKS Pano (/Reports/Run/13) ✓ — 15 widget render, console error yok
  - Satış Pano (/Reports/Run/14) ✓ — 16 widget render (UserDataFilter atama sonrası; Plan 07 deny-by-default davranışı)

## Commit Durumu

- Uncommitted (smoke test öncesi): 8 dosya (Migration + 3 kod + 3 test + plan + TODO + journal)
- Yeni commit: 1 (bu oturumda atılacak): `feat(dashboard): M-10 Faz 6 — widget binding migrate + legacy fallback kaldır (plan: 06)`
- Manuel SQL (kullanıcı SSMS): Migration 27 PortalHUB üzerinde çalıştırıldı (31 widget UPDATE).

## Yarım Kalan / Yarına

- **M-11 Plan 02 F-9** live preview endpoint (~5h) — V2 builder draft preview iframe configOverride
- **M-11 Plan 02 F-10/F-11/F-12** şablon + smart defaults + e2e (~9h)
- **DateTime Faz E** veri shift + SP/seed audit (~½ gün, backup zorunlu)
- **G-09 SP read-only login** (canlı blocker, ~2h, sıra: en son)
- **rsN regex fallback kaldırma** (potansiyel Faz 7) — V2 builder davranışı contract isimlerine zorlanırsa builder-drawer.js + builder-render.js + DashboardConfig.cs:46-50 güncelleme. Şimdilik kapsam dışı.
- **KPI Test (sil) raporu (id=16)** — geliştirme artığı, IsActive=1 ama scope dışı bırakıldı. NoBinding'i 1 widget'ta kaldı; admin save sertleşmesi yüzünden artık düzenlenemez. Kullanıcı silmeli (sil prefix'i bunu zaten gösteriyor).

## Kararlar

- **rsN regex fallback KORUNDU** — V2 builder default davranışı (`result: "rs0"` üreten) bozulmasın diye. Plan §2 "kapsam dışı" kararı.
- **NoBinding warning → hard error** — V2 builder default `rs0` üretiyor, manuel boş bırakma kabul edilemez. KPI Test (16) kollateralı kabul edildi.
- **`DashboardComponent.ResultSet` property tam silindi** — `[Obsolete]` yerine düz silme. Eski JSON config'lerde `resultSet` field varsa JsonExtensionData ile `Extra` dict'e düşer (sessiz ignore), deserialize bozulmaz.
- **Plan-First disiplini uygulandı** — plan onayı öncesi kod değişikliği yapılmadı; plan dosyası onaylandı, fazlar TODO + plan §7'ye yazıldı, tamamlanınca arşive taşındı.

## Dikkat Edilmesi Gerekenler

- **Migration 27 cursor pattern** — JSON_MODIFY array path'inde `$.tabs[X].components[Y]` dinamik olduğu için set-based UPDATE imkansız. Cursor pratik + idempotent + audit'lenebilir. Migration 26'dan farklı pattern (oradakı tek-step JSON_MODIFY); 27 daha karmaşık ama 31 widget için uygun.
- **MCP `git mv` track edilmemiş dosya** — taşırken `not under version control` hatası verir. Düz `mv` kullan, sonra add.
- **Plan 07 UserDataFilter smoke pre-koşulu** — Satış Pano test ederken login user'ın `urunKategori` filter'ı atanmış olmalı; admin'de yoktu, fikri.eren'de var (5 Mayıs Plan 07 atamasından beri). Faz 6 ile **alakasız** auth katmanı.

## Yarına Başlangıç Noktası

1. **DateTime Faz E** (~½ gün, backup zorunlu) — veri shift + SP/seed `GETDATE()` → `GETUTCDATE()` audit.
2. **KPI Test (id=16) silmek** — 1 dakikalık temizlik, admin form'dan silinemiyor (NoBinding hard error), DB UPDATE ile `IsActive=0` veya direkt DELETE.
3. **M-11 Plan 02 F-10** (~3h) — Şablon sistemi (KPI Trio / Trend Grafik / Detay Tablo) + 5 kbd shortcut + ? modal.
4. **G-09 SP read-only login** (canlı blocker, ~2h, sıra: en son).

---

## EK: M-11 F-9 Live Validate + Toast + Geri Al (aynı oturum)

Plan 06 sonrası ek scope: M-10 Faz 6 + M-11 F-9 ardışık tamamlandı. F-9'un büyük bölümü zaten yapılmıştı (PreviewDashboardV2 endpoint, Tam Önizle butonu/iframe, Plan 04 dirty chip+beforeunload). Eksik UX katmanı bu oturumda tamamlandı.

**Tamamlananlar:**
- **`DashboardValidate` endpoint** ([V2Preview.cs:396](ReportPanel/Controllers/ReportsController.V2Preview.cs#L396)): POST configJson → JSON `{success, errors, warnings}`. AntiForgery + admin-only.
- **`builder-feedback.js`** ([builder-v2/builder-feedback.js](ReportPanel/wwwroot/assets/js/builder-v2/builder-feedback.js)) ~130 satır mixin: `runValidation()`, `restoreSnapshot()`, `pushToast()`, `dismissToast()`, `captureSnapshot()`. `init()` page-load anında `captureSnapshot()` çağırır.
- **`_ReportFormBuilderFeedbackV2.cshtml`** yeni partial: validation banner (kırmızı errors / sarı warnings, X kapat) + toast container (sağ alt fixed, 4 tip: success/error/warn/info, 3sn auto-dismiss). Custom event köprü `@@builder-validate.window` + `@@builder-undo.window` parent scope'unda dinleniyor.
- **Topbar V2** ([_ReportFormBuilderTopActionsV2.cshtml](ReportPanel/Views/Admin/_ReportFormBuilderTopActionsV2.cshtml)): "Doğrula" butonu (`$dispatch('builder-validate')`) ve "Geri Al" butonu (`$dispatch('builder-undo')` + `:disabled="!$store.builder.dirty"`) eklendi.
- **EditReportV2 + CreateReportV2**: builder root `<div x-data="builderV2">` içine partial reference, script tag yüklemeleri.
- **Smoke** (browser, /Admin/EditReportV2/13): geçerli PDKS config (6400 char) → toast "Yapılandırma geçerli." banner gizli ✓; bilerek bozuk inject (NoBinding) → kırmızı banner "1 hata - 'Test' sekmesi → 'Bozuk' bileşeni: sonuç bağlantısı yok" + toast ✓; original config restore. Console temiz, build yeşil.

**Karar — Throttle 300ms (Plan §F-9 madde 49) KAPSAM DIŞI:** Manuel "Tam Önizle" butonu zaten çalışıyor; otomatik input → preview re-render performans + UX karmaşıklığı (her widget eklemede flicker). İleride opsiyonel feature olarak değerlendirilir. Plan 02'de `[~]` (kapsam dışı) işaretlendi.

**Yarına bırakılanlar değişmedi** ama F-9 düştü, F-10 öne çıktı.

---

## Oturum 2 — M-11 Plan 09 Designer ↔ Run Görsel Parite + Yan İşler

### Ana Konu
Plan 09 (V2 builder canvas widget'larının Run sayfasıyla görsel parite) onaylandı, 4 faz + 3 hotfix ile bitirildi. Ek scope: V1→V2 link swap, M-10 Faz 6 follow-up bug fix'leri (selectedRsIdx + findResultSetForComp named binding), M-11 Plan 02 F-10 template + kbd shortcut, RS modal → drawer inline panel refactor.

### Tamamlananlar (9 commit)

**M-11 ek işler:**
- `04839f2` — V1→V2 link swap: `_AdminTabReports` + `Reports/Index` "Düzenle"/"Yeni Rapor" linkleri V2 route'una (V1 route'lar geri-uyumluluk için açık).
- `6b798d5` — Chart auto-fill X/Y + `selectedRsIdx` named binding bug fix: M-10 Faz 6 sonrası "rsN" pattern'i düşmüştü; ResultContract resolve eklendi. + `builder-chart.js` mixin (10 variant requirements + auto-fill on variant change).
- `581c0d0` — Plan 02 F-10: 3 preset template (KPI Üçlüsü/Trend Grafik/Detay Tablosu) + 5 kbd shortcut (Ctrl+S/P, Esc, Delete, ?). 2 yeni partial + 2 yeni mixin.
- `6a9918e` — KPI "İleri Ayar" formula render + `findResultSetForComp` named binding fix: drawer'da formulaA/Op/B alanları çalışıyor ama renderer kullanmıyordu; server `KpiRenderer` + client `builder-render.js` eklendi. Plus tüm KPI'lar Migration 27 sonrası "—" gösteriyordu (yanlış RS) — 3-katmanlı resolver fix.
- `5383986` (revert) → `27955a7` — RS modal "çok geç açılıyor kullanışsız" geri bildirim sonrası modal silinip drawer Veri tab'ında inline collapsible panel'e refactor (Veri Seti dropdown'ında "Önizle" toggle, 10 satır × tüm kolon kompakt tablo, drawer kapanmadan workflow korunur).

**Plan 09 Designer ↔ Run görsel parite (4 faz, 3 fix):**
- `66de65b` — Faz 1: KPI 4 variant brand kart paritesi (basic/delta/sparkline/progress) + edit overlay (`.w-edit-overlay` preview mode'da gizli) + Chart.js CDN view'lara eklendi. V2 paleti tam: red/blue/green/amber/violet/rose/slate.
- `0f6a55a` — Faz 2: `builder-chart-render.js` mixin (~180 satır, `widgetCharts` Map + lifecycle + 10 variant Chart.js config server `EmitChartInit` paritesi). Table brand kart shell (full conditional format Faz 4'e ertelendi).
- `95810d7` — Faz 3: KPI variant-spesifik form alanları (delta için compareColumn/label, sparkline için trend.valueColumn, progress için Sabit/Kolon target seg) + `kpiCapacityWarning()` + `applyKpiFieldDefaults()` akıllı dolum + `setKpiVariant(v)` zincir.
- `9cb2ecb` — Faz 4: Tablo conditional format (4 mod: negativeRed/iconUpDown/colorScale/dataBar) + total row + stripe + sticky header. Search/Pager/CSV ADR-008 gereği bilinçli kapsam dışı (Tam Önizle iframe sağlıyor).
- `7250d36` — Plan 09 archive (`plans/` → `plans/archive/`, TODO.md M-11 Plan 09 [x]).
- `d114726` (fix) — brand kart Gridstack hücresine sığsın: height zinciri + overflow guard, w-content min-height:0, h3/text-3xl ellipsis.
- `ece2c63` (fix) — Önizle modunda drawer gizlensin: canvas full-width (1fr) + drawer display:none. Tam Önizle iframe paritesi tutturuldu.
- `c68c032` (fix) — flex item min-width:0: tablo sağ kolon kayması için (default min-width:auto, table content genişliği taşıyordu).

### Build / Test Durumu
- Build: yeşil (her commit sonrası `dotnet build` 0w 0e)
- Test: 228/228 yeşil (Plan 09'da test güncelleme yok, mevcut yeşil)
- Smoke: kullanıcı browser'da yapıldı (preview server stabil çalışmadı, otomatik smoke kısmen). Kullanıcı ekran görüntüsü ile doğruladı: Plan 09 Faz 1 KPI brand kart, Faz 2 Chart.js render, fix #2 sonrası canvas full-width.

### Commit Durumu
- Bugün toplam 9 commit (`27955a7..c68c032`)
- Bütün commit'ler upstream'e push edildi (`git push` tek atımda 16 commit'i alıp upstream'e gönderdi — daha önceki commit'ler de bu push'taydı)
- Uncommitted: 0

### Yarım Kalan / Yarına Bırakılanlar

- **Plan 10 (potansiyel) — Ana sayfa dashboard parite**: Kullanıcı ekran görüntüsünde "ana sayfadaki dashboard ta farklı mesela onu da aynı yapmak lazım" dedi ama hangi sayfayı (Reports/Index `qcard`, Admin/Index, Home/Index dummy) kastettiği netleştirilmedi. Aday: `Reports/Index.cshtml` `qcard` brand kart harmonize.
- **F-12 M-11 Plan 02 son madde**: e2e smoke + 4 screenshot (builder edit/preview/drawer-veri/drawer-gorunum) + handoff journal — Plan 02 §F-12 madde 67-73 (~3h). M-11 13-fazın son kapanış parçası.
- **KPI Test (id=16) sil**: 1 dk DB cleanup. NoBinding hard error yüzünden artık admin form'dan düzenlenemiyor.
- **F09 Faz 4 ek (opsiyonel)**: tablo search/pager/CSV — şimdilik Tam Önizle iframe sağlıyor, gerekli olduğunda eklenebilir.
- **DateTime Faz E**: ~½ gün veri shift + SP/seed `GETDATE()` → `GETUTCDATE()` audit. Backup zorunlu.
- **G-09 SP read-only login**: ~2h, canlı blocker, sıra: en son.

### Kararlar

- **Designer ↔ Run görsel parite** — server-side renderer'lara dokunulmadı, client (builder-render.js) Run paritesine yakınlandı. Bakım çift-kat trade-off kabul edildi (CLAUDE.md checklist not ileride: "server `KpiRenderer/ChartRenderer/TableRenderer` değişikliği = client paritesini koru").
- **Search/Pager/CSV builder canvas'ta ekleme YOK** — ADR-008 "preview = Reports/Run tek renderer" kararı: builder canvas WYSIWYG, interaktif feature'lar Tam Önizle iframe (Run sayfası) tarafından sağlanır. 257 satırlık `DashboardClientScripts.Table.cs` JS port'u atlandı.
- **Önizle mode'da drawer gizli** — Plan 04 opacity:.55 + pointer-events:none yetmiyordu (yine 360px yer kaplıyordu). Tam display:none + canvas full-width (1fr) Run paritesi için doğru.
- **RS modal → inline panel** — kullanıcı modal'ı "kullanışsız" buldu (workflow kırıyor, yavaş açılıyor). Drawer Veri tab'ında collapsible panel daha "yerinde", anlık (10 satır × N kolon, modal'ın 50×N×7 RS overhead'i yok).
- **V1 deprecate yolda** — V1 route'lar (EditReport, CreateReport) geri-uyumluluk için açık, ama navigasyon link'leri V2'ye yönlendirildi. CreateReportV2'da "Klasik Düzenleyiciye Geç" linki hala V1'i çağırır.
- **V2 paleti server ColorMap'inde eksik** — V2'de var olan amber/violet/rose/slate server `RenderContext.ColorMap`'inde yok (sadece blue/green/red/yellow/gray/indigo/purple). Client tam destek, server fallback blue. Ayrı bir bug, kapsam dışı not düşüldü.

### Dikkat Edilmesi Gerekenler

- **Preview server stabil çalışmadı** — birkaç defa "Server not found" hatası, otomatik smoke yapamadım. Build artık file lock yapmıyor (preview_stop sonrası dotnet build temiz) ama eval sırasında server kapatma garip. Sebep belirsiz; kullanıcı browser'da test etti.
- **Browser cache** — `builder-v2.css` `asp-append-version` olmadan yükleniyor; CSS değişikliklerinde **Ctrl+Shift+R** hard reload zorunlu. JS dosyaları append-version ile cache busting var.
- **Cookie expired** — server restart sonrası login kayboldu, kullanıcı tekrar login yaptı. Tek seferlik, smoke akışını sekteye uğrattı.
- **Alpine reactive proxy quirk** — eval'da `JSON.stringify(reactive_obj)` farklı sonuç verebilir (örn. array → indexed object); plain inspect için `JSON.parse(JSON.stringify(...))` snapshot tercih.
- **Bakım çift-kat (server↔client renderer)** — Plan 09 D yaklaşımının trade-off'u. Server `KpiRenderer/ChartRenderer/TableRenderer` değişiklikleri otomatik client'a yansımaz, manuel sync gerekir.

### Yarına Başlangıç Noktası

1. **Plan 10 netleştir** — kullanıcıdan hangi sayfa "ana sayfa dashboard" demek istediğini sor. Aday: Reports/Index `qcard` brand kart harmonize.
2. **F-12 M-11 kapanış** (~3h) — Plan 02 son madde, 4 screenshot + e2e smoke + handoff entry. M-11 13-fazın bitişi.
3. **KPI Test (id=16) sil** — 1 dk: `UPDATE dbo.ReportCatalog SET IsActive=0 WHERE ReportId=16` veya direkt DELETE. SQL'i kullanıcı SSMS'te çalıştırır.
4. **DateTime Faz E** — ~½ gün, backup zorunlu, `GETDATE()` → `GETUTCDATE()` audit.
5. **G-09 SP read-only login** — ~2h, canlı blocker (sıra: en son).
