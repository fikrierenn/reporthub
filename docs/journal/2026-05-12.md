# Oturum Günlüğü — 2026-05-12

## Ana Konu

Plan 23 sidebar reorganizasyonu, Plan 31 email altyapısı, yönetim paneli dashboard'a dönüşüm, V1 Builder silimi, mimari haritası + otomatik tazeleme sistemi. Kullanıcı "yapıya hakim ol" direktifi → 3 katmanlı önlem koyuldu.

## Tamamlananlar

- **Plan 31 — Email/SMTP altyapısı** (`e2da9b6`)
  - `Mosaik.Core/Email/{IEmailService, SmtpSettings}` cross-modül interface
  - `Mosaik/Services/Email/SmtpEmailService.cs` System.Net.Mail tabanlı, Enabled=false default
  - `EmailTemplates.cs` 5 HTML şablon ($$"""…""" raw string ile CSS literal `{` problem fix): OverdueObligation, ObligationReminder, AiExtractionComplete, CircularPublished, ApprovalPending
  - `appsettings.json` SmtpSettings + AppUrl
  - Program.cs DI kayıt
- **Plan 23 — Sidebar 5 grup + collapsible** (`53c5c35`, `a0f3dab`)
  - Migration 56 — `AppModules.GroupKey` kolonu + 4 yeni modül (contracts/obligations/orgchart/documents) + mevcut 7 modül grup ataması
  - `_AppLayout.cshtml` 5 grup loop: Ana / Çalışma Alanı / Sözleşmeler & Uyum / Yapı & Yapay Zeka / Sistem
  - Alpine `sidebarGroup(key)` factory + `localStorage 'mosaik.sidebar.group.<key>'` state
  - Sistem grubu 14 admin link + 5 alt-section (İçerik & Veri / Erişim / Yapı / Görünüm / İzleme)
  - Chevron rotation, x-transition, collapsed mode (icon-only) desteği
- **Yönetim Paneli yatay subnav kaldırıldı** (`1c596e4`)
  - `Admin/Index.cshtml` inline `<nav class="subnav">` + `_AdminSubnav.cshtml` partial silindi
  - 3 view'dan referans çıkarıldı (BrandSettings, Modules, OrgChart)
- **Admin Overview Dashboard** (`cc0b64c`)
  - `/Admin` default tab `datasources` → `overview`
  - `_AdminOverview.cshtml` yeni partial — sistem sağlık şeridi + 4 metrik tile + son aktivite feed (audit log event-type tone) + hızlı eylem 4 buton + son 5 rapor
  - AdminIndexViewModel: ActiveUserCount, ActiveModuleCount, TodayAuditCount, FailedLoginCount24h, RecentAudits
- **V1 Report Builder silindi — V2 canonical** (`01f4df2`)
  - `CreateReport.cshtml` + `EditReport.cshtml` view dosyaları silindi (567 satır)
  - V1 GET action'ları route attribute kaldırıldı (V2 wrapper'lardan çağrılır)
  - `CreateReportLegacyRedirect` / `EditReportLegacyRedirect` → eski URL'leri V2'ye 302
  - POST endpoint'ler (`/Admin/CreateReport`, `/Admin/EditReport/{id}`) kalır — V2 form'ları oraya submit ediyor
  - `_AdminOverview` linkleri V2'ye çevrildi
- **`docs/ARCHITECTURE_MAP.md` + `.claude/rules/before-major-change.md`** (`8bcc94b`)
  - Single source of truth: sidebar→route→view, V1/V2/deprecated tablosu, POST endpoint asimetri, refactor öncesi 9 adımlı çek-liste
  - CLAUDE.md § 0 madde 5 + § 2 madde 11 referans
  - `memory/feedback_canonical_check_zorunlu.md` eklendi
- **Otomatik refresh + staleness hook** (`0c39860`)
  - `scripts/refresh-arch-map.sh` — sqlcli (pre-built dll + CP857→UTF-8) ile AppModules, admin views envanteri (lines + inline-style sayımı), controller routes, last refresh date
  - `.claude/hooks/architecture-map-staleness.sh` — PostToolUse Edit|Write matcher, view/controller/Modules dokunuldukça uyarı
  - `.claude/settings.json` hook kaydı
  - `session-protocol.md § Oturum Sonu` ARCHITECTURE_MAP refresh mandatory
- **session-handoff SKILL Adım 1.5** (`dafc3ef`)
  - Handoff her çalıştığında `refresh-arch-map.sh` + diff varsa ayrı commit
- **Plan 32 taslak** (`830888f`)
  - `plans/32-scheduled-reports-email-distribution.md` — 6 açık soru, kullanıcı onayı bekliyor
- **Plan 33 + Design Prompt** (`94911a9`)
  - `docs/MOSAIK_DESIGN_PROMPT.md` — AI'ye verilebilen tasarım üretme prompt (kullanıcı "promtu unut" dedi, dosya kalır referans olarak)
  - `plans/33-admin-views-standardization.md` — 14 admin view standardizasyon planı (V1 silimi sonrası kapsam küçüldü)

## Build / Test Durumu

- **Build:** son commit (dafc3ef) sonrası 0 hata 0 uyarı
- **Test:** çalıştırılmadı (refactor + doc oturumu)
- **Smoke test:** preview start çalıştı, auth gerektiren sayfalar manuel test edilmedi (admin/admin reddedildi)

## Commit Durumu

- 12 yeni commit (e2da9b6 → dafc3ef)
- Uncommitted: 2 dosya (TODO.md + bu journal)
- 15 eşiğinin **altında**

## Yarım Kalan / Yarın'a Bırakılan İşler

- **Plan 33 admin views refactor** — kalan 14 view (V1 silindi):
  - HIGH (>30 inline): AiSettingsEdit (55), CreateDataSource (36), EditDataSource (37), OrgChart (39), AiSettings (30)
  - MID (15-21): BrandSettings (21), EditGroup, EditRole, Lookup, Modules (16)
  - LOW (7): CreateFilter, EditFilter
  - V2 Builder (CreateReportV2 81, EditReportV2 97) scope dışı — canvas/drawer ayrı plan
- **Plan 32 Scheduled Reports** — 6 açık soru cevap bekliyor (PDF format, external email, cron picker, max attachment, schedule sahibi pasifleşince, test recipient mode)
- **Admin form'u smoke test** — auth ile login sonrası sidebar render + dashboard görünüm + V2 redirect manuel doğrulanmadı

## Kararlar

- **V1 Report Builder silinir (V2 canonical):** Kullanıcı "v2 var zaten" dedi — V1 view'ları silindi, GET action'ları wrapper olarak kaldı, eski URL'ler V2'ye 302 redirect.
- **ARCHITECTURE_MAP.md kalıcı, otomatik tazelenir:** Manuel sync zorunluluğu kaldırıldı, handoff skill + PostToolUse hook ile sistemli.
- **DikkatIQ vs Mosaik:** Email modülünü DikkatIQ'da yazdım sonra "v2 yapıya hakim ol" gibi. Tek yön: DikkatIQ kaynak, **Mosaik hedef**. Plan 33 daha temkinli.
- **Plan 32 ertelendi, Plan 33 öne alındı:** Kullanıcı "yönetim paneli sıkıntılı" dedi → standardizasyon ve V1 temizliği önce.
- **Design prompt kalır ama tetik değil:** `docs/MOSAIK_DESIGN_PROMPT.md` ileride lazım olabilir. Kullanıcı "promtu unut" dedi — şimdilik kullanmıyoruz, dosya repo'da kalır.

## Dikkat Edilmesi Gerekenler

- **POST endpoint asimetrisi:** V1 GET silindi ama POST endpoint'leri aktif (V2 form submit hedefi). Bunu silmeyin → `AdminController.Reports.cs` line 85, 170. ARCHITECTURE_MAP § 4'te belgeli.
- **CP857 encoding:** sqlcli Türkçe çıktısı CP857 (DOS-Türkçe), CP1254 değil. refresh script bunu UTF-8'e iconv ediyor. Başka script yazarken not.
- **dotnet run subshell:** Script içinde `dotnet run` MSBuild temp clash veriyor — pre-built dll ile çağır.
- **Grep Türkçe binary:** `grep` UTF-8 multi-byte görünce "Binary file matches" dönüyor; `-a` flag zorunlu.
- **Plan 31 templates inline preview yok:** Email template'leri test edilmedi (SMTP Enabled=false). İlk gerçek mail gönderiminde render kontrol şart.

## Yarına Başlangıç Noktası

1. **Kullanıcıya sor:** Plan 33 sıradaki view? Önerim: en yüksek inline'dan başla — **AiSettingsEdit (55)** veya batch (4 view paralel agent ile).
2. **Veya:** Plan 32 6 açık sorusu cevaplansın → Migration 57 + entity başlat.
3. **Smoke test:** Login ile sidebar 5 grup + Sistem alt-section + AdminOverview dashboard görsel doğrula.

## Mimari Etkileri (yeni)

ARCHITECTURE_MAP.md'ye yansıtıldı:
- § 2: Sidebar Sistem grubu 14 link, alt-section'lar
- § 3: V1 Report Builder silimi + legacy redirect (canonical V2)
- § 4: POST endpoint asimetrisi (V1 silindiğinde dikkat)
- § 7: 10 modül + GroupKey kolonu (Migration 56)
- § 8: Plan 23/31 tamamlandı, Plan 32/33 taslak
- § 12: 18 admin view envanteri (inline-style sayımı dahil — Plan 33 hedef listesi)

---

# Oturum 2 — 2026-05-12 (akşam)

## Ana Konu

Plan 25.1 (CSS standardizasyon) ana hedefin tamamlanması — admin tarafı dışında modül + partial + 3 modül CSS dosyası komple ortak generic yapıya indirildi. Plan 25.2 (JS organize) V1 dead silimi + AntiForgery helper. 3 Mosaik uzman skill yazıldı (csharp-razor, css-expert, js-expert).

## Tamamlananlar (25 commit)

**Plan 25.1 CSS sağlık reorganizasyonu (P1-P8):**
- `f62144e` P1+P2 — Görsel bug (btn-soft, alert-error, sp-chip) + components.css duplicate temizliği (7 sınıf tek tanıma)
- `96f5db7` P3 — tokens.css alias'lar (`--border, --muted, --ink-mute, --err, --bg-1, --ink-5, --violet`) sessiz fail önlendi
- `e9c1259` P4 — Modül CSS çakışması temizlendi (7 sınıf modülden components'e merge — `filter-bar, file-pill, ai-summary-card, qcard child, section-title, pill.info-tone, btn.compact-sm`)
- `6c8c8bb` P5 — Dead Tamim card (79 satır) + acil-pill generic `.pill.urgent`'e taşındı
- `8605c71` P6 — tokens.css semantic değişkenler (`--info-ink, --info-bg-soft/-softer/-hover/-chip/-badge/-border-soft, --karar-ink/-bg-soft, --bilgi, --uyari, --duyuru, --violet`) + sed batch hardcoded renk → token
- `0ae8819` P7 — Tamim CSS gerçekten domain-özele indirildi (766→95 satır, %88 azalma). Generic'ler components.css'e: `.list-stack, .list-item + __head/__no/__desc/__body/__meta + .urgent/.karar/.uyari/.bilgi/.duyuru, .attached-file-list, .upload-zone, .read-receipt + .ack-*, .badge.is-read/.is-new, .form-row-2col, .file-chip`. Tamim CSS sadece `tamim-envelope, tamim-quill, tamim-urgent-toggle, tamim-scope`
- `6f3423e` P7 final — org-chart + builder-v2 generic'leştirildi. Kullanıcı kuralı: "tüm modüller ortak generic". inline-style-guard.md tablo güncel
- `db84fb1` P8 — Dead class kapsamlı temizlik (406 satır, 6 dosya), agent diff scan (951 CSS × 2226 token)

**Plan 25.2 JS organize:**
- `47da5e4` — V1 dead silimi (`dashboard-builder/` 7 dosya 976 satır + `admin-report-form/` 2 dosya 559 satır + `dashboard-builder.css` 435 satır = 1970 satır), AntiForgery global helper (`window.getAntiForgeryToken()` cached, 5 callsite tek source), org-chart-render.js XSS sertleştirme (`innerHTML` → `createElement + textContent`)

**3 Mosaik uzman skill:**
- `0841080` — `mosaik-csharp-razor`, `mosaik-css-expert`, `mosaik-js-expert` SKILL.md dosyaları, 918 satır. Plan 25.1+25.2 sonuçlarını proje-spesifik kurallarla belgeliyor. Sistem otomatik yükledi.

## Build / Test Durumu

- Build: yeşil (her commit sonrası `dotnet build` 0 hata 0 uyarı)
- Test: çalıştırılmadı (refactor + standardizasyon oturumu)
- Smoke test: preview start her büyük commit'te (`/`, `/Admin`, `/Reports`, `/Documents`, `/Calendar`, `/Circular/Circular`, `/OrgChart`, `/Admin/CreateReportV2`, `/Admin/EditReportV2/1` hepsi 200 / auth redirect, console error yok)

## Commit Durumu

- Bu oturumun commit'leri: 9 (`f62144e` → `0841080`)
- Toplam bugünkü: 35 commit (e2da9b6 → 0841080)
- Uncommitted: 2 dosya (TODO.md + journal-prev — eski oturum kalıntısı, normal)

## Yarım Kalan / Yarın'a Bırakılan İşler

- **Plan 32 Scheduled Reports** — 6 açık soru hala cevap bekliyor (PDF format, external email, cron picker, max attachment, schedule sahibi pasifleşince, test recipient mode)
- **Plan 18B HR Sync** (~16-24h) — plan dosyası yok, en üst öncelik TODO
- **Smoke test** — login + sidebar görsel manuel doğrulama (auth blok için her seferinde yapılamadı)
- **`dashboard-builder.css` deprecate adayı** — silindi bu oturumda (Plan 25.2)
- **Code review** — kullanıcı "akşam kapanışı ile code inceleme" demişti, başlamadan oturum bitiyor — yarın paralel agent (code-reviewer + silent-failure-hunter + security-review) yapılabilir
- **IIFE wrap eksik** — admin-datasource-form.js + test-page.js (rapor yanlıştı, ikisi de wrap'li çıktı — false alarm)

## Kararlar

- **Tüm modüller ortak generic yapı kullanır, modül-özel istisnai** — kullanıcı net kural (2026-05-13). `inline-style-guard.md` tablo güncel.
- **css-classify skill her CSS ekleme öncesi zorunlu** — 3 adımlı karar akışı: ara → reuse testi → modül-özel testi. Şüphede generic varsay.
- **AntiForgery tek source `window.getAntiForgeryToken()`** — app-shell.js'te, her sayfada yüklü, cached.
- **V1 Builder JS+CSS asset'leri** Plan 33 V1 silimi sırasında unutulmuştu — bu oturumda 1970 satır net silindi.
- **3 Mosaik uzman skill** — gelecek kod yazımında otomatik tetikleniyor. csharp-razor + css-expert + js-expert ile birlikte css-classify + alpine-js + htmx-expert zincirleme çalışır.

## Mimari Etkileri

ARCHITECTURE_MAP.md güncel — auto-refresh hook çalıştı:
- § 7: AppModules 10 modül + GroupKey
- § 12: Admin views envanteri (inline-style=0 hepsi)
- § 13: Controller routes
- Last refresh: 2026-05-13 (geç saat)

## Dikkat Edilmesi Gerekenler

- **CSS canonical = components.css**. Yeni utility eklerken **mutlaka** css-classify skill akışı (3 adım). Modül CSS'ine generic koymak yasak (Plan 25.1 ihlali).
- **AntiForgery cached** — sayfa içinde form replace edilirse cache stale olur. Plan 25.2 helper'ı **page load'da** çağrılır varsayımıyla. Çoklu form senaryosunda dikkat.
- **org-chart-render.js XSS fix** — JSON parse hata mesajı artık textContent. `e.message` artık güvenli render.
- **Tamim CSS 95 satır kaldı** — sadece envelope/quill/urgent-toggle/scope. Bir şey ekleneceğinde önce "generic mi, sadece Tamim mi" testi (css-classify).
- **3 uzman skill aktif** — Skill tool listesinde gözüküyor. Yeni controller/view/JS yazılırken otomatik referans alınır.

## Plan 25.1 + 25.2 Net Etki (toplam)

- **CSS:** components-tamim 1368→95 (−%94), org-chart 273→161 (−%41), builder-v2 1711→1516 (−%11), dashboard-builder 539→0 (silindi), legacy 56→51, components 1300→2096 (+ %61 canonical), tokens 95→130 (+11 semantic değişken)
- **JS:** 1970 satır V1 dead silindi (dashboard-builder/ + admin-report-form/ + dashboard-builder.css)
- **Cross-file class collision:** 0
- **Tanımsız CSS değişkeni:** 0
- **Duplicate tanım:** 0
- **Hardcoded modül rengi:** 0 (semantic token)
- **Görsel bug:** 0
- **Net silinen satır:** ~3200+ (CSS + JS dead)

## Yarına Başlangıç Noktası

1. **Code review** — kullanıcı "akşam kapanışı ile code inceleme" istemişti; 3 paralel agent (code-reviewer + silent-failure-hunter + security-review) bu oturum commit'leri tarar
2. **Plan 32 sorularına cevap** veya kullanıcı yön versin
3. **Plan 18B HR Sync** plan dosyası yazılır (en üst öncelik TODO)
4. **Smoke test** — login + yeni sidebar 5 grup + Sistem alt-section + V2 builder canvas + Tamim Details list-item render

---

# Oturum 3 — 2026-05-12 (öğleden sonra)

## Ana Konu

Code review (oturum 2'den devir) + güvenlik altyapısı kurma + 7 HIGH bulgu fix + dogfood. Kullanıcı önce code review istedi → 3 paralel agent (code-reviewer + silent-failure-hunter + general-purpose security) son 35 commit'i taradı. **0 CRITICAL exploitable**, 7 HIGH + 2 MEDIUM bulgu. Sonra "security ile ilgili skill ve kurallarımız net bir yapımız yok sanırım" → 3 katman security altyapı kuruldu. Sonra 7 HIGH fix. Dogfood ile 3 yeni silent-failure bulgusu daha → onlar da fix.

## Tamamlananlar

**Code review (oturum 2 commit'leri, range e2da9b6^..0841080):**
- 3 paralel agent çalıştırıldı (code-reviewer + silent-failure-hunter + general-purpose security)
- 0 CRITICAL exploitable
- 7 HIGH: route ambiguity, SMTP exception swallow, EmailTemplates HtmlEncode, AntiForgery cache null-poison, _AdminOverview try/catch eksik, OrgChart export catch yok, EditReportLegacyRedirect id validation yok
- 2 MEDIUM: SMTP password User Secrets uyarısı, AppModules CHECK constraint

**Security altyapısı (3 katman):**
- `.claude/agents/security-reviewer.md` — denetleyici agent (Mosaik-spesifik, file:line + attack path + fix snippet)
- `.claude/skills/mosaik-security/SKILL.md` — proaktif uygulayıcı skill (yeni POST/SQL/email/JS fetch yazılırken otomatik tetik, 10 kural × doğru pattern + anti-pattern)
- `.claude/commands/security-check.md` — `/security-check [range]` 3 paralel agent slash command
- CLAUDE.md §2 + `.claude/rules/security-principles.md` referansları güncellendi

**7 HIGH fix:**
- **HIGH-1** [AdminController.Reports.cs:16,132](Mosaik/Controllers/AdminController.Reports.cs:16) — V1 GET method'ları `private` yapıldı (convention route çakışması ortadan kalktı)
- **HIGH-7** [AdminController.Reports.cs:126](Mosaik/Controllers/AdminController.Reports.cs:126) — `EditReportLegacyRedirect` async + `AnyAsync` validation + yoksa warning redirect
- **HIGH-3** [EmailTemplates.cs](Mosaik/Services/Email/EmailTemplates.cs) — `E()` + `SafeUrl()` helper, tüm placeholder HtmlEncode
- **HIGH-2** [SmtpEmailService.cs](Mosaik/Services/Email/SmtpEmailService.cs), [EmailSendResult.cs](Mosaik.Core/Email/EmailSendResult.cs) — `Task<EmailSendResult>` + `Task<EmailBulkResult>` record, 4 specific catch (SmtpException, SocketException, OperationCanceledException, generic Exception)
- **HIGH-4** [app-shell.js:97](Mosaik/wwwroot/assets/js/app-shell.js:97), [_AppLayout.cshtml:413](Mosaik/Views/Shared/_AppLayout.cshtml:413) — `__aftCache = ''` initial + truthy check, `window.invalidateAntiForgeryToken()` API, layout'a global `@Html.AntiForgeryToken()`
- **HIGH-5** [AdminController.cs:117](Mosaik/Controllers/AdminController.cs:117) — `_AdminOverview` try/catch + `OverviewError` flag + view alert
- **HIGH-6** [org-chart-render.js:79](Mosaik/wwwroot/assets/js/org-chart-render.js:79) — `try/catch (e)` + `result.catch(reportFailure)` Promise rejection

**Dogfood (yeni altyapı kendi fix'lerimiz üzerinde):**
- `security-reviewer` agent ilk denemede yüklenmedi → frontmatter YAML parse hatası (description boş satırla `Examples:` ayrılmış, multi-paragraph) → tek paragrafa indirildi, examples body'e taşındı (NOT: yeni agent definition'ı runtime'da hot-reload OLMUYOR — yeni session gerekir, skill+slash hot-reload olur)
- `silent-failure-hunter` 3 yeni bulgu çıkardı:
  - **HIGH** `OperationCanceledException` generic catch'e düşüyordu → `AdminController.cs:144` + `AdminController.Reports.cs:128` `catch (OperationCanceledException) when (HttpContext.RequestAborted.IsCancellationRequested) throw;` eklendi, `EditReportLegacyRedirect`'a SqlException catch + redirect
  - **MEDIUM** `org-chart-render.js:54` dead `token` decl silindi
  - **MEDIUM** `oc.export` async Promise rejection sessizdi → `result.catch(reportFailure)` koluna eklendi

## Build / Test Durumu

- **Build:** Yeşil 0 hata 0 uyarı (her fix sonrası)
- **Test:** Çalıştırılmadı
- **JS syntax:** `app-shell.js` + `org-chart-render.js` parse OK
- **Smoke test (preview):**
  - `/`, `/Admin/CreateReport`, `/Admin/EditReport/1`, `/Admin/EditReport/99999` hepsi 302 Login (route ambiguity yok)
  - Login sayfası 200, 1 `__RequestVerificationToken` element
  - `window.getAntiForgeryToken()` length 198, `window.invalidateAntiForgeryToken` aktif
  - Console error: 0

## Commit Durumu

- Bu oturum yeni commit yok (commit-split önerildi ama henüz atılmadı, kullanıcı yarına bıraktı sanırım — `bunları sonra bırakalım` Plan 32/18B için, commit kararı net değil)
- Uncommitted: 18 dosya (15 eşiği aşıldı)
- Beklemede: 3 bucket önerildi
  1. `feat(security): 3 katman altyapı` — agents/security-reviewer.md + skills/mosaik-security/ + commands/security-check.md + CLAUDE.md + security-principles.md + TODO.md
  2. `feat(email): Result pattern + HtmlEncode templates` — Mosaik.Core/Email/* + Services/Email/*
  3. `fix(security): post-review hardening (7 HIGH + 3 silent-failure)` — AdminController + ViewModels + Views + JS
  + ARCHITECTURE_MAP auto-refresh diff (refresh-arch-map.sh çalıştı, -6/+4)
  + journal handoff

## Yarım Kalan / Yarın'a Bırakılan İşler

- **Commit-split** — 3 bucket henüz atılmadı (handoff sonrası kullanıcı kararına bırakıldı)
- **Plan 32 Scheduled Reports** — 6 açık soru hâlâ cevap bekliyor (PDF format, external email, cron picker, max attachment, schedule sahibi pasifleşince, test recipient mode)
- **Plan 18B HR Sync** — plan dosyası yok, kullanıcı "sonraya" dedi (~16-24h yatırım, ayrıca PII/Email kararları gerekli)
- **MEDIUM bulgular** — SMTP password User Secrets uyarısı (README/INSTALL.md), AppModules CHECK constraint migration (TODO backlog'da)
- **security-reviewer agent runtime restart** — yüklenmesi için yeni session gerekir

## Kararlar

- **Security 3 katman yapı kurumsal disiplini** — proje skill'lerinin (mosaik-csharp-razor, mosaik-css-expert, mosaik-js-expert) Mosaik-spesifik kod yazımı katmanı vardı; security için aynı katman eksikti → mosaik-security skill eklendi, security-reviewer agent ile çift kontrol, /security-check slash 3 paralel agent ile tek tetik.
- **EmailSendResult Result pattern caller eklenmeden önce sertleştirildi** — Plan 31 altyapı henüz caller'sız; API caller'lar girmeden önce `Task<EmailSendResult>` ile değiştirildi, sonra kırılmaz hale getirmek imkansızlaşacaktı.
- **HIGH-3 EmailTemplates encode** — `$$"""..."""` raw string Razor `@` auto-encode yapmaz; her placeholder elle `E()` ile sarılı + `appUrl` http/https scheme whitelist.
- **AntiForgery global token layout'ta** — sayfa içinde form olmasa bile `_AppLayout.cshtml`'a `@Html.AntiForgeryToken()` (cache stale + Login öncesi token element yok senaryolarını engeller).
- **Yeni agent definition runtime hot-reload OLMUYOR** — sadece skill + slash command runtime'da yüklenir. Yeni agent için session restart şart. Bu Cowork/Claude Code davranışı.
- **ADR yazımı:** Bu kararlar henüz ADR'ye girmedi. Plan 25.1 / 25.2 sonraki oturumda ADR-011 (security skill+agent+slash 3 katman) yazılabilir.

## Dikkat Edilmesi Gerekenler

- **`security-reviewer` agent yeni session'da yüklenir.** Bu oturumdaki dogfood çağrısı "Agent type 'security-reviewer' not found" döndü — frontmatter düzeltildi ama Agent registry runtime cache'liyor. `/security-check` slash command yarın gerçek anlamda çalışacak.
- **Email caller hâlâ yok.** Plan 31 SmtpEmailService DI'da kayıtlı ama IEmailService.SendAsync çağıran tek satır yok. Plan 32 (Scheduled Reports) caller olacak. Result pattern + HtmlEncode bu yüzden caller'sız sertleştirildi.
- **`_AppLayout.cshtml` global `@Html.AntiForgeryToken()`** — her authenticated sayfada hidden input render olur. Sayfada başka form varsa **2 token element** olur, `querySelector` ilkini alır. Tokenlar request başına aynı değer üretir (server-side), bu sorun değil.
- **AntiForgery cache** — `if (__aftCache)` truthy check, boş string falsy → DOM lookup tekrar. Login sayfasında Auth layout farklıysa token element olmayabilir, ama Login'de `<form>` zaten `@Html.AntiForgeryToken()` render eder → element mevcut.
- **`SafeUrl` `#"` döner geçersiz URL'de** — `<a href="#/Contracts">` malformed link olur, tıklayan sayfada kalır. Bilinçli (kullanıcıya yanlış yere gitme yerine hiçbir şey olmasın).
- **Anthropic security-review built-in skill** — `/security-check` proje slash command'ı 3 paralel agent çağırır (security-reviewer + silent-failure-hunter + general-purpose); Anthropic'in hazır `security-review` skill'i ayrı (current branch pending changes scan). İkisi farklı.

## Mimari Etkileri

ARCHITECTURE_MAP.md auto-refresh çalıştı (-6/+4 satır kozmetik). § 7 + § 12 + § 13 güncel.

**Yeni eklenen security katmanları ARCHITECTURE_MAP'a yansıtılmadı** — yarın § 14 "Security agent + skill + slash" alt-bölümü açılabilir veya CLAUDE.md §2 listesi yeterli sayılabilir. Karar sonraki oturuma.

## Yarına Başlangıç Noktası

1. **`bunları sonra bırakalım`** dedi kullanıcı — Plan 32 + Plan 18B sonraki oturumlara. Şu an commit-split (3 bucket) bekliyor; kullanıcı onaylarsa ardışık 3 commit at, ARCHITECTURE_MAP diff ayrı 4. commit.
2. **`/security-check` dogfood ikinci deneme** — yeni session'da `security-reviewer` agent yüklenmiş olacak; gerçek 3-agent paralel çalıştırması mümkün.
3. **MEDIUM bulgular** — README'ye SMTP password User Secrets notu (~5dk) + Migration 57 `AppModules.GroupKey` CHECK constraint (~10dk). Hızlı kazanım.
4. **Sıradaki ana iş** — kullanıcı yön versin: Plan 32 6 açık soru cevap, Plan 18B plan dosyası yazımı, veya farklı bir iş.
