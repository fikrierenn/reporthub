# Oturum Günlüğü — 2026-07-02

> Not: iş gece yarısını geçti; M7 Faz 4 çalışması 2026-07-01 gec saat başlayıp 07-02'ye taştı.

## Ana Konu
Plan 41 M7 Form Builder **Faz 4** (file upload + signature + KVKK DataElement eşleme). Faz döngüsü disiplini tam uygulandı: danış(mosaik-portal-danismani)→kod→full scan(3 agent)→preview E2E→commit.

## Tamamlananlar
- **Plan 41 Faz 4** ✅ `3a3c72e` — 8-10h faz:
  - `FormSubmissionFile` entity + **izole** `App_Data/forms/{firmaId}/` disk storage. ADR-002: Documents ana projeye compile bağlanamaz → magic-byte + path-traversal guard + GUID name **kopyalandı** (advisor conf 88).
  - `FormFileStorage` — base64 dataURL decode (PDF/PNG/JPG/Office magic-byte) + **decode-before-write** orphan koruması + `TryDeleteAll` rollback. Public/anonim 10MB, dahili 50MB.
  - **Signature** — survey-core **native signaturepad** (bundled, ayrı signature_pad lib GEREKMEDİ) → PNG dosya. Plan "signature_pad 5.1.1" yerine native (footprint-ladder: vendored survey-core reuse).
  - `DataElementLookupService` — cross-modül `KvkkDataElements` SqlQueryRaw (FirmaId YOK, 60 global) + graceful degrade (KVKK yoksa `DbException`→boş liste).
  - `FormFieldService` Map/UnmapDataElement (firma+form ownership guard) + Details admin eşleme UI. `DataElementMapValidator` YARATILMADI — `FormPublishValidator.GetUnmappedFieldWarnings` zaten vardı (advisor conf 90).
  - `DownloadFile` admin endpoint (firma guard + App_Data path guard, trailing-sep).
  - Migration 75 `FormSubmissionFiles` + FieldValue→File FK NoAction.
- **Full scan (3 paralel agent):** security-reviewer + silent-failure-hunter + code-reviewer. **0 CRIT/0 HIGH.** 3 MED düzeltildi:
  - path-prefix trailing separator (M-1, WriteToDisk + DownloadFile)
  - mid-batch WriteToDisk IOException orphan cleanup (write loop + SaveChanges tek try)
  - **InvariantCulture decimal/tarih** locale bug (survey "." vs tr-TR "," — Faz 3 _elapsed bug'ının aynısı, code-reviewer yakaladı)
  - DbException log metni neutral + TryDeleteAll geniş catch (F4).
- **12 yeni test** (FormFileStorage decode/magic-byte, schema signaturepad/file, validator file-empty).

## Build / Test Durumu
- Build: **yeşil** (0 hata, 1 legacy NU1903 kabul).
- Test: **697/697 yeşil** (12 yeni). CLAUDE.md marker 552→564 ([Fact] sayımı).
- Preview: **E2E doğrulandı** (admin/123456 + [AllowAnonymous] public). File(PDF)+signature(PNG)+number(3.5 locale) submit → disk dosya + ValueFileId ✓; bogus magic-byte reddi + orphan yok + token refund ✓; cross-modül DataElement map ✓; admin DownloadFile 8-byte PDF ✓. Test verisi + disk temizlendi, preview durduruldu.

## Commit Durumu
- Bu oturum 3 commit: `3a3c72e` (Faz 4), `8f94a9d` (plan ✅), `69fc2d3` (dünkü journal oturum 2).
- Uncommitted: `.claude/_archive/htmx` + `.codex` (kod-dışı) + arch-map/CLAUDE marker (handoff'ta commit'lenecek).

## Yarım Kalan / Yarına Bırakılan — Plan 41 kalan
- **Faz 5** AES-256 şifreli alan (`FormEncryptionService`) + İhbar (whistleblower) template + "İhbar Komitesi" decrypt-only + 4 test. `FormDefinition.IsEncrypted` alanı zaten var; ValueText field-level encrypt.
- **Faz 6** submission admin liste + Detail + ClosedXML Excel export + `form_submission_viewed` audit.
- **Faz 7** 8 template seed (DSAR/İhbar/Aday/İhlal/Rıza/Engelli/DPA/Review) + kapanış (ADR-020, VISION, CLAUDE.md modül listesi).

## Kararlar
- **File storage izole modül-içi** (Core IFileStore abstraction DEĞİL) — advisor conf 88, footprint-ladder: IFileStore çıkarmak Documents migrate gerektirir, tek tüketici için YAGNI.
- **Signature = survey-core native** (ayrı lib yok).
- **DataElement eşleme OPTIONAL** — Yayında bloklamaz, sadece warning banner.

## Dikkat Edilmesi Gerekenler
- **sqlcli DefaultProfile = `bkm` REMOTE** (192.168.40.201) → Mosaik local sorgusu için `--conn "Server=BT-FIKRI\SQLEXPRESS;Database=Mosaik;Integrated Security=true;TrustServerCertificate=true;"` explicit ver (parolasız = classifier-safe). sqlcli.json'a local profil eklemek istedim ama Edit bloklandı (dosyada sa parola).
- **Module migration otomatik uygulanmıyor** — `MigrationFolder` interface'te var ama consumer yok. sqlcli ile ELLE + **GO batch'leri ayrı çalıştır** (sqlcli tek batch).
- **FormSubmission→FormVersion FK Restrict** → test verisi silerken submission ÖNCE, sonra definition.
- **Build green ≠ runtime OK** — EF LINQ/locale bug'ları preview'de yakalandı (tekrarlayan pattern, her CRUD faz preview zorunlu).

## Yarına Başlangıç Noktası
1. **Plan 41 Faz 5** — danış (AES-256 key yönetimi: dev appsettings / prod env; field-level encrypt FormSubmissionFieldValue.ValueText, IsEncrypted flag) → FormEncryptionService + İhbar template → full scan → preview → commit.
2. Faz döngüsü disiplini (danış→kod→scan→preview→commit).
3. Sonra Faz 6 (submission admin+export), Faz 7 (8 template + kapanış).

## Kullanıcı Notu — Yarına Başlangıç (2026-07-02 gece)
Kullanıcı direktifi (aynen): _"session-start hook + bu journal + HANDOFF + DECISIONS G/H oku · İş → önce danış (içerik=bkm / kod=reporthub) → yap → kontrol ettir → smoke · Somut: doküman-editör ekranı · T-16 · veya kullanıcı frontend eşlemeyi bitirir"_

⚠️ **ÇÖZÜLMEMİŞ REFERANS UYARISI:** `HANDOFF`, `DECISIONS G/H`, `T-16`, `doküman-editör ekranı` — bu repo'da (`D:\Dev\reporthub`) **bulunamadı** (glob + grep negatif). Bu not muhtemelen **başka projeye ait** (bkm içerik repo / DevSuite / brain — "içerik=bkm" ipucu). Yarın İLK İŞ: kullanıcıya hangi repo/proje sorulacak; reporthub değilse doğru cwd'ye geç. Reporthub kalırsa doküman-editör ≠ Plan 41 Faz 5 — kullanıcı önceliği netleştirmeli. Süreç yine: **danış → yap → kontrol → smoke.**

---

## Oturum 2 (Görev Tanımları modülü) — 2026-07-02 gece

> **↑ Yukarıdaki "ÇÖZÜLMEMİŞ REFERANS UYARISI" ÇÖZÜLDÜ:** O not başka bir projeye (`D:\Dev\gorevtanimlari\bkm`, git değil, İÇERİK repo) aitti. Bu oturumda o projenin görev-tanımları **reporthub'a yeni bir Mosaik modülü olarak** eklendi. `HANDOFF`/`DECISIONS G/H`/`T-16`/`doküman-editör` = bkm'deki kanonik kayıtlar (aşağıda tam path).

### Ana Konu
BKM görev-tanımları (ayrı içerik projesi `D:\Dev\gorevtanimlari\bkm`) reporthub'a **`Mosaik.Modules.GorevTanimlari`** modülü olarak eklendi. Zirve personeli → görev-tanımı **kişi-düzeyi eşleme ekranı**. Circular/SOP deseni.

### Tamamlananlar
- **Yeni modül `Mosaik.Modules.GorevTanimlari\`:** GorevTanimlariModule (IMosaikModule) · Entities (GorevDocument/GorevZirveMap/GorevPersonelMap — **Personelno string** "4634-BKM") · Services/MappingService (IK datasource→Zirve kişileri, kişi-map/rol-map merge; base DbContext + DataSources raw SQL) · Areas/GorevTanimlari/Controllers/MappingController (ILogger+try/catch+validation+DbUpdateException) · Views/Mapping/Index.cshtml · Database/01 (4 tablo + AppModules, idempotent) · README.md.
- **Repo geneli değişen:** Mosaik.sln + Mosaik.csproj (ProjectReference) · Program.cs (AI warmup config-guard `Ai:WarmupEnabled`) · appsettings.Development.json (`Ai:WarmupEnabled=false`) · StoredProcedureExecutor (yorum) · Mosaik/Database/80_*.sql SİLİNDİ (modül Database/01 devraldı).
- **DB (Mosaik):** GorevDocuments(48)+GorevVersions(48)+GorevZirveMap(34)+GorevPersonelMap; DataSources IK connstring named-instance→**port 64507** (error 26 düzeltmesi); AppModules 'gorevtanimlari' satırı.
- **Bağımsız review** (code-reviewer deseni) → kritik fix: AppModules migration + controller hata yönetimi + validation.

### Build / Test Durumu
- Build: modül + ana app **0 hata**.
- Smoke: CANLI doğrulandı — `/GorevTanimlari/Mapping` HTTP 200, 271 kişi render + Save persist + auth 302. (`Ai:WarmupEnabled=false` → ONNX/LLM startup yüklemez.)
- Birim test: **VAR** (Mosaik.Tests/GorevMappingServiceTests.cs — T-16 parallel kapatıldı, 701/701 yeşil; view canonical class'lara geçti; IndexVm→ViewModels/).

### Commit Durumu
- **Uncommitted** — bu modül + repo-geneli dosyalar working tree'de. Commit KULLANICI kararı (atılmadı).

### Yarım Kalan / Yarına
- ~~T-16~~ ✅ KAPANDI (parallel): birim test (GorevMappingServiceTests, 701/701) · view canonical class · IndexVm→ViewModels/MappingIndexViewModel. code-review 0 CRIT/0 HIGH.
- **Doküman-editör ekranı:** GorevVersions CRUD (tanım/KPI düzenle, yayınla=Rev N; SOP deseni).
- Ön-borç (platform, yeni değil): plaintext connstring (DataSources), inline SQL (sp_Ik*).

### Kararlar
- Mapping **kişi-düzeyi** (Personelno), rol-düzeyi (Departman+Unvan) DEĞİL — aynı ünvan farklı tanıma gider (Satın Alma Görevlisi → kişi Kırtasiye vs Oyuncak).
- Ekran = **Mosaik MVC modülü** (offline HTML reddedildi).
- **work-protocol** (kullanıcı direktifi): danış→yap→kontrol ettir→smoke, her iş. İçerik→bkm danışmanları, kod→reporthub danışmanları.

### Dikkat Edilmesi Gerekenler
- **İki repo:** kod = reporthub (`Mosaik.Modules.GorevTanimlari`, git), içerik/karar = `D:\Dev\gorevtanimlari\bkm` (git değil; DECISIONS G/H + docs/journal/2026-07-02.md + .claude/rules/work-protocol.md). Modül README + HANDOFF bu bağı verir.
- Personelno = STRING. IK connstring port'lu (,64507). `Ai:WarmupEnabled=false` dev.

### Yarına Başlangıç Noktası
1. Bu repo'da görev-tanımı işi = `Mosaik.Modules.GorevTanimlari` (README.md oku). İçerik/karar için bkm DECISIONS G/H.
2. İş → danış (kod=reporthub csharp/razor/security-conventions) → yap → kontrol ettir → smoke.
3. Somut: T-16 (test/view refactor) veya doküman-editör ekranı (GorevVersions CRUD).
