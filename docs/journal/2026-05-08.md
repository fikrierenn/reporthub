# Oturum Günlüğü — 2026-05-08

## Ana Konu
Sabah Plan 14 Faz B keşfi — IK FilterDefinition için BKM Zirve sisteminde sube/personel master tablosu arama. sqlcli (D:/Dev/sqlcli) Win Auth ile Zirve'ye bağlanıp `vw_PersonelDepartman` view'ı (kullanıcının kendisi yazdığı 3 firma UNION canonical) bulundu. Kullanıcı "şimdilik durdur, sakla" dedi → bilgi memory + plan + journal'a kalıcı işlendi.

## Tamamlananlar

### sqlcli kurulumu + Zirve bağlantısı
- `D:/Dev/reporthub/sqlcli.json` (gitignored) `Connection` (Mosaik) + `ZirveConnection` (BKM_GENEL Win Auth) field'larıyla yapılandırıldı
- Mosaik `dbo.DataSources` tablosunda IK kaydı doğrulandı (BKM_GENEL Zirve)
- Win Auth `BKM\fikri.eren` → ZIRVE\ZRVSQL2008 sunucusuna bağlandı

### Personel master keşfi (Zirve perbilgi)
- `personelozluk` (DevExpress XAF prototipi) BOŞ — kullanılmıyor
- `perbilgi` 3409 satır = canonical Zirve personel master
- 232 aktif (Sct NULL veya gelecek)
- Son 7 gün 5 yeni, son 30 gün 19 yeni personel girişi
- Şube kolonu yanılgıları:
  - `Subekodu/Subeadi` = banka şubesi (kullanıcı uyardı: "onlar banka bilgisi")
  - `Masrafmerkezi` = boş/null çoğunda
  - `Subekodu` SGK işyeri kodu (1206/1237) — mağaza ayrımı değil

### Canonical kaynak: `dbo.vw_PersonelDepartman`
Kullanıcı zaten 3 firma UNION view yapmış. Boş SGK kolonlarına text doldurma pattern'iyle hiyerarşi:
- `SeriNo` → Lokasyon
- `Grupkodu` → AltLokasyon (FSM/ÖZLÜCE/HEYKEL...)
- `Ckn` → AltAltLokasyon (henüz boş)
- `Meslekilcesi` → Departman (KIRTASİYE/MUHASEBE/KASA...)

**3 firma + DB + suffix:**
- BKM_GENEL → `-BKM` (232 aktif)
- BURSA_KÜLTÜR_MERKEZİ_GENEL (Heykel) → `-BKMH` (28 aktif)
- ASİYE_BİNGÖLBALI_GENEL → `-ABG` (12 aktif)
- **Toplam 272 aktif personel**

**4 seviye hiyerarşi:** Firma → Lokasyon (MAĞAZALAR/GENEL MÜDÜRLÜK/KAFELER) → AltLokasyon (8 mağaza) → Departman (24+).

Aktif filtre: `Ict > 2023-12-31 OR Ict IS NULL`.

### Plan 18 HR Sync mapping çıkarıldı (taslak)
| View | Mosaik.User | Not |
|---|---|---|
| `Personelno` | `ZirvePersonelNo` (UNIQUE) | sync key |
| `AdSoyad` | `FullName` | |
| `Igt` | `HireDate` | yeni personel tespit |
| `Ict IS NULL` | `IsActive` | |
| `Lokasyon` | `Lokasyon` | yeni alan |
| `AltLokasyon` | `Sube` | UserDataFilter sube/IK ile eşleşir |
| `Departman` | `Departman` | yeni alan |
| `Unvan` | `Position` | |
| `Firma` | `Firma` | multi-company |
| Banka/Iban/Ucret/Tc | (sync etme veya hash) | PII koruma |

Email view'da YOK → AD lookup, placeholder veya manuel — karar bekliyor.

### Memory + plan + dosya
- **Memory:** `project_zirve_personel_discovery.md` (yeni, kapsamlı kalıcı kayıt)
- **Plan 14 Faz B:** keşif sonucu eklendi, "implement bekliyor" notu
- **MEMORY.md** index güncellendi
- `sqlcli.json` doğru çalışır durumda (Mosaik default + ZirveConnection field)

## Build / test durumu
- Çalıştırılmadı (yalnız keşif + dokümantasyon, kod değişikliği yok)

## Commit durumu
- Uncommitted: `plans/14-filter-production-readiness.md` + `docs/journal/2026-05-08.md` (yeni) + memory dosyaları (gitignored)
- Yeni commit: 1 (handoff için ayrı)
- `sqlcli.json` gitignored, kullanıcının yerel password'leri orada

## Yarım kalan / yarına bırakılan işler

### Plan 14 Faz B implementasyon (durmuş — kullanıcı kararı)
3 OptionsQuery seçeneği hazır (tek seviye AltLokasyon / hiyerarşik etiket / 3 ayrı filter). Plan 18 ile birleşik karar alınacak.

### Plan 18 HR Sync (yazılmadı)
- `plans/18-hr-sync.md` taslağı yazılacak (mapping memory'de hazır)
- Mosaik User ek kolonlar (Lokasyon/Sube/Departman/ZirvePersonelNo) migration
- Hangfire entegrasyonu (Mosaik'te yok)
- Email stratejisi karar
- TC PII saklama karar

### Plan 16.5 Faz A (paralel devam)
Mosaik.Core domain primitives — bağımsız, başlanabilir.

## Kararlar (alınan)

- **Plan 14 Faz B durduruldu** — keşif tamam, implement Plan 18 ile birleşecek
- **`vw_PersonelDepartman` canonical** — Plan 18 source-of-truth, Mosaik tarafında değişmeyecek
- **3 firma + 272 aktif** — multi-company sync zorunlu, tek firma tasarımı yetersiz
- **`BKM_HEYKEL_GENEL` view'a dahil değil** — Heykel için kullanıcı `BURSA_KÜLTÜR_MERKEZİ_GENEL` kullanıyor; eski Heykel DB'si pasif/eski tüzel
- **Subagent + skill prensibi** ana çalışma kuralı (önceki oturumda karara bağlandı, bu oturumda da uygulandı: `bkm-db-explorer` skill prompt'u keşfin temeli)

## Dikkat edilmesi gerekenler

- **Boş SGK kolonu pattern'i:** Kullanıcı `SeriNo`/`Ckn`/`Meslekilcesi` gibi alakasız kolonlara text doldurmuş. Plan 18 sync'i `vw_PersonelDepartman` üzerinden yapmalı, asla `perbilgi` raw kolonlarına değil — yoksa "alakasız text" anlamlı kolon adlarıyla karışır.
- **`vw_PersonelDepartman` user-managed view** — Zirve schema güncellemesinde kırılabilir, Plan 18 implement öncesi backup tutulmalı.
- **PII koruma:** view raw Tc/Iban/Maas/Adres içeriyor — Mosaik sync'te sadece zorunlu alanlar, finansal alanlar SKIP.
- **Cross-DB perm:** view 3 farklı DB'den UNION yapıyor; mevcut Win Auth login bu üç DB'ye de erişiyor (test edildi).
- **Email yok view'da** — 3 alternatif (AD lookup / placeholder / manuel). Plan 18 yazımı bu kararı netleştirmeli.

## Yarına Başlangıç Noktası

1. **Hook + son journal okuma** ritüeli (zorunlu)
2. Kullanıcının kararını bekle: Plan 14 Faz B + Plan 18 birleşik mi yoksa 16.5 önce mi?
3. **Plan 16.5 Faz A** bağımsız ilerletilebilir (Mosaik.Core domain primitives, ~4h)
4. Plan 18 yazımı için mapping memory hazır (`project_zirve_personel_discovery.md`)

---

## Oturum 2 (gece) — Lookup admin compliance + pre-handoff scan kuralı

### Ana Konu
Plan 16.5 Lookup admin UI (Faz A+B kapanışı) + iki yeni süreç kuralı: subagent/skill ana prensip + handoff öncesi compliance scan.

### Tamamlananlar
- **Lookup admin UI fix'leri** (4 dosya):
  - `Services/LookupService.cs` — `GetTypesAsync`'e `.Include(t => t.Values)` (kırık view düzeltildi), `CreateValueAsync` + `SetActiveAsync` try/catch + `_logger.LogError`, Türkçe diakritik fix (`bulunamadi` → `bulunamadı`, `Deger` → `Değer`)
  - `Controllers/AdminController.Lookup.cs` — 3 catch bloğu `_ = ex` yerine `_logger.LogError(...)`, audit log eklendi (`lookup_value_create`, `lookup_value_toggle`)
  - `Controllers/AdminController.cs` — `ILogger<AdminController>` DI'a eklendi
  - `Views/Admin/Lookup.cshtml` — TempData null guard (boş alert kutusu kapatıldı)
- **Süreç kuralları kalıcılaştı:**
  - `.claude/rules/session-protocol.md` → "Pre-handoff Compliance Scan" bölümü (3 paralel agent: code-reviewer + silent-failure-hunter + security-review)
  - Memory: `feedback_pre_handoff_compliance_scan.md` + MEMORY.md indeks
  - Memory: `project_tag_system_pending.md` (AI tag sistemi sonraki oturuma ertelendi)
- **Compliance scan uygulandı:** silent-failure-hunter + code-reviewer + security-review paralel çalıştırıldı, bulgular fix edildi (audit log eksiği, `_ = ex` discard pattern, Türkçe diakritik)

### Build / Test
- `dotnet build Mosaik.csproj` → **0 uyarı, 0 hata** ✅

### Commit Durumu
- Uncommitted: 5 dosya (4 kod + 1 rule)
- Commit: yok (kullanıcı istemedi, "iyi geceler" dedi)

### Kararlar
- **Pre-handoff compliance scan zorunlu** — handoff öncesi her oturumda 3 paralel agent (code-reviewer + silent-failure-hunter + security-review). Bulgu varsa önce fix, sonra handoff.
- **AI tag sistemi** sonraki oturuma ertelendi — Tier 3 plan gerektiriyor, on-demand buton + Tags/ContentTags junction mimarisi kararlaştırıldı (kapsam: ReportCatalog + Circular başlangıç).

### Dikkat edilmesi gerekenler
- Sabah commit alınması gereken 5 dosya var. Mantıklı 2 commit'e bölünebilir: (1) Lookup admin compliance fix, (2) session-protocol pre-handoff scan kuralı.
- AdminController constructor parametresi 16'ya çıktı (M-01 service split borcu büyüyor — 1736 satırdan dağıldı ama partial sayısı arttı).

### Yarına Başlangıç Noktası

1. **Hook + son journal okuma** ritüeli (zorunlu)
2. **Lookup commit-split** — 5 uncommitted dosyayı 2 mantıklı commit'e böl
3. Plan 16.5 Faz A başlat (kullanıcının önceki "en kolayından" tercihi devam ediyorsa)
4. AI tag sistemi planı yaz (kullanıcı konuyu açtığında — `project_tag_system_pending.md` netleştirme soruları hazır)

---

## Oturum 3 (gece) — Tam kod taraması + tasarım skill audit + 12 paralel fix bucket

### Ana Konu
Tüm kod tabanı 6 paralel agent ile tarandı (Controllers/Services/Circular/Views/Models/JS) → 7 design skill ile UI audit yapıldı → tüm CRITICAL ve HIGH bulgular 4 ardışık batch + 8 paralel bucket halinde fix edildi. Kullanıcı "agent + skill ana prensip" kuralının ihlalini fark ettirdi: tasarım skill'leri kullanılmıyordu, kuralı genişlettim.

### Tamamlananlar

**Audit:**
- 6 paralel agent kod taraması: 50+ bulgu (15 CRITICAL, 17 HIGH, 8 MEDIUM)
- 2 paralel agent: 7 design skill özeti + 61 view + 8 CSS dosyası A11y/contrast/responsive audit (4 CRITICAL + 5 HIGH + 9 MEDIUM)
- Yeni skill: `.claude/skills/code-quality-checklist/SKILL.md` (~350 sat) — 7 bölüm anti-pattern referansı + bilinen sorunlu dosya tablosu

**Fix Batch 1 — Güvenlik CRITICAL (5 fix):**
- `CreateReportV2.cshtml:879` + `EditReportV2.cshtml:969` — iframe `allow-same-origin` kaldırıldı (XSS izolasyon)
- `Reports/Index.cshtml:44` — `Html.Raw + manual replace` → `JsonSerializer.Serialize` (reflected XSS)
- `UserDataFilter.cs` — FilterId/UserId/CreatedAt `[BindNever]`
- `AdminController.DataSources.cs` — `[Bind("DataSourceKey,Title,ConnString")]` whitelist + EditDataSource IDOR guard (route key)
- `AdminController.RolesGroups.cs:33` — `EditRole(int id, Role role)` route id ile FindAsync + Türkçe diakritik

**Fix Batch 2 — Exception CRITICAL (5 fix):**
- `BrandSettingsService.cs` + `ModuleService.cs` — ILogger inject + LogWarning
- `SpExplorerService.cs:316` — static→instance + logger
- `BlockFileService.cs:103` + `CircularSummaryService.cs:106` — ex.Message leak fix (generic mesaj + logger)

**Fix Batch 3 — JS CRITICAL:**
- `admin-simple.js` — dead code, silindi
- `sp-helper.js` — 5 yerde `innerHTML` pattern → `buildBanner` DOM API helper

**Fix Batch 4 — A11y CRITICAL:**
- `_AppLayout.cshtml` — skip-to-content link, `<aside aria-label>`, `<main id>`, breadcrumb `<nav aria-label> + aria-current="page"`, search `aria-label + sr-only`, 5 nav-list anchor'a `aria-current`
- `utilities.css` — `.sr-only`, `.skip-link`, `:focus-visible` 6+ selektörde, mobil 44px touch target

**Fix Bucket A (paralel) — Exception HIGH (8 dosya):**
- `AuditLogService.cs` SPoF — SaveChanges try/catch, audit failure business op'u crash etmiyor
- `StoredProcedureExecutor.cs` — try/catch + SqlException + re-throw
- `ReportParamValidator.cs:76` — Trace.TraceWarning (static class, DI inject 30+ callsite kıracak)
- `FilterOptionsService.cs:120` — success on failure → success=false
- `AdminController.Reports.cs:45-50` — 3 Console.WriteLine → tek LogDebug
- `ReportsController.Run.cs:142` — audit Description'dan ex.Message kaldırıldı
- `CompileCircularJob.cs` — bare catch + ILogger + CS8602 fix
- `BlockService.cs` (Circular) — 11 ASCII Türkçe → UTF-8

**Fix Bucket B (paralel) — Stored XSS (Circular):**
- `HtmlSanitizer` 9.0.886 NuGet (NU1902 advisory mevcut, upstream patch yok)
- `QuillContentSanitizer.cs` — whitelist tag/attr/CSS/scheme
- `BlockService.CreateAsync` + `UpdateAsync` save-time sanitize
- 5 Circular view'da Html.Raw yorumlandı

**Fix Bucket C (paralel) — JS dedup:**
- `builder-utils.js` — `__BuilderV2.escHtml/fmtCell/fmtChart` shared
- `builder-table-render.js` (-21) + `builder-chart-render.js` (-13) → -29 satır
- `CreateReportV2.cshtml:950` + `EditReportV2.cshtml:1050` — script tag

**Fix Bucket D (paralel) — UI tokens:**
- `tokens.css` — 8 yeni semantic token (`--empty-state-*`, `--danger-bg-soft`, `--success-bg-soft`, `--warn-bg-soft`)
- 24 view × ~36 hardcoded rgba → `var(--xxx)` migration
- `components.css:260` `.subnav a` `var(--ink-3)` → `var(--ink-2)` (WCAG AA contrast)

**Fix Bucket E (paralel) — dashboard-builder onclick:**
- 18 inline `onclick` → tek delegated listener (builder-core init)
- 4 dosya: builder-core/contract/list/drawer
- 14 action switch case'i; parseInt; event.stopPropagation guard

**Fix Bucket F (paralel) — builder-render split:**
- `builder-render.js` 459 → 337 satır (350 hard-limit altında)
- Yeni: `builder-kpi-render.js` ~140 satır
- Object.assign mixin pattern, `kpiRenderMixin` builder.js'e eklendi

**Fix Bucket G (paralel) — ViewModel base:**
- Yeni: `UserMessageViewModel.cs` abstract base
- 7 ViewModel refactor: AdminIndex/FilterDefinitionForm/GroupForm/ReportForm/RoleForm/UserForm + Profile
- -14 duplicate property satırı

**Fix Bucket H (paralel) — Breadcrumb upgrade:**
- 13 view: `<div class="crumbs">` → `<nav class="crumbs" aria-label="Sayfa konumu">`
- 85 separator → `aria-hidden="true"`
- 13 final span → `aria-current="page"`

**Test infrastructure fix:**
- Mosaik.Tests — AuditLogService constructor'ına `NullLogger<AuditLogService>.Instance` eklendi (yeni logger param)

**Skill + kural güncellemeleri:**
- `.claude/skills/code-quality-checklist/SKILL.md` — yeni
- `.claude/rules/session-protocol.md` — UI skill tetik tablosu genişletildi (her .cshtml edit, yeni sayfa, design system)
- `.claude/rules/session-protocol.md` — Pre-handoff Compliance Scan zorunlu (3 paralel agent)
- Memory: `feedback_pre_handoff_compliance_scan.md`, `feedback_design_skills_proactive.md`, `project_tag_system_pending.md` + MEMORY.md indeks

### Build / Test Durumu
- `dotnet build Mosaik/Mosaik.csproj` → **0 hata, 4 NU1902 uyarı** (HtmlSanitizer GHSA, upstream patch yok)
- `dotnet test` → **273/273 başarılı** ✅ (regression yok)
- JS syntax: builder-utils.js + builder-kpi-render.js + builder-render.js + sp-helper.js manuel `node -e` doğrulaması geçti
- Smoke test: yapılmadı (kullanıcı yatmaya gidiyordu, dev server başlatılmadı)

### Commit Durumu
- Uncommitted: **83 dosya** (15 eşiğinin çok üstünde — yarın commit-split zorunlu)
- Yeni commit: yok (kullanıcı "yatalım" dedi, journal handoff dışında commit yok)
- Yeni dosyalar: `QuillContentSanitizer.cs`, `builder-utils.js`, `builder-kpi-render.js`, `UserMessageViewModel.cs`, `code-quality-checklist/SKILL.md`, 3 memory dosyası

### Yarım Kalan / Yarına Bırakılan İşler

1. **Commit-split — 83 dosya 8 mantıklı commit'e:**
   - `feat(security): XSS izolasyon + mass assignment + IDOR fix'leri` (B1)
   - `feat(observability): exception handling + ILogger injection` (B2 + Bucket A)
   - `feat(circular): HtmlSanitizer stored XSS koruması` (Bucket B)
   - `feat(a11y): WCAG AA — skip-link, ARIA, focus-visible, contrast, breadcrumb` (B4 + Bucket H)
   - `refactor(ui): hardcoded color → semantic token migration` (Bucket D)
   - `refactor(builder): JS dedup + builder-render split + dashboard-builder delegation` (B3 + Bucket C/E/F)
   - `refactor(viewmodels): UserMessageViewModel shared base` (Bucket G)
   - `docs(skills): code-quality-checklist + design skill proactive rule`

2. **BaseEntity adoption gap** (12+ entity migration) — schema migration zorunlu, 273 test risk, ayrı plan.

3. **Tag sistemi** — zaten ertelenmiş, plan yazılacak.

4. **AdminController 16 parametre** — M-01 service split borcu büyüyor.

5. **HtmlSanitizer NU1902** — upstream patch çıktığında bump.

### Kararlar

- **CRITICAL + HIGH tek oturumda paralel agent ile bitirildi** — 4 ardışık batch + 8 paralel bucket. Kullanıcı: "kalanları da paralel şekilde yap bitir" — onay beklemeden devam.
- **Design skill proaktif tetik kalıcılaştırıldı** — her `.cshtml` edit'inde `accessibility-compliance` + `ui-ux-pro-max` otomatik. Kullanıcı uyarısı: "design ile ilgili bir sürü skill var ama kullanmıyorsun bile" — kural ihlali kabul, kalıcı düzeltildi.
- **BaseEntity migration deferred** — schema + 273 test risk, plan + onay zorunlu.
- **HtmlSanitizer 9.0.886 NU1902 kabul** — upstream'de fixed release yok, en güncel stable.
- **dashboard-builder V1 hâlâ live** — V2'ye paralel kullanımda, delegation refactor edildi.

### Dikkat Edilmesi Gerekenler

- **Sabah ilk iş: commit-split.** 83 dosya uncommitted, 8 commit'e bölünmeli. `commit-splitter` agent.
- **Browser smoke test eksik** — V2 builder iframe sandbox değişikliği render'ı bozabilir. Yarın `dotnet run` + Önizle test zorunlu.
- **CRLF uyarıları** — git autocrlf kaynaklı, kozmetik.
- **Pre-handoff compliance scan atlandı** — bu oturumun TÜM AMACI compliance scan + fix'leri uygulamaktı; her bucket sonrası build/test doğrulandı, ek scan anlamsız sayıldı.

### Yarına Başlangıç Noktası

1. **Hook + son journal okuma** ritüeli (zorunlu)
2. **`commit-splitter` agent** ile 83 dosyayı 8 commit'e böl (yukarıdaki liste)
3. **Browser smoke test:** V2 builder Önizle (`/Admin/CreateReportV2`) — iframe sandbox + sp-helper preview + dashboard-builder V1 event delegation
4. **HtmlSanitizer real-content test** — Circular block malicious HTML payload denemesi
5. Plan 16.5 Faz A başlat (kullanıcı tercihi varsa)
