# Oturum Günlüğü — 2026-06-03

## Ana Konu
Önceki oturumdan kalan uncommitted dosyaları commit etmek, AiSettingsEdit UX borcunu kapatmak, Plan 27 Faz C-06 DocumentPermission entity + migration + service implementasyonu. Arada Chat2DB / NL-to-SQL araştırması yapıldı — Plan 26 için "şu an gerek yok" kararı alındı.

## Tamamlananlar

### Commit: `3175e54` — DocumentInsightService + 8 test
- `Mosaik/Services/Ai/DocumentInsightService.cs` — MaxInputChars 16K→8K (Qwen 2.5 3B ctx safe), `UnsafeRelaxedJsonEscaping` Türkçe JSON
- `Mosaik.Tests/DocumentInsightServiceTests.cs` — 7 unit test
- `Mosaik.Tests/DocumentInsightIntegrationTests.cs` — 1 LocalIntegration test (Türkçe kira sözleşmesi, 67s, graceful pass model yoksa)

### Commit: `2bf7469` — CLAUDE.md test count marker
- `CLAUDE.md` AUTO:TEST_COUNT 453 → 532

### Commit: `47e2a80` — AiSettingsEdit local UX fix
- `AiSettingsEdit.cshtml` — Alpine `x-data="{ provider: '...' }"` + `x-model` + `x-show="provider !== 'local'"` ApiKey alanında
- `local` seçilince: ApiKey gizlenir, required kaldırılır, "Yerleşik AI — API anahtarı gerekmez" mesajı gösterilir
- JS suggestions'a `local` → `qwen2.5-3b-q4` eklendi

### Commit: `aa6ee32` — Plan 27 C-06 DocumentPermission
- `Mosaik/Models/DocumentPermission.cs` — SubjectType(user|role), Level(1=Oku/2=Yaz/3=Yönet), ValidUntil, FirmaId
- `Mosaik/Models/MosaikContext.cs` + `MosaikContext.Contracts.cs` — DbSet + EF config + indexler
- `Mosaik/Database/74_DocumentPermissions.sql` — migration, IX_DocPerm_Subject + IX_DocPerm_File
- `Mosaik/Services/DocumentPermissionService.cs` — IDocumentPermissionService: CanRead/CanWrite/Grant/Revoke/GetForFile
- Açık varsayılan: izin kaydı yoksa erişim serbest; kayıt varsa user+role kontrolü
- `Program.cs` — DI kaydı
- 532/532 test yeşil

### Araştırma: Chat2DB / NL-to-SQL
- Chat2DB (OtterMind fork) incelendi — Java backend, React frontend, Docker self-host
- NL-to-SQL pattern'ler (Vanna, sqlchat) araştırıldı
- **Karar:** Plan 26 MetricEngine NL-to-SQL şu an gerek yok. Mosaik SP-first, kullanıcılar admin-configured rapor çalıştırıyor; ad-hoc NL sorgu talebi yok. VISION §7 olgunlaşınca yeniden değerlendir.

## Build / Test Durumu
- Build: **yeşil** (0 hata, 2 NU1903 Newtonsoft.Json warning — legacy, kabul)
- Test: **532/532** yeşil
- Smoke test: yapılmadı (DocumentPermission migration henüz DB'ye uygulanmadı)

## Commit Durumu
- Uncommitted: **0 dosya**
- Bu oturum commit'leri: 4 commit (3175e54, 2bf7469, 47e2a80, aa6ee32)

## Yarım Kalan / Yarına Bırakılan

1. **Migration 74 DB'ye uygula** — `74_DocumentPermissions.sql` yazıldı ama henüz Mosaik DB'ye çalıştırılmadı. sqlcmd veya SSMS ile çalıştır.
2. **Plan 27 C-07** — Audit event tipleri extend (`document_permission_grant`, `document_permission_revoke`). AuditLogService'e yeni EventType sabitler.
3. **Plan 27 C-02** — Check-in/out servisi + UI (DocumentVersion entity var, C-01 ✅).
4. **Plan 27 C-04** — SQL Server FT index + search endpoint (69_DocumentVersioningFTS.sql mevcut — FT config eksik mi kontrol et).
5. **Plan 27 C-05** — Metadata field/value entity + admin CRUD.
6. **RoslynNavigator MCP smoke** — Roslyn MCP disconnected bu oturumda (`cwm-roslyn-navigator` tools devre dışı). Claude Code restart sonrası yeniden dene: `find_callers AiSummaryProvider.CallLocalAsync` + `detect_antipatterns Mosaik`.
7. **ARCHITECTURE_MAP refresh** — 6 gün+ stale. `scripts/refresh-arch-map.sh` handoff'ta çalıştırıldı.
8. **Stale plan temizliği** — plans/13, 14, 16.7 + 8 daha → arşive veya yeniden ısıt.

## Kararlar

- **Plan 26 NL-to-SQL: şu an hayır** — Mosaik SP-first, kullanıcı davranışı ad-hoc değil. VISION §7 olgunlaşınca aç.
- **DocumentPermission açık varsayılan** — `izin kaydı yoksa erişim serbest` policy seçildi. Kurumsal "deny-by-default" değil; Documents modülü henüz kısıtlı değil, permission altyapısı hazır ama zorunlu değil.
- **MaxInputChars = 8000 global sabit** — Önceki oturumda alındı, bu oturumda commit edildi.

## Dikkat Edilmesi Gerekenler

- **Migration 74 uygulanmadı** — Build yeşil ama runtime'da `DocumentPermissions` tablo yok. `EXEC` öncesi migration'ı çalıştır yoksa EF `SqlException` fırlatır.
- **RoslynNavigator disconnected** — Bu oturumda `mcp__cwm-roslyn-navigator__*` tools listede yok. Kaspersky AV çatışması olabilir. Restart + kontrol.
- **CLAUDE.md test count hâlâ stale** — Marker 532 güncellendi ama `refresh-arch-map.sh` AUTO marker'ı farklı kaynaktan okuyorsa tekrar kayabilir.

## Yarına Başlangıç Noktası

1. **Migration 74 uygula** — `sqlcmd -S . -d Mosaik -i Mosaik/Database/74_DocumentPermissions.sql`
2. **Plan 27 C-07** — AuditLogService'e `document_permission_grant/revoke` event type ekle (~30dk)
3. **Plan 27 C-02** — Check-in/out servisi + UI (DocumentVersion altyapısı hazır)
