# Oturum Günlüğü — 2026-06-29

## Ana Konu
Pusula/Operax yetenek transferi + çatı bloat budama + vizyon/rakip araştırması + Plan 54 vNext roadmap v2 (modül-modül seri + denetim kapısı) ve ilk 3 modülün (M1-M3) implementasyonu.

## Tamamlananlar

### Strateji / çatı
- **Pusula transfer:** `footprint-ladder.md` rule + `consolidate-mosaik` curator skill + `mosaik-portal-danismani` agent port edildi (`e3f96a4`).
- **reporthub bloat pasif:** MEMORY.md 25.5→14.8KB, 11 plan + 7 skill arşiv, stale rule trim (`c147a61`, `1d99587`).
- **Mosaik logon auto-start:** `scripts/start_mosaik.ps1` + `register-mosaik-task.ps1`, Task Scheduler AtLogon + tray, port 5197 (`ddd35c8`).
- **Plan 21 lookup TAM + arşiv:** 5 enum→DictionaryType (`75_SeedContractLookups.sql`) + Operax port IsSystemDefined lock + never-blank `LabelAsync` (`76`) + Alpine recurrence label (`7682ec5`/`036c20a`/`87584fc`/`1bcebfe`).
- **Vizyon + rakip araştırması:** `docs/RESEARCH_VISION_2026-06-29.md` + `docs/COMPETITIVE_SCOPE_2026-06-29.md` (CORE/ADJACENT/OUT + global/TR parity gap). `mosaik-portal-danismani` kapsam yeteneğiyle güçlendirildi (`23be649`/`16e8ac3`/`aa3e986`).
- **Plan 54 ONAYLANDI** (`a8c5e8b` + sonrası): modül-modül seri + denetim kapısı. Plan 53+16 superseded, 49/50/51 CUT, 40/41/42 düzeltme notu foldlandı.

### Modüller (Plan 54)
- **M1 SMTP** — ✅ verified-done. IEmailService (Plan 31) + INotificationService (Plan 17H) zaten canlı, kod gerekmedi. Digest→M5, Plan 32 scheduled-reports→Park.
- **M2 Unified Inbox** — ✅ `22bbc30`. LEAN: tek `IInboxProvider` + explicit DI + workflow/obligation provider + `/Inbox` + sidebar. Circular deferred (read-receipt entity yok). Authed smoke /Inbox 200, gerçek obligation kalemleri render.
- **M3 Cross-module Search** — ✅ `42fd98c`. LEAN: tek `ISearchProvider` + LIKE (FTS/vector yok) + report/contract/obligation/circular provider + `/Search` + topbar arama. SOP deferred (çok-katmanlı access). Authed smoke /Search 200 "Arama", security 0 leak.

## Build / Test Durumu
- Build: **yeşil** (0 hata, 2 legacy NU1903 Newtonsoft warning kabul).
- Test: **532/532 yeşil** (her modül kapanışında koşuldu).
- Smoke: M2 /Inbox + M3 /Search authed (admin login) **200 render** doğrulandı. Startup smoke (Now listening) her ikisinde geçti.

## Commit Durumu
- Uncommitted (excl `_archive`/`.codex`): **0**.
- Bu oturum ~18 commit. Branch: `feature/m-11-dashboard-builder-redesign`.

## Yarım Kalan / Yarına
- **M4 Dashboard→Alert** (sıradaki) — eşik aşımı → `INotificationService` fire, Hangfire sweeper. OI≠BI. Simplicity-gate: lean, mevcut Hangfire+Notification reuse, yeni engine yok.
- **Deferred (access modeli karmaşık):** Circular inbox provider (read-receipt entity yok), SOP search provider (dept/companywide/securitylevel/RAG çok-katman).
- **Park:** Plan 32 scheduled-reports+attachment, PWA(46), Auto-tuning(47), Executable-SOP(48), Trend(52).
- **VERIFY→arşiv adayı:** Plan 14 (Plan 18'e bağlı).

## Kararlar
- **Plan 54 modül-modül seri + denetim kapısı** (kullanıcı): tek modül açık, kapanmadan sonraki yok; her kapanışta o oturumda dokunulan TÜM modül denetlenir (build+test+4 paralel agent+portal-danışman+startup/authed smoke+arch-map+commit).
- **Simplicity-gate tüm modüllere** (kullanıcı): build öncesi minimum soyutlama; reflection/FTS/spekülatif framework yok. M2/M3 lean: ADR-018'in 5 capability+reflection yerine tek interface+explicit DI.
- **CUT 49/50/51** (kullanıcı onayı): biyometrik KVKK-risk / surveillance / tek-tenant overkill.
- **Differentiator/wedge:** local-LLM KVKK-safe on-prem AI (rakip cloud LLM eşleşemez).

## Dikkat Edilmesi Gerekenler
- **Tray Release dll kilidi:** tray (start_mosaik) çalışırken `bin/Release/*.dll` kilitli → `dotnet build -c Release` MSB3027 fail. Release rebuild öncesi tray'i durdur (Stop-ScheduledTask + Stop-Process watcher+dotnet), build, sonra Start-ScheduledTask.
- **Dev smoke login:** /Inbox + /Search [Authorize] — authed smoke için login şart (kimlik kullanıcıda; journal'a yazılmadı).
- **Git Bash URL path-conv:** curl `/Search?q=` → MSYS `C:/Program Files/Git/...` mangle. `export MSYS_NO_PATHCONV=1` kullan.
- **Çift userId claim:** AuthController hem ClaimTypes.NameIdentifier hem "userId" issue ediyor (eşdeğer). Yeni kod yalnız `ICurrentUserService` kullansın (Reports eski path — drive-by düzeltme yok).
- **Login path = `/Login`** (`/Auth/Login` değil — 404).

## Yarına Başlangıç Noktası
1. **M4 Dashboard→Alert** başla — simplicity-gate (lean, Hangfire+INotificationService reuse, EscalationRule), sonra build → denetim kapısı → authed smoke → commit. Plan 54'teki M4 satırı.
2. Denetim kapısı şablonu: 4 paralel agent (code-reviewer + silent-failure-hunter + security-reviewer + mosaik-portal-danismani) scope=o oturum diff.
3. Release smoke için ÖNCE tray durdur (dll kilidi), build, restart.
