# Oturum Günlüğü — 2026-05-27

## Ana Konu
İki paralel iş: (1) Documents AI'yı yerleşik LLamaSharp/Qwen runner'ına bağlama (Plan 27 Faz B follow-up); (2) `codewithmukesh/dotnet-claude-kit` deep audit + selektif manuel cherry-pick (RoslynNavigator MCP + C# 14 modern features + Result pattern rule).

## Tamamlananlar

### (A) Yerleşik AI provider — `AiSummaryProvider` "local" case
- `Mosaik/Services/AiSummaryProvider.cs:5` — `using Mosaik.Core.AI.Local`
- `Mosaik/Services/AiSummaryProvider.cs:20-38` — opsiyonel `ILlmRunner? _localRunner` constructor inject
- `Mosaik/Services/AiSummaryProvider.cs:75-78` — provider switch'e `"local"` case
- `Mosaik/Services/AiSummaryProvider.cs:343-378` — `CallLocalAsync` metodu (Qwen 2.5 3B Q4 inference + `RequireJson=true` ise `ExtractJsonBlock` ile JSON ayıkla; runner not-ready → fallback)
- `Mosaik/Views/Admin/AiSettingsEdit.cshtml:78` — provider dropdown'a `"Yerleşik AI (Qwen 2.5 3B — KVKK uyumlu, $0)"` option
- `Mosaik.Tests/AiSummaryProviderTests.cs` — 3 yeni test: `Local_RunnerReady_ReturnsSuccess`, `Local_RunnerNotReady_FallsBackToNextProvider`, `Local_RequireJson_ExtractsJsonBlock` + `FakeLocalRunner` fake
- Build: yeşil (0 hata, 2 NU1903 Newtonsoft.Json warning legacy)
- Test: **524/524** geçti (önceki 521 + 3 local)

### (B) dotnet-claude-kit manuel cherry-pick
- `dotnet tool install -g CWM.RoslynNavigator` (v0.7.0) — global tool
- `~/.claude.json` user-scope MCP register: `cwm-roslyn-navigator` stdio
- **15 semantic tool** (Claude Code restart sonrası `mcp__cwm-roslyn-navigator__*` aktif): find_symbol/references/callers/implementations/overrides/dead_code, get_type_hierarchy/public_api/symbol_detail/project_graph/dependency_graph/diagnostics/test_coverage_map, detect_antipatterns, detect_circular_dependencies
- `.claude/rules/csharp-conventions.md:134-170` — **C# 14 modern features** bölümü eklendi (primary ctor / `field` kw / collection expression / record / extension members / raw string / `Span<T>`); karar matrisi + anti-pattern + Mosaik pratik (yeni kod modern, legacy touch-ettikçe — drive-by refactor yok)
- `.claude/rules/error-handling.md` — **yeni rule dosyası** (137 satır, 200 eşik altı): Result pattern + ServiceResult Mosaik bağlamı + exception handling 5 mutlak kural + ProblemDetails opsiyonel + anti-pattern tablosu
- `CLAUDE.md` § Dosya konvansiyonları — `csharp-conventions.md` (C# 14 section) + yeni `error-handling.md` referansları
- `CLAUDE.md` § Roslyn MCP — 2026-05-27 satırı eklendi

### (C) Reddedilenler (kasıtlı, dökümantasyonlu)
- 10 specialist agent (Mosaik 11 agent var, ~%80 overlap)
- 16 slash command (Mosaik `/feature-dev`, `/review-pr`, `/security-check` zaten var)
- 5 template (Mosaik kurulu; minimal-api/Blazor/worker hiç biri MVC değil)
- Architecture skill'leri VSA/Clean/DDD (ADR-002 modüler monolith sabit)
- minimal-api/Aspire/api-versioning skill'leri (MVC + Razor, geçersiz)
- Plugin marketplace install (`/plugin install dotnet-claude-kit`) — sürpriz dependency + agent çakışma + rule conflict

## Build / Test Durumu
- Build: **yeşil** (0 hata, 2 NU1903 Newtonsoft.Json warning legacy — Mosaik.Modules.SOP transitive)
- Test: **524 yeşil, 0 başarısız, 0 atlandı** (önceki 521 + 3 yeni AiSummaryProvider Local case)
- Smoke test: yapılmadı (Qwen gerçek PDF Türkçe summary kalitesi henüz ölçülmedi)
- RoslynNavigator smoke test: Claude Code restart bekliyor (`mcp__cwm-roslyn-navigator__*` aktif değil yet)

## Commit Durumu
- Uncommitted bu oturum başladığında: **0**
- Bu oturumda değişen / yeni dosya: **8** (commit-split eşiği 15'in altında)
  - `M  CLAUDE.md` (Roslyn MCP + error-handling referans + arch-refresh auto markers)
  - `M  .claude/rules/csharp-conventions.md` (C# 14 modern features)
  - `??  .claude/rules/error-handling.md` (yeni rule)
  - `M  Mosaik/Services/AiSummaryProvider.cs` (ILlmRunner inject + Local case)
  - `M  Mosaik/Views/Admin/AiSettingsEdit.cshtml` (Yerleşik AI dropdown)
  - `M  Mosaik.Tests/AiSummaryProviderTests.cs` (3 yeni test + FakeLocalRunner)
  - `M  docs/ARCHITECTURE_MAP.md` (auto-refresh § 7/12/13)
  - `~/.claude.json` (user-scope, repo dışı — MCP register)
- **Commit beklemede:** Henüz commit edilmedi. Kullanıcı onayı bekliyor. Mantıksal commit-split önerisi:
  - `feat(ai): yerleşik AI (Qwen) provider 'local' case + 3 test` → AiSummaryProvider + AiSettingsEdit + AiSummaryProviderTests
  - `docs(rules): C# 14 modern features + error-handling rule + Roslyn MCP referans` → csharp-conventions + error-handling + CLAUDE.md
  - `docs(map): ARCHITECTURE_MAP auto-refresh 2026-05-27` → handoff skill ayrı atacak

## Yarım Kalan / Yarına Bırakılan
1. **Qwen gerçek PDF smoke test** — Türkçe sözleşme PDF'i yükleyip `DocumentInsightService` üzerinden yerleşik AI'ya düşür, JSON summary + tag kalitesini ölç. Context 4096 token (~3K char) limiti `DocumentInsightService.MaxInputChars=16000` üstü = kırılır → MaxInputChars'ı local provider için dinamik düşürmek gerekebilir.
2. **AiSettingsEdit ApiKey HTML5 required** — local için anlamsız ama dropdown'a `local` seçilince input `required` attribute Alpine/JS ile dinamik kaldırılmadı. Kullanıcı dummy "n/a" yazmak zorunda. UX küçük borç (Tier 2 follow-up).
3. **Plan 27 Faz C C-02/C-04/C-05/C-06/C-07** — Check-in/out, FTS index (LIKE fallback aktif), metadata field/value, Document permission middleware, audit event extend. C-01 ✅ (DocumentVersion entity + migration 69), C-03 ✅ (App_Data + Download Plan 33 BUGFIX-3). En mantıklı sıradaki: **C-06 DocumentPermission entity + middleware** (Plan 44 chunk-level permission Documents'a port — zemin taze).
4. **Plan 27 Faz E cross-doc RAG** — Documents için RAG pipeline (ContractFileChunks tablo + DocumentEmbeddingJob + Retriever + Advisor). Yerleşik AI provider çalışınca doğal follow-up. C-06 permission Faz E öncesi şart (chunk leak engelle).
5. **RoslynNavigator smoke** — Claude Code restart sonrası `find_callers SopChunkRetriever.SearchAsync` + `detect_antipatterns Mosaik` + `find_dead_code Mosaik` 3 query, token kazancı + Kaspersky AV çatışma kontrolü.
6. **6 stale plan** — 14+ gün dokunulmamış: plans/13, 14, 16.7, vs. Plan ölüm tarihi disiplini (`.claude/rules/plan-first.md` Adım 6) — yeniden ısıt veya arşive.

## Kararlar
- **Yerleşik AI entegrasyonu = mevcut switch'e provider case** (yeni IAiSummaryProvider impl değil). Sebep: `AiSummaryProvider` zaten provider-agnostic router, "local" satırı eklenerek fallback chain'e doğal entegre olur, DocumentInsightService/DocumentChatService/AiExtractionWorker kod değiştirmeden otomatik yararlanır.
- **dotnet-claude-kit plugin install YAPMA**, manuel cherry-pick yap. Sebep: 47 skill context şişirir, 10 agent ~%80 Mosaik agent overlap, rule conflict riski, "olur dependency yarın gelir" sürprizi.
- **RoslynNavigator MCP user-scope kayıt** (`--scope user`) — proje-spesifik değil; Mosaik dışı .NET projelerinde de kullanılır. `~/.claude.json` repo dışı.
- **modern-csharp + error-handling = sadece içerik adapte**, skill dosyası IMPORT EDİLMEDİ. Mosaik kuralları (`.claude/rules/`) tek source-of-truth, ek katman istemiyor. ADR-004 skill-design-principles uyumlu.
- **Architecture skill'leri SKIP** (VSA/Clean/DDD). Sebep: ADR-002 modüler monolith canonical. Karışıklık yaratır.
- ADR yazılacak? **Hayır şimdilik.** Cherry-pick işi mimari karar değil tool seçimi; CLAUDE.md + error-handling.md referansları yeterli. Roslyn MCP token kazanç datası gelirse ADR-026 sonrası ADR-027 düşünülebilir.

## Dikkat Edilmesi Gerekenler
- **Arch-map refresh test count 445 gösterdi**, gerçek 524. Refresh script TestExplorer cache veya farklı counter kullanıyor — yanıltıcı. CLAUDE.md AUTO:TEST_COUNT marker güncel değil. İleride script'i `dotnet test --list-tests | wc -l` veya `xunit.runner.json` parse ile düzelt.
- **`mcp__cwm-roslyn-navigator__*` Claude Code restart olmadan aktif değil.** Bu oturumda smoke test yapılamadı. Restart sonra ilk iş: `find_callers AiSummaryProvider.CallLocalAsync` test query.
- **Qwen JSON output** — strict `response_format=json_object` yok (LLamaSharp StatelessExecutor). System prompt'ta JSON istemi + `ExtractJsonBlock` markdown fence/curly brace ayıklamaya güveniyoruz. Gerçek prod'da malformed JSON oranı bilinmiyor — smoke test öncelikli.
- **`Mosaik.Core/Ai/` (lowercase) vs `Mosaik.Core/AI/` (uppercase)** — `using Mosaik.Core.AI.Local` doğru (uppercase AI), `using Mosaik.Core.Ai` (mixed case). Windows case-insensitive ama git case-sensitive — refactor riski (Plan 44 dün benzer kaybetme yaşadı). `AiSummaryProvider.cs` her iki using'i kullanıyor — tek namespace'e konsolide etmek ileride gerekli olabilir.
- **dotnet-claude-kit `convention-learner` skill** — Mosaik'in `instinct-system` analogu yok ama auto-memory `feedback_*.md` + `project_*.md` benzer pattern. İleride confidence scoring formalize edilebilir (Tier 3 düşünce, şimdi gerek yok).

## Mimari Etkileri
- `AiSummaryProvider` artık opsiyonel `ILlmRunner` taşıyor. Production etkisi yok (default null = mevcut davranış). Test izolasyonu için FakeLocalRunner pattern eklendi.
- 6 modül × `IAiSummaryProvider` tüketicisi (`DocumentInsightService`, `DocumentChatService`, `AiExtractionWorker`, `AiExtractionWorker.Stage3`, `WizardExtractionService`, `LlmServiceAdapter`) hepsi otomatik yerleşik AI'dan yararlanır — kod değişikliği yok.
- ARCHITECTURE_MAP § 7 AppModules + § 13 routes değişmedi; § 12 admin views envanteri AiSettingsEdit görünür stat değişimi yansıttı.

## Yarına Başlangıç Noktası
1. **Claude Code restart** + RoslynNavigator MCP smoke (`find_callers AiSummaryProvider.CallLocalAsync` + `detect_antipatterns Mosaik`) — token kazancı kanıtla.
2. **Qwen Türkçe smoke test** — bir Türkçe sözleşme PDF'i + `DocumentInsightService` üzerinden yerleşik AI çağrı, JSON kalite + token sayım + latency ölç. MaxInputChars 16000 → 3000 düşürme gerek mi karar ver.
3. **Sonra:** Plan 27 Faz C-06 DocumentPermission entity + middleware (Plan 44 chunk-permission pattern Documents'a port). VEYA Plan 27 Faz E cross-doc RAG iskeleti — yerleşik AI çalışırsa doğal follow-up.
