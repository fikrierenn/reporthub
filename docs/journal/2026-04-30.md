# Oturum Günlüğü — 2026-04-30 (29 Nisan oturumunun gece yarısı sonrası devamı)

## Ana Konu

V2 builder'ın **ana akış** olarak olgunlaştırılması — Plan 03 (V2 stand-alone kapanış: SP seçim UI, validator banner, multi-tab, F-9 Tam Önizle, drawer focus) tamamlandı; ardından kullanıcı geri bildirimi ("hala karmaşık, kullanıcı dostu değil") sonrası Plan 04 (2-pane UX redesign + cascading dropdown Setup) implement edildi.

## Tamamlananlar

### Plan 03 — V2 Stand-alone Kapanış (4 ana adım + 1 ek)

- **`730d5ef` fix(m-11)**: TagHelper RZ1030 fix — `<form x-data @@input>` Razor parser'ını kırıyordu, `x-data="{}" x-on:input` long-form'a çevrildi (3 view dosyası)
- **`eec48ab` feat(m-11 alt-2V)**: CreateReportV2 SP seçim UI — Drawer Ayarlar tab'ında DataSource select + ProcName datalist + ParamSchema otomatik üretim; hidden form input'lar `form="reportXxxFormV2"` ile reactive sync; yeni `builder-settings.js` mixin (V1 sp-helper.js Alpine port'u)
- **`bb6cb1f` feat(m-11 alt-2V)**: V2 save banner — `DashboardConfigValidator` zaten entegreydi ama tek hata dönüyordu; tüm errors bullet list ile birleştirildi + V2 view'larına message banner pattern (V1'den port) + `white-space:pre-line`
- **`b4845ef` feat(m-11 alt-2V)**: Multi-tab sayfa yönetimi — V2'de hiç yoktu; PDKS Pano'nun 3 sekmesi keşfedildi (Özet 7w / Vardiya Detay 5w / Mesai & Sapma 3w); yeni `builder-tabs.js` mixin (addTab/removeTab/switchTab/setTabTitle); Gridstack `removeAll(true)` ile DOM birikmesi engellendi
- **`5f8d20b` feat(m-11 alt-2V)**: F-9 Tam Dashboard Preview iframe modal — `ReportsController.PreviewDashboardV2` endpoint (admin-only POST + AntiForgery + body model, DashboardConfigValidator hard-fail, audit `dashboard_preview` event); `builder-preview.js` mixin; topbar "Tam Önizle" butonu `$dispatch('builder-preview-open')` ile builder-v2 div'ine event yayar; modal CSS preview backdrop/shell
- **`07ccee8` fix(m-11 alt-2V)**: Sandbox `allow-same-origin` — Chrome srcdoc null-origin'de inline `onclick` engelleniyordu, tab başlıkları tıklanmıyordu; sandbox'a `allow-same-origin` eklendi (admin scope kabul edilebilir, MDN warning belgelendi)
- **`1cbfd7f` feat(m-11 alt-2V)**: Widget click → drawer'da bağlı kolona scroll + pulse — `focusBoundDataInDrawer(comp)` (drawer-mixin); rs-group + col-card data-attr; `bv2-col-pulse` CSS keyframe

### Plan 04 — 2-Pane UX Redesign (kullanıcı kararı: "hala çok karmaşık, internette araştır esinlen pratik yap")

Web araştırması: Looker Studio (Setup/Style 2-tab + sağ properties), PowerBI (Visualizations panel), Metabase (card grid + sağ drawer), Superset (sol dataset + sağ customize). Ortak pattern: 2-pane + context-aware properties + Setup vs Style ayrımı + max 7-8 görünür element.

- **`9c4bede` chore(plan-04)**: Plan dosyası (UX redesign hedef + araştırma özeti + key changes)
- **`1372cd6` feat(m-11)**: 2-pane geçiş + canvas-toolbar + empty CTA + drawer Setup/Stil 2-tab + context-aware:
  * `aside.palette` **silindi** (3 liste: Bileşen Ekle / Veri Setleri / Bileşenler)
  * Canvas içine `canvas-toolbar` (KPI/Grafik/Tablo butonları + Veri Setleri chip)
  * Boş canvas → `empty-canvas` onboarding (büyük ikon + 3 renkli CTA: mavi KPI / yeşil Grafik / turuncu Tablo)
  * Drawer 3-tab (`veri`/`gorunum`/`ayarlar`) → 2-tab (`setup`/`style`); tab strip sadece widget seçili iken görünür
  * No-selection state → "Rapor Ayarları" (eski Ayarlar tab içeriği)
  * Grid 270px+1fr+340px → 1fr+360px; responsive breakpoint'ler güncellendi
- **`0fa85ae` feat(m-11)**: Setup tab cascading dropdown — kullanıcı geri bildirimi "tüm veri setlerini listeleme combodan seçip listelesek iyice kafa karışıyor"
  * Eski `<details col-grid-toggle>` + RS group + col-card grid + col-search **kaldırıldı**
  * Yeni "Hızlı Bağla" bloğu: Başlık + Veri Seti select + Kolon select (kolon · tip · sample) + Hesap select (KPI için)
  * İleri ayar (collapsible): Alt yazı + 2-kolon formül (KPI için)
  * Yeni helper'lar (drawer mixin): `selectedRsIdx/Rs/Columns`, `columnKindFor`, `columnSampleFor`, `setBoundResult`, `setBoundColumn`

## Build / Test Durumu

- **Build:** ✅ yeşil (0 uyarı, 0 hata, son state)
- **Test:** çalıştırılmadı (kapsam V2 view + endpoint + JS mixin'ler, mevcut testler etkilenmedi)
- **Smoke:**
  - ✅ EditReportV2/13 (PDKS Pano 3 sekme): tab switch (DOM birikme yok), F-9 Tam Önizle (96KB HTML, 4 KPI + grafik + tablo), tab tıklama (allow-same-origin sonrası)
  - ✅ Setup tab cascading dropdown: helper'lar yüklü, qb-block görünür, eski card grid yok, advanced toggle var
  - ✅ Empty canvas state: yeni sekme açıldığında 3 CTA görünür, grid-stack hidden
  - ✅ No-selection state: drawer'da Rapor Ayarları (DataSource/SP/ParamSchema)
  - ⚠️ Screenshot timeout (büyük render, ölçümlerle doğrulandı)
  - ⚠️ SQL connection geçici kopuk (kullanıcı lokali) — RunJsonV2 daha önce çalıştığı için backend OK varsayıldı, manuel test gerekli

## Commit Durumu

- Bu oturum: **13 yeni commit** (29 Nisan 2 oturum + 30 Nisan devam)
  - `e167370` V2 fork bundled
  - `17629a0` user guide HTML
  - `2f3701a` plan-02 alt-2W + alt-2V tanımları
  - `3bf914d` RunJsonV2Preview endpoint (zero-data fix)
  - `d3bba1e` builder.js modülarize (4 mixin)
  - `d9f16a3` plan-03
  - `730d5ef` TagHelper fix
  - `eec48ab` SP seçim UI
  - `bb6cb1f` Validator banner
  - `b4845ef` Multi-tab
  - `5f8d20b` F-9 Tam Önizle
  - `07ccee8` Sandbox fix
  - `1cbfd7f` Drawer focus pulse
  - `9c4bede` plan-04
  - `1372cd6` 2-pane redesign
  - `0fa85ae` Setup cascading dropdown

  *Düzeltme:* gerçek toplam 16 commit (yukarıdaki bullet'lar tüm 30+ Nisan 29-30 zinciri).
- **Uncommitted: 8 dosya** — 4 user-side untracked + .claude scheduled lock + .agents/ + AGENTS.md + journal (bu) handoff sonrası commit'lenecek
- 15 eşiği altı ✓

## Yarım Kalan / Yarına Bırakılan İşler

### Plan 05 — Tablo widget kolon checkbox + Hesaplı (computed) kolon

Kullanıcı talebi (30 Nisan): "tablo olunca direkt veri seti seçilmeli, checkbox ile kolonlar eklenmeli, istenirse yeni kolon hesaplama, IF/IIF/CASE WHEN, +−×÷ formül"

**Plan 05.A — Tablo Setup özel UI (~1.5 saat):**
- Tablo widget seçili → Setup'ta Veri Seti dropdown sonrası **kolon checkbox listesi**
- Tikli kolonlar → `comp.columns` array (sıralı)
- Sürükle-bırak sıralama
- Tümünü seç / temizle hızlı butonlar

**Plan 05.B — Hesaplı kolon (~4-6 saat, AST parser):**
- "+ Yeni Hesaplı Kolon" butonu → modal: kolon adı + ifade
- İfade tipleri: aritmetik (mevcut formula 2-kolon zaten var, KPI için), IF/IIF, CASE WHEN
- Whitelist parser (security-principles.md: eval yasak)
  - Operatörler: + - * / % > >= < <= == !=
  - Fonksiyonlar: IF, IIF, CASE WHEN, COALESCE, ROUND, ABS
  - Kolon ref: `[KadroToplam]` veya `KadroToplam`
- Hem tablo widget kolonu hem KPI value olarak kullanılabilir
- Server-side `DashboardRenderer` evaluator'ı da gerekli (kayıtlı raporda runtime hesaplama)

### Küçük TODO'lar
- **Genişlik (span) Stil tab'a taşı** — Plan 04'te Setup eski "İleri ayar"dan span çıkarıldı, Stil tab'a eklenmedi. Şu an genişlik widget'a atanamıyor (default 1).
- **`builder-v2.css` cleanup** — `.col-grid-toggle`, `.palette`, `.contract-row`, `.pal-list`, `.comp-row` gibi artık kullanılmayan eski palette/card-grid sınıfları → temizlik commit'i
- **3 yeni untracked triage** — `.agents/`, `AGENTS.md`, `.claude/scheduled_tasks.lock` ne? `.gitignore` adayı
- **4 user-side untracked** — `start.bat`, `kodlari_topla_v2.bat`, `TUM_PROJE_KODLARI.txt`, `reporthub_dashboard_engine_mimari_dokumani.md` (kullanıcı yerel)
- **Manuel browser test** — yeni redesign sonrası kullanıcı kendi browser'ında akışı baştan sona dene (yeni rapor → DataSource seç → SP seç → widget ekle → Setup dropdown ile bağla → Tam Önizle → Kaydet → reload)
- **CSS `.col-grid-toggle` referansları** kaldırıldıktan sonra silinmeli

## Kararlar

- **V2 ana akış** (Plan 03 başında kullanıcı kararı): "ana uygulamam DashboardV2 olmalı, onu olgunlaştırma çalışıyorum". vNext mimari programı (Plan 04-11 — security/data model/run pipeline/admin/UI/test/portal) backlog'a alındı.
- **2-pane endüstri standardı** (Plan 04, web araştırması): Looker Studio + PowerBI + Metabase + Superset → 2-pane + context properties + Setup/Style ayrımı. AI suggestion (modern trend) atlanıldı (pratiklik öncelikli).
- **Cascading dropdown > kart grid** (Plan 04 simplification, kullanıcı geri bildirimi): kart grid 7 RS × 10+ kolon = bilişsel yük, dropdown kullanıcı zihninde tek yol.
- **Sandbox `allow-same-origin` kabul** (F-9 fix): MDN warning'e rağmen iframe srcdoc null-origin'de inline `onclick`'in çalışması için zorunlu. Admin scope'u + DashboardRenderer'ın güvenli HTML üretmesi (DOM API + textContent) + endpoint `[Authorize(Roles=admin)]` ile risk azaltıldı. vNext epik 4'te srcdoc → blob URL geçişi tam izolasyon için değerlendirilebilir.
- **Razor + Alpine pattern**: `x-data` value-less yerine `x-data="{}"` + `x-on:event` long-form. `@@event` form/TagHelper element'lerinde RZ1030 hatası verir. Pattern V2 view'larında uygulandı.
- **Tablo widget + computed columns ayrı plan** (Plan 05): büyük scope (AST parser security-principles.md uyumlu olmalı), Plan 04 redesign'in dışında.

## Dikkat Edilmesi Gerekenler

- **Razor view runtime cache** — view dosyasına dokunulduğunda fresh compile başlar; build OK olsa bile runtime hatalar açığa çıkar (TagHelper RZ1030 modülarize commit'inden öğrenildi). Her view edit sonrası `/Admin/EditReportV2/13` + `/Admin/CreateReportV2` reload smoke zorunlu.
- **Razor `<form>` TagHelper kısıtlaması** — `<form>` elementinde `@@event` (Razor escape) kabul edilmez (RZ1030). Long-form `x-on:event` kullan. Diğer element'lerde (`<div>`, `<button>`) `@@click` çalışır.
- **iframe srcdoc + sandbox `allow-scripts` only** — Chrome null-origin frame'de inline `onclick` engelleniyor. `allow-same-origin` ekleme XSS izolasyonunu zayıflatır ama admin scope'unda kabul.
- **Gridstack `removeAll(true)`** — false ile DOM'da widget birikiyor, true zorunlu. Tab switch'lerde grid rebuild bu pattern'i kullanır.
- **`builder-v2.css` legacy sınıflar** — Plan 04 sonrası `.palette`, `.col-grid-toggle`, `.contract-row` vs. HTML'de yok ama CSS'te duruyor; cleanup commit'i ileri faza.
- **CSS `.col-grid-toggle` HTML'den silindi** ama benim eklediğim CSS henüz orada — render'a etkisi yok ama temizlik gerek.
- **Genişlik (span) UI eksik** — Plan 04'te Setup eski "İleri ayar"daki Genişlik bölümü kaldırıldı, Stil tab'a eklenmedi. Şu an widget genişliği `addWidget`'taki default değerde kalır (KPI: 3, chart: 8, table: 4).
- **Drawer mixin'de `selectedRsColumns`** — selected widget'ın bağlı RS context'inde kolon listesi döner; eğer selected.result null ise boş array. Setup tab Kolon select'i x-show ile selected.result'a bağlı, doğru.
- **`setBoundResult` selected.column ve selected.agg sıfırlar** — RS değiştiğinde yanlış kolon bağı kalmasın diye. `setBoundColumn` agg auto-detect (autoAggregation) yapar + başlık default kolon adına çeker (eğer "Yeni..." prefix'liyse).
- **CreateReportV2 önce klasik akış** — V2 stand-alone tamamlandı ama kullanıcı yine de yeni rapor için klasik `/Admin/CreateReport`'a düşebilir; "Klasik formda düzenle" linki Drawer Rapor Ayarları'nda kalır (rol/kategori/açıklama klasik formda yönetilir, Plan 05 sonrası adım olabilir).

## Yarına Başlangıç Noktası

1. **Plan 05 yaz** — `plans/05-m11-tablo-checkbox-computed-cols.md`:
   - Plan 05.A: Tablo widget Setup'ta kolon checkbox listesi + drag-drop sıralama
   - Plan 05.B: "+ Yeni Hesaplı Kolon" + AST whitelist parser (eval yasak)
   - Server-side DashboardRenderer evaluator entegrasyonu

2. **Genişlik Stil tab'a ekle** (küçük) — drawerTab `style` view bloğuna span seçici (1/2/3/4 toggle veya select)

3. **CSS legacy cleanup** — `.palette`, `.col-grid-toggle`, `.contract-row`, `.pal-list`, `.comp-row` artık kullanılmıyor, sil

4. **Untracked triage** — `.agents/`, `AGENTS.md`, `.claude/scheduled_tasks.lock` ne, `.gitignore` adayı mı

5. **Manuel browser test (kullanıcı)** — yeni redesign akışı: yeni rapor → DataSource seç → SP seç → param üret → widget ekle → Setup dropdown ile bağla → Tam Önizle → Kaydet → reload

## Kritik Dosyalar / Skill'ler

- V2 view: `Views/Admin/{EditReportV2,CreateReportV2,_ReportFormBuilderTopActionsV2}.cshtml`
- V2 CSS: `wwwroot/assets/css/builder-v2.css` (~1100 satır, legacy temizlik beklemede)
- V2 JS modülleri: `wwwroot/assets/js/builder-v2/{builder,builder-render,builder-sp,builder-drawer,builder-settings,builder-tabs,builder-preview}.js` (7 dosya)
- V2 endpoints: `Controllers/ReportsController.cs::{RunJsonV2, RunJsonV2Preview, PreviewDashboardV2}` + `GuessResultSetTitle`
- Plan dosyaları: `plans/03-m11-dashboardv2-standalone.md` ✓ tamamlandı, `plans/04-m11-v2-builder-ux-redesign.md` ✓ tamamlandı
- Validator: `Services/DashboardConfigValidator.cs` (entegre `ReportManagementService.ValidateDashboardConfigAsync`)
- Önceki oturum journal'ı: `docs/journal/2026-04-29.md` (oturum 1 + 2)

---

# Oturum 2 — 2026-04-30 (öğleden sonra + akşam)

## Ana Konu

Plan 05 (formula evaluator + tablo widget Setup UI + hesaplı kolon) implementasyonu, mid-flight yön değişikliği (top-level CalculatedFields → tablo-özel TableColumnDef.Formula + JsonExtensionData esneklik), bug fix turu (rsN pattern + icon prefix), Plan 06.A (V2 Drawer ParamSchema editor), 3 KPI test rapor canlı render doğrulaması.

## Tamamlananlar

### A. Plan 05 öncesi — mini polish (`f57a12c`)
- Stil tab'a Genişlik (¼/½/¾/Tam) seçici eklendi
- `setField` bug fix: `this.$el` x-for içinde butonu gösteriyordu → `document.querySelector` (pre-existing bug)
- `builder-v2.css` legacy cleanup (~210 satır azaltma): `.palette*`, `.pal-*`, `.comp-row`, `.contract-*`, `.col-grid-toggle`, `.span-toggle`
- `.gitignore`: `.agents/`, `AGENTS.md`, `scheduled_tasks.lock`, `TUM_PROJE_KODLARI.txt`, `start.bat`, `kodlari_topla_v2.bat` eklendi
- Mimari doküman `docs/dashboard-engine-architecture.md`'ye taşındı
- Journal append `7af52a6`

### B. Plan 05 — Formula Evaluator (Tier 3, 5 commit)

- `c836301` **Faz 1**: AST whitelist parser + 44 unit test
  - `Services/Eval/{FormulaToken, FormulaNode, FormulaTokenizer, FormulaParser, FormulaEvaluator}.cs` (~700 satır net)
  - Recursive descent (OR < AND < NOT < cmp < +- < */ < unary < primary)
  - Decimal aritmetik, 3VL null semantik, sıfıra bölme→null
  - Türkçe identifier (Unicode letter), DoS limit (4096 char + 1000 token + depth 64)
  - 44 test (happy path + 3VL + IF/IIF/CASE + Türkçe kolon + DoS + whitelist atlama)

- `99084cb` **Faz 2 v1** (sonradan davranış olarak revert): top-level CalculatedFields global enrichment + TableColumnDef.Computed flag

- `6d5ca5c` **Faz 2 v2 + Faz 3**: yön değişikliği — tablo-özel `TableColumnDef.Formula`
  - `DashboardRenderer.EnrichTableFormulas` helper (sadece bağlı RS, satır-bazlı eval)
  - `DashboardConfigValidator` formula sözdizim doğrulaması
  - Setup tab'a Tablo widget için "Kolonlar" bloğu (checkbox + drag-drop reorder + hizalama toggle)
  - 4 entegrasyon test (`DashboardRendererTests.TableFormula_*`)

- `9f8cca0` **Bonus** — `JsonExtensionData` esneklik valve
  - DashboardConfig + Component + Tab + TableColumnDef'e `Extra: Dictionary<string, JsonElement>?`
  - Frontend yeni alan ekleme schema değişikliği gerektirmez (round-trip otomatik)
  - 7 round-trip test
  - Komple JSON refactor (Plan 06 alternatifi A) yerine en-az-kod alternatif (B): mevcut typed alanlar korunur, yeni alanlar Extra'ya düşer

- `5ee0e96` **Faz 4** — hesaplı kolon UI + `AdminController.ValidateFormula`
  - Setup tab tablo bloğunda "+ Yeni Hesaplı Kolon" inline form (alias + formula + format)
  - Server-side validate (textarea blur + Kaydet öncesi), Türkçe hata + pos
  - Client-side parser portu yok — backend tek source-of-truth

- `3d2c989` Plan 05 kapanışı — durum "Tamamlandı", commit zinciri özet, done criteria checkbox'ları işaretli

### C. Bug fix turu (`08e46f0`)

3 KPI test sırasında Tam Önizle modal'ında validator hatası + ikon görünmeme.

- **`DashboardConfig.ResolveResultSet`**: "rsN" regex fallback — V2 builder default `result: 'rs0'` string'i resultContract entry yoksa int index olarak yorumla, bounds check
- **`DashboardConfigValidator`**: aynı pattern recognition; `rsN` formatı bilinmeyen isim sayılmaz
- **`builder-drawer.js setField('icon', val)`**: `fa-X` → `fas fa-X` otomatik prefix normalize (renderer FontAwesome `fas` prefix bekliyor, icon picker plain class emit ediyordu)

### D. Test rapor + 3 KPI canlı render (ReportId=16)

- `/Admin/CreateReportV2` UI üzerinden (programatik Alpine state + Kaydet click) yeni rapor oluşturuldu: ReportId=16 "KPI Test (sil)"
- 3 KPI tanımlandı: Toplam Kadro (rs2.KadroToplam), Plana Göre Geldi (rs2.PlanGeldi), Şu An İçeride (rs1.SuAnIceride) — 3 farklı renk + ikon
- Tam Önizle modal screenshot: **TOPLAM KADRO 253, PLANA GÖRE GELDİ 124, ŞU AN İÇERİDE 123** canlı veri
- Validator hata yok, ikonlar `fas fa-X` formatında, Türkçe karakterler doğru render

### E. Plan 06.A — V2 Drawer ParamSchema editor (`52e0077`)

ReportId=16'da Veri Seti dropdown boştu (UX bug): CreateReportV2 form akışı SP'den **tüm parametreleri** ParamSchema'ya alıyor → RunJsonV2 boş string→NULL SqlParameter → SP "TOP/OFFSET NULL" hatası → spPreview false.

Fix: V2 Drawer "Rapor Ayarları" no-selection state'inde read-only "SP Parametreleri (otomatik)" listing → editable textarea (rows=7, monospace) + "SP'den yeniden çek" buton + SP'nin tüm parametreleri (referans, collapsible).

Smoke: ReportId=16 ParamSchema textarea'dan 4 alan silindi (sadece Tarih), Kaydet, reload → spPreview=true, 7 set · 196 satır, KadroToplam/KadroGelen kolonları rs2'de mevcut, Veri Seti + Kolon dropdown dolu.

## Build / Test Durumu

- **Build:** Yeşil (sıfır uyarı/hata)
- **Test:** **217 yeşil** (önceki 162 → +55: 44 FormulaEvaluator + 7 JsonExtensionData + 4 TableFormula entegrasyon), sıfır regression
- **Smoke:** Tam Önizle iframe canlı 3 KPI render (253/124/123), V2 Drawer ParamSchema editor düzenleme persist OK

## Commit Durumu

- **Uncommitted dosya sayısı:** 0 (handoff öncesi)
- **Bugünkü commit'ler (10 + 1 handoff):**
  1. `f57a12c` Plan A mini polish
  2. `7af52a6` 2026-04-29 oturum 3 append
  3. `c836301` Plan 05 Faz 1 — AST parser + 44 test
  4. `99084cb` Plan 05 Faz 2 v1 (davranış revert edildi, kod history'de)
  5. `6d5ca5c` Plan 05 Faz 2v2 + Faz 3 — TableColumnDef.Formula + tablo Setup UI
  6. `9f8cca0` JsonExtensionData esneklik valve
  7. `5ee0e96` Plan 05 Faz 4 — hesaplı kolon UI + ValidateFormula endpoint
  8. `3d2c989` Plan 05 kapanışı
  9. `08e46f0` Bug fix — rsN pattern fallback + icon prefix normalize
  10. `52e0077` Plan 06.A — V2 Drawer ParamSchema editor

## Yarım Kalan / Yarın'a Bırakılan İşler

- **Plan 06.B** (tam V2 parity) — Roles/Categories/Description/IsActive Drawer'a, klasik `/Admin/EditReport` + `/Admin/CreateReport` deprecate + redirect → Plan dosyası yazılıp onay alınmalı (~1.5-2 saat). Plan 06.A bunun ön adımı.
- **`ReportParamValidator` kök bug** — boş string SqlParameter'ı NULL'a çeviriyor → SP `TOP @FmTop` NULL ile fail. Plan 06.A bypass etti (kullanıcı ParamSchema'yı kısalt) ama kök fix yapılmadı. Çözüm: validator boş string parametresini SqlParameter listesinden skip etsin → SP default kullansın.
- **ReportId=16 "KPI Test (sil)" DB'de duruyor** — kullanıcı silme kararı vermedi, sonraki oturumda silinebilir veya tutulabilir.

## Kararlar

- **Komple JSON refactor (radikal A) reddedildi** — kullanıcı "en az kod" prensibi + esneklik istedi. Hibrit B yapıldı: mevcut typed alanlar + `JsonExtensionData` Extra valve. Plan 06 hiç yazılmadı, mini Plan 06.A doğrudan implement edildi.
- **Plan 05 yön değişikliği mid-flight** — top-level CalculatedFields global enrichment yerine tablo widget'a özel `TableColumnDef.Formula`. Tek state path, scope karmaşası yok, kolon silinince formula da gider. Faz 2 v1 (commit 99084cb) git history'de kalır ama davranış olarak silindi (commit 6d5ca5c içinde). Future me: `EnrichWithCalculatedFields` global helper kodda YOK.
- **AST parser DSL v1 sözdizimi** — aritmetik, karşılaştırma, AND/OR/NOT, IF/IIF nested, CASE WHEN/THEN/ELSE/END, kolon ref `[Şube]`/bareword. CONCAT/SUBSTRING/DATEDIFF kapsam dışı (gelecek v2'ye). Token whitelist explicit + auditable.
- **Plan 06.A mini-fix yaklaşımı** — sadece ParamSchema editor V2 Drawer'a; Roles/Categories/Description henüz klasik `/Admin/EditReport` linki ile delege ediliyor. Plan 06.B sonraki oturum.
- **ADR yazılmadı** — Plan 05 büyük bir feature ama mevcut ADR-008 v2 schema F-8 maddesini gerçekleştiriyor; ADR-008'e "F-8 tamamlandı" notu eklenmesi opsiyonel TODO.

## Dikkat Edilmesi Gerekenler

- **Plan 05 commit `99084cb` yanıltıcı** — git log'da "renderer + validator computed kolon entegrasyonu" yazıyor ama o dosyalar `6d5ca5c`'de mini-revert + farklı yaklaşımla yeniden yazıldı. Tek başına 99084cb'ye revert atılmamalı, ya tüm zincir ya hiç.
- **`ReportParamValidator` boş string→NULL kök bug** — V2 builder yeni rapor akışında SP'nin tüm parametrelerini ParamSchema'ya almak yanlış default. Plan 06.A textarea ile bypass edildi ama validator tarafı düzeltilmedi. Yeni rapor oluştururken kullanıcı manuel kısaltmalı (ya da sonraki oturum kök fix).
- **`preview_eval window.location.assign` navigation tutarsız** — bazen `setTimeout` arttırınca çalışıyor bazen hiç gitmiyor. Çözüm: `preview_start` ile fresh server, sonra HTTP fetch. Veya kullanıcıdan manuel nav.
- **CreateReportV2/EditReportV2 hidden input form-binding** — `name="ParamSchemaJson" form="reportEditFormV2/reportCreateFormV2" :value="paramSchemaJson"` reactive sync. Backend klasik EditReport/CreateReport endpoint'i kullanıyor (V2 ayrı POST endpoint yok). Plan 06.B'de yeni V2-specific POST endpoint mantıklı olabilir.
- **`ReportParamValidator` davranışı** — `Services/ReportParamValidator.cs` içeriğine bakılmadı bu oturumda, fix öncesi inceleme gerek (boş string → NULL conversion noktasını bul).
- **3 KPI screenshot kanıtı** — Tam Önizle iframe içinde TOPLAM KADRO 253, PLANA GÖRE GELDİ 124, ŞU AN İÇERİDE 123. Edit mode'da KPI değerleri "—" görünür (bu beklenen, RS verisi sadece preview mode + iframe'de işlenir).

## Yarına Başlangıç Noktası

1. **ReportId=16 silme kararı** — kullanıcı tutsun mu sileyim mi? (UI üzerinden /Admin/EditReport/16 → Sil veya SQL ile)

2. **Plan 06.B yaz** — `plans/06-m11-v2-edit-parity.md`:
   - Roles checklist (V1'deki AllowedRoles seçimi)
   - Categories checklist
   - Description textarea
   - IsActive toggle (UI'da gösteriliyor read-only, edit'e açılmalı)
   - Klasik `/Admin/EditReport` ve `/Admin/CreateReport` route'ları deprecate / V2'ye redirect
   - Backend POST: V2-specific endpoint mi yoksa mevcut klasik endpoint genişletme mi (Tier 3 karar)

3. **`ReportParamValidator` kök fix** (Plan 06.A bypass'inin yanı sıra):
   - `Services/ReportParamValidator.cs` oku, boş string → NULL noktasını bul
   - Skip et (SqlParameter listesine ekleme)
   - SP default kullansın
   - Test: ReportId=16'a 5 alanlı paramSchema geri yüklesem yine çalışsın

4. **Manuel browser test (kullanıcı)** — ReportId=16 üstünden:
   - V2 Drawer ParamSchema textarea düzenle ✓
   - Veri bağlantısı (Veri Seti + Kolon dropdown) ✓
   - 3 KPI canlı veri ✓
   - Tam Önizle ✓

## Kritik Dosyalar / Skill'ler (oturum içi)

- AST parser: `Services/Eval/{FormulaToken, FormulaNode, FormulaTokenizer, FormulaParser, FormulaEvaluator}.cs`
- Renderer entegrasyon: `Services/DashboardRenderer.cs::EnrichTableFormulas` (helper, tablo-özel)
- Validator: `Services/DashboardConfigValidator.cs` — TableColumnDef.Formula + rsN pattern fallback
- Frontend mixin: `wwwroot/assets/js/builder-v2/builder-drawer.js` — tableDisplayedColumns, toggleTableColumn, drag-drop, addCalcColumn, validateFormulaLive
- View V2: `Views/Admin/{EditReportV2, CreateReportV2}.cshtml` — Setup tab tablo bloğu + ParamSchema editor
- Endpoint: `Controllers/AdminController.cs::ValidateFormula` (admin-only, [HttpPost], [ValidateAntiForgeryToken])
- Plan dosyaları: `plans/05-m11-tablo-checkbox-computed-cols.md` ✓ Tamamlandı
- Önceki oturum journal'ı: bu dosyanın oturum 1 bölümü
