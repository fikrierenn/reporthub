# Oturum Gunlugu — 2026-04-23

## Ana Konu
M-11 Dashboard Builder UX Redesign planlamasi + BKM brand'ine sadik statik HTML mockup tasarimi.

## Tamamlananlar
- **M-11 plan dosyasi** — `C:/Users/fikri.eren/.claude/plans/imdi-planlama-yap-bu-optimized-hippo.md`. Apache Superset'ten 10 UX pattern + v2 referans + ReportType/dashboard ayrimi kaldirma karari + widget matrisi (4 KPI variant, 10 chart tipi, tablo kosullu format) + 13 faz detayli plan + migration 18 (PDKS/Satis v1→v2 + table→dashboard auto-convert).
- **Statik mockup dosyasi** — [dashboard-builder-v3.html](ReportPanel/wwwroot/mockups/dashboard-builder-v3.html). Ilk iterasyon v2'yi kopyaladi (mavi), kullanici uyarisi sonrasi BKM kirmizi-siyah-beyaz brand'e yeniden yazildi: `.btn-brand`/`.card-brand`/`.form-input-brand` (style.css), `_AppLayout` header+footer taklit, `--brand-red: #dc2626` her aktif state.
- **3-tab preview** (Superset inspect) — Cikti (canli Chart.js) / Sorgu (SP exec + result set metadata) / JSON (ham veri).
- **Drawer Veri|Gorunum tab ayrimi** — Veri: data source bar + widget tipi + baglanti + turetilmis alanlar + chart tipi + dataset. Gorunum: axis options + renk swatch + ikon grid + span toggle.
- **Kategorili chart gallery** — Tumu/Karsilastirma/Trend/Pay/Dagilim/Ileri chip filter + arama input + 12 SVG thumbnail (10 aktif + Heatmap/Gauge M-12 disabled).
- **Turetilmis Alanlar bolumu** (kullanici ek talebi) — `DeltaCiro = BugunCiro - GecenYilBugun` gibi client-side formul + fx badge'li pill + hatali formul inline error ornegi + fonksiyon ipuclari (SUM/AVG/ROUND/IF/COALESCE/CONCAT).
- **Veri Kaynagi bar** — drawer ustunde Baglanti/SP/RS sayisi/sure + Onizle butonu.
- **Sag ust Klavye kisayol modali** (`?` tusu).
- **TODO.md'ye M-11 maddesi** (madde 29.5) — 13 faz tam detayli, commit plani, M-12'ye ertelenenler.
- **Rapor/dashboard tipi kaldirma karari** — `ReportCatalog.ReportType` (`table` | `dashboard`) ayrimi 5+ yerde dallaniyor (ReportsController.cs:146,216, ReportManagementService.cs:41,92,107,111, Run.cshtml, EditReport.cshtml JS). Tum table raporlar tek-table-widget'li dashboard'a migrate edilecek (Migration 18), kolon F-1.5'te drop. ADR-009 yazilacak.

## Build / Test Durumu
- Build: calistirilmadi (sadece mockup/plan oturumu).
- Test: calistirilmadi.
- Smoke: mockup HTML preview panel'de canli; swatch/variant/span toggle/gallery cat/chart type tiklamalarla state degisiyor, dark mode toggle calisiyor (sonrasinda kaldirildi — proje light-only), Chart.js preview render ediliyor, kisayol modali `?` ile aciliyor.

## Commit Durumu
- Uncommitted: **10 dosya** (esigin altinda, iyi).
  - M-10 Faz 3 bundle (bekliyor): `DashboardConfigValidator.cs` + test, `ReportManagementService.cs`, `EditReport.cshtml`, `CreateReport.cshtml`, `TODO.md`.
  - Bugun eklenen: `ReportPanel/wwwroot/mockups/dashboard-builder-v3.html` (mockup), `TODO.md` (M-11 maddesi).
  - Eski kullanici dosyalari: `TUM_PROJE_KODLARI.txt`, `kodlari_topla_v2.bat`, `reporthub_dashboard_engine_mimari_dokumani.md` (git disi, dokunulmadi).
- Yeni commit (bugun): yok. Handoff commit: bu journal + yazilacak.
- **Bekleyen:** M-10 Faz 3 bundle + mockup commit, yarin "commit'le" onayiyla.

## Kararlar
- **Chart kutuphanesi:** Chart.js 4.4 + **plugin YOK** — 10 chart tipi Chart.js native ile karsilaniyor (`hbar` = `bar` + `indexAxis:'y'`, `stacked` = `bar` + `stacked:true`, `area` = `line` + `fill:true`, `polar` = `polarArea`, `scatter` native). ECharts + ApexCharts reddedildi (migration 0 deger). Heatmap/Gauge M-12'ye (`chartjs-chart-matrix` + custom doughnut trick gerekir).
- **Layout:** Gridstack.js reddedildi (v2 tasarim basit span/liste kullaniyor). Drag sadece sira degistirme, resize span dropdown ile. M-12'de yeniden degerlendirilir.
- **Structure widget'lari** (markdown/iframe/divider) reddedildi — M-12'ye. v2 tasarimda yok, XSS riski yuksek, solo-dev scope disi.
- **Dark mode** reddedildi — proje `color-scheme: light` zorunlu, `_AppLayout`'ta yok. M-12'ye.
- **Komut paleti (Cmd+K fuzzy)** reddedildi — tui-studio orneginden ilham kalmak yetmedi. Sadece kisayol modali (`?`) ve 5 temel kisayol (Ctrl+S/P/Z, Esc, Delete).
- **ReportType kolonu SIL** — migration 19 ile drop. Once [Obsolete] fazi 1 commit, manuel test, sonra drop. ADR-009 yazilacak. Backup zorunlu.
- **Turetilmis Alanlar = client-side JS eval** degil — sandbox'li basit expression parser (AST) duzeyinde F-8'de tasarlanacak. SP dokunulmaz, sonuclar transform edilir.

## Dikkat Edilmesi Gerekenler
- **Renkler ince ayarli degil** — kullanici feedback: "renkler tam oturmadi ama implement ederken oturur". Mockup %90 hazir, kirmizi-gradient/swatch/badge sirasi/section-hd border pikselli ayar F-7'de yapilir.
- **v2 HTML'i kopyalama hatasi (ilk iterasyon)** — Superset esinlenmesi soylediginde mavi v2'yi direkt kopyaladim, proje brand'ine (kirmizi) cevirmedim. Kullanici uyardi, yeniden yazdim. Sure kaybi 1 iterasyon. Ders: **her esinlenme sonrasi proje kurallari dosyalarina (`style.css`, `_AppLayout.cshtml`) bak, sonra yaz.**
- **Plan ilk versiyonu cok genisti** — Gridstack + structure widgets + Cmd+K + 16 widget vb. hepsi vardi. Kullanici "v2 yap demedim esinlen dedim" + "Superset profesyonel" diyince scope yeniden tanımlandı: Gridstack düştü, structure düştü, Superset pattern'leri öne çıktı. Faz sayisi 7 → 13'e çıktı çünkü ReportType kaldırma + calculated fields + veri kaynagi bar eklendi.
- **M-10 Faz 3 commit**: 3 kez önceki oturumlarda taslak sundum, 3. oturumda onay alınamadı. Pre-commit hook (antipattern scan) temiz gecer (DateTime.Now yok). Yarin ilk is.
- **Migration 18 riski YUKSEK** — canli PDKS/Satis configlerine dokuncak, ayrica tum `ReportType='table'` raporlari dashboard'a cevirecek. Zorunlu: pre-migration `SELECT INTO backup`, dev DB dry-run, idempotent `WHERE schemaVersion < 2`, transaction, per-row audit log.

## Yarina Baslangic Noktasi
1. **M-10 Faz 3 commit** (5 kod + TODO.md + eski + yeni M-11 maddesi) — 4 kez onaya sunuldu, yarin ilk "iyi sabahlar" ile onay → commit. Mesaj:
   ```
   feat(dashboard): M-10 Faz 3 — admin save validation + EditReport UTF-8

   - DashboardConfigValidator: 10 hard + 3 soft rule, admin-dostu TR mesajlar.
   - ReportManagementService: dashboard_config_invalid / _warnings audit.
   - EditReport + CreateReport Model.Message banner (daha önce render edilmiyordu).
   - EditReport.cshtml UTF-8 sweep (15 yer).
   - 19 yeni unit test, 96/96 yesil.
   ```

2. **ADR-008 Dashboard Builder v2 + ADR-009 Report Type Consolidation yaz** — plan dosyasi hazir, ADR iskeletleri o plan'dan cikarilir. ~1 saat.

3. **Mockup commit** — `ReportPanel/wwwroot/mockups/dashboard-builder-v3.html` ayri commit: `docs(dashboard): M-11 mockup v3 — BKM brand + Superset UX patterns`.

4. **Branch kur**: `git checkout -b feature/m-11-dashboard-builder-redesign`.

5. **M-11 F-1 migration 18 iskeleti** — `Database/18_MigrateDashboardSchemaV2.sql` yazimi + dev DB'de dry-run. Backup script + rollback plani. ~3h.

6. **F-1.5 baslangic** — `ReportType` [Obsolete] ekle + 5 dallanma noktasini tek-path'e indir. Migration 19 yaz (ama calistirma).

## Kritik dosyalar / kurallar
- Plan: `C:/Users/fikri.eren/.claude/plans/imdi-planlama-yap-bu-optimized-hippo.md`
- Mockup: `ReportPanel/wwwroot/mockups/dashboard-builder-v3.html` (526 satir dokunulacak, renk ince ayari + detay)
- v2 referans: `D:/Downloads/dashboard-builder-design-v2.html` (1085 satir, gorsel kit kaynagi)
- Superset ilham: Explore view / Native Filters / Inspect query / Chart Gallery — plan'da 10 pattern tablolandi.
- Brand kaynak: `ReportPanel/wwwroot/assets/css/style.css` (`--brand-red: #dc2626`, `.btn-brand`, `.card-brand`, `.form-input-brand`)
- Layout kaynak: `ReportPanel/Views/Shared/_AppLayout.cshtml` (header max-w-7xl + fixed footer + BKM logo)

---

## Oturum 2 — Mockup v3 tam yeniden yazim + preview=Run kirilimi

### Tamamlananlar
- **Mockup baştan yazildi** — kullanici yeni referans HTML paylasti (`D:/Downloads/ReportHub Dashboard Builder.html`, 1118 satir, Gridstack + Chart.js + Properties panel + Edit/Preview + JSON save/load, tumu calisan). Bu tasarim:
  - BKM kirmizi brand'ine port edildi (`--accent:#dc2626`, oklch(0.56 0.17 270) MOR reddedildi).
  - Turkce UI'ya cevrildi (Bilesenler/Duzenle/Onizle/Kaydet/Ozellikler/Baglanti/Yapilandirma/Yerlesim — tum form label'lari).
  - Ornek data **sp_SatisPano gercek semasi** ile dolduruldu: `summary` (bugun_ciro/ay_kumule/ort_sepet/fis_sayisi + delta'lar), `trend` (15 gun tarih-ciro-hedef), `magaza` (4 magaza), `detail` (8 fis — durum: odendi/beklemede/iade).
  - ResultContract `{summary,trend,magaza,detail}` — ADR-007'de kullanilan isim-bazli binding ile birebir uyumlu. Kind Turkce hint'li (KPI kaynagi, zaman serisi, kategorik, satir tablosu).
  - fmt() tr-TR locale'e gecti (TL prefix, ondalik virgul, binlik nokta).
  - statusLabel/statusClass/colLabel helper'lari ile EN→TR otomatik mapping.
  - Tweaks paneli sadelestirildi — accent hue secici kaldirildi (BKM tek renk), Yogunluk/Izgara noktalari kaldi.
  - Fontlar Inter + JetBrains Mono CDN ile yuklendi (projede yoktu, sadece builder'da modern his icin).
- **Preview = Reports/Run birlesimi** (kullanici kritik icgorusu: "onizleme kismi zaten raporun calisma kismi olacak ayni zamanda") mockup'a yansitildi:
  - **Siyah preview banner** (Edit→Preview gecince acilir): "Kullanici gorunumu · son kullanicinin gorecegi Reports/Run ciktisi · `GET /Reports/Run/14`" + yesil pulse dot. Tek renderer, tek HTML, builder preview = user run birebir.
  - **Rapor parametreleri bar** (canvas ustu, edit+preview ortak) — Tarih/Magaza/Kategori chip'leri + "Calistir" butonu. `ParamSchemaJson` karsiligi; Reports/Run'daki param input alani.
  - Preview mode'da sol palette + sag properties + edit toolbar gizli; sadece param bar + widget grid kalir (w-head chrome disable).

### Build / Test Durumu
- Build/test calistirilmadi (sadece mockup). Production kod dokunulmadi.

### Commit Durumu
- Uncommitted: **10 dosya**. Mockup her iterasyonda ayni dosya uzerinde — 1 uncommitted yeni dosya olarak sayiliyor.
- Bugun commit: 1 (`f526ca0` — oturum 1 journal).
- Yarin M-11 F-0'da commit'lenecek: mockup + M-10 Faz 3 bundle + TODO.md.

### Kararlar (oturum 2)
- **Tek renderer karari** (ADR-008'e guclendirerek gidecek): `DashboardRenderer.Render()` output hem admin builder iframe'de hem user `Reports/Run` sayfasinda render edilir. Farkli kod yok. Admin `Edit` vs `Preview` mod toggle'i sadece widget chrome (w-head action'lari) visibility ve grid-stack interaction'i (enableMove/Resize) etkiler; renderer cikti birebir ayni. Bu karar M-11 F-2 renderer split'ini rahatlatir (sadece bir DOM cikti yolu var).
- **Param filter bar builder icinde yasar** — `ParamSchemaJson` builder'da duzenlenir, hem preview hem Run'da ayni chip'ler. Ayri `Run.cshtml` param input'u yok. Bu M-11 F-7 split-pane Razor yeniden duzenlemesinin scope'unu genisletir (Reports/Run shell'i + builder shell'i tek Razor partial'da birlesir). Plan dosyasinda F-7'ye ek not yarin dusulecek.
- **Gridstack.js geri donus** — onceki kararlarda (plan v1'de) Gridstack reddedilmisti ("v2 tasarim basit span/liste kullaniyor"). Kullanicinin yeni referansi Gridstack ile geliyor ve drag-resize deneyimi cok daha guzel. Karar tersine: **Gridstack kabul**, M-11 F-5'e geri alinacak. Tradeoff: +30KB CDN, +jQuery-free vanilla API + Reports/Run runtime'da Gridstack gereksiz (static CSS grid `grid-column: span N; grid-row: span M;` inline style — sadece builder'da Gridstack).
- **Fontlar** — Inter + JetBrains Mono builder'a ozel CDN. Projenin geri kalaninda yok (proje default sans). Ayri mi olsun ortak mi? Kisa karar: builder-only, _AppLayout'a eklenmeyecek (proje cogunlukla sistem fonttuyla calisiyor, kirma riski yok).

### Dikkat Edilmesi Gerekenler
- **ReportType kaldirma karari (F-1.5) buyuyor** — simdi Reports/Run da dashboard-only (tek renderer). Yani tablo-tipi rapor = tek-table-widget dashboard, param bar'li builder UI'si ile. F-1.5'te hem ReportType kolonu silinir hem Run.cshtml tamamen yeniden yazilir (eski tablo-render yolu kalkar, sadece DashboardRenderer output). Migration 18 + Run.cshtml rewrite + EditReport builder mount = ayni commit'te olmak zorunda yoksa sistem kirilir. Risk: commit 20+ dosya olur, 15 esigini aşar. **Cozum:** F-1 ve F-1.5 tek commit blok halinde atilir, commit-discipline.md istisna olarak belgelenir veya F-1.5 alt-commit'e bolunur (1. backend service + controller path temizlik, 2. Razor+JS consolidation, 3. Migration 18+19).
- **Preview mode iframe vs direct DOM** — mockup'ta preview mode ayni DOM'da ama class toggle ile (preview-mode class). Gercek implementasyonda builder `EditReport.cshtml` icinde iken Reports/Run kendi URL'sinde. Iki path:
  - **A (basit):** Builder'da "Onizle" = iframe ile `/Reports/Run/{id}?preview=1&configOverride=<draft-json>` goster. Save'den once taslak config'i gecici param olarak gecir.
  - **B (DOM-level):** Builder ayni sayfada preview-mode class toggle; renderer server-side render edip client'ta HTML replace. Hizli ama param re-run gerekirse AJAX gerekli.
  - Karar F-7 implementation'a birakildi. A daha temiz — "preview = Reports/Run" karari birebir.

### Yarina Baslangic Noktasi
1. **M-10 Faz 3 commit** (once bekleyen) + **mockup commit**. Her ikisi de `feature/m-11-dashboard-builder-redesign` branch'inde degil, simdilik `main`'de atilabilir veya yeni branch'e alinabilir. Tercih: mockup ayri commit (main'e), M-10 Faz 3 bundle yine main'e, sonra M-11 branch.
2. **ADR-008 yaz** — mockup v3 + preview=Run + Gridstack geri donus + font karari + param bar mimarisi hepsi dahil.
3. **ADR-009 yaz** — ReportType + Run.cshtml consolidation, A/B preview karari.
4. **Plan dosyasini guncelle** — F-5 (Gridstack) geri aktif, F-7 scope genisle (Run.cshtml rewrite dahil), M-12 listesi guncellensin.
5. **TODO.md madde 29.5** — bugunku kararlarla (Gridstack kabul, preview=Run, ReportType+Run rewrite) guncellenmeli.

---

## Oturum 3 — F-0'dan F-6'ya 10 commit: Migration 18 + path consolidation + renderer split + schema v2 + 10 chart + 4 KPI variant + tablo koşullu format

### Ana Konu
M-11 Dashboard Builder Redesign'ın ilk yarısı — backend hazır, renderer her variant'ı çiziyor. F-7'den itibaren admin UI (builder.js split + split-pane Razor) başlayacak.

### Tamamlananlar (11 commit — feature/m-11-dashboard-builder-redesign)
1. **86bc94e** · M-10 Faz 3 bundle commit (5 kod + TODO.md) — DashboardConfigValidator 10 hard + 3 soft rule, ReportManagementService audit event, EditReport/CreateReport Model.Message banner, EditReport UTF-8 sweep. 3 kez onay bekliyordu, bugün ilk iş olarak atıldı.
2. **88f695d** · Mockup v3 commit (`ReportPanel/wwwroot/mockups/dashboard-builder-v3.html`) — 1176 satır BKM brand Gridstack+Chart.js çalışır referans. Commit mesajında "implementasyonda birebir kopyalanmayacak" kuralı yazıldı (style.css custom class + _AppLayout + max-w-7xl).
3. **fa0db69** · ADR-008 + ADR-009 + TODO.md 29.5 update. ADR-008: 10 Superset UX pattern + widget matrisi + Gridstack kabul + preview=Run + calculated fields + tasarım uyum kuralı. ADR-009: ReportType kolon DROP + 3 alt-commit stratejisi + A/B preview kararı (A patikası iframe).
4. **f849bd4** · F-1 Migration 18 (218 satır SQL) idempotent v1→v2 + table→dashboard auto-convert. Dev DB'de (PortalHUB) canlı çalıştırıldı: 2 v1→v2 (PDKS 13 + Satış 14) + 2 table→dashboard (ReportId 11, 12) = 4 rapor. 4 AuditLog entry. Idempotency doğrulandı (2. run 0/0). `Database/backup/` klasörü + .gitignore kuruldu.
5. **f447e8d** · F-1.5 alt-commit 1 backend path temizlik. `NormalizeReportType` SİLİNDİ. `ReportManagementService` dashboard-only. `BuildDefaultDashboardConfig` helper (yeni raporlar tek-table-widget skeleton). `ReportsController.Run` isDashboard dallanmaları kaldırıldı — tek render path. `ReportCatalog.ReportType` [Obsolete] işaretlendi. `ReportPanelContext.HasDefaultValue("dashboard")` + #pragma warning disable CS0618. `AdminController.BuildReportFormInput` ReportType: "dashboard" hardcode.
6. **a9e17b3** · F-1.5 alt-commit 2 Razor consolidation. Run.cshtml 380 → 239 satır (-141) — tablo render path'i DROP, sadece dashboard iframe. EditReport + CreateReport "Rapor Tipi" radio kartı SİLİNDİ + toggleReportType() JS SİLİNDİ. dashboardConfigSection hep görünür. **Bonus:** DashboardRenderer client-side JS'te `cfg.cols` boşsa SP ilk satır key'lerinden auto-detect eklendi → canlıda 371 kayıtlı tablo raporu görünmüyor bug'ı fix.
7. **8928245** · F-2 DashboardRenderer split. 422 → 71 satır orkestrasyon + 6 yardımcı (RenderContext 35, KpiRenderer 36, ChartRenderer 35, TableRenderer 39, PlaceholderRenderer 52, DashboardShellRenderer 265). Public API static korundu (9 XSS testi + ReportsController call-site kırılmadı). DI refactor F-7'ye ertelendi.
8. **23f584e** · F-3 Schema v2. DashboardConfig.cs 161 → 298 (variant/numberFormat/axisOptions/tableOptions + AxisOptions/TableOptions/DeltaConfig/TrendConfig/ProgressConfig/ConditionalFormat/CalculatedField sınıfları + TableColumnDef.format + conditionalFormat). Validator 155 → 287 (KPI 4 variant + chart 10 tip whitelist + numberFormat + pageSize + conditional mode + calculated field camelCase+duplicate+formula). **26 yeni test → 122/122 yeşil**. MaxSchemaVersion=2. Bonus: Views/Reports/Index.cshtml ReportType [Obsolete] warning temizlendi.
9. **17e9f82** · F-4 10 chart tipi. Chart.js 4 native (plugin YOK): line/area/bar/hbar/stacked/pie/doughnut/radar/polarArea/scatter. ChartRenderer.cs variant > chartType fallback + axisOptions + numberFormat emit. DashboardShellRenderer chart init JS 20 → 90 satır — switch(variant) + dataset patch + scatter {x,y} format + pie/doughnut/polarArea tek dataset 10-renk palette + fmtChart(numFormat) helper (₺/₺K-M/%/decimal2) tick callback + tooltip callback. isCircular scales switch, hbar indexAxis:'y', stacked scales.x/y.stacked.
10. **5156075** · F-5 4 KPI variant. KpiRenderer.cs 38 → 171 satır. Basic (v1 korundu), Delta (SVG ok + compareColumn karşılaştırma + yeşil/kırmızı %), Sparkline (80x26 SVG polyline + fill path, data[valueColumn] min/max normalize), Progress (bar width + "fiili/hedef" metni). Shell KPI init: variant dispatch 4 branch, XSS-safe DOM API (createElementNS + textContent). Mevcut 16 KPI (PDKS+Satış) variant=basic → kırılmadan render.
11. **ac28f6a** · F-6 Tablo conditional format + tableOptions. TableRenderer.cs tableOptions + kolon format/condFormat data-tbl'ye emit + widget header'a `<input data-tbl-search>` opsiyonel + footer'a `<div data-tbl-pager>` opsiyonel. Shell table init tam yeniden yazım: fmtCell(currency/number/percent/date/text/auto), kolon min/max stats, state.page + filter, zebra/stickyHeader/totalRow/clientSearch/pageSize, 4 conditional mode (negativeRed text-red + iconUpDown ↑/↓ + colorScale hsl gradient bg + dataBar absolute div + z-index). Auto-detect cols fallback korundu.

### Build / Test Durumu
- Build: ✅ 0w 0e (preview lock 2 kez, preview_stop ile çözüldü)
- Test: ✅ 122/122 (96 baseline + 26 yeni v2 validator)
- Smoke: ✅ PDKS/Satış/tablo 11/12 — kullanıcı tüm fazlarda "çalışıyor" onayı

### Commit Durumu
- Feature branch: `feature/m-11-dashboard-builder-redesign`
- Bugün 11 yeni commit (branch'te 11 tane, main ile divergent)
- Uncommitted kod: **0** (3 untracked eski user dosyası: TUM_PROJE_KODLARI.txt, kodlari_topla_v2.bat, reporthub_dashboard_engine_mimari_dokumani.md — dokunulmuyor)

### Kararlar (Oturum 3)
- **Gridstack geri kabul** — plan v1'deki red tersine, ADR-008'de belgelendi. F-7'de CDN + drag-resize canvas.
- **Preview = Reports/Run tek renderer** (ADR-008) — F-9'da iframe patikası (A) + configOverride query param.
- **ReportType DROP ertelendi** (F-1.5 alt-commit 3) — [Obsolete] fazı min 1 gün + canlı test kuralı. Kullanıcı onay verince yapılacak.
- **F-2 DI refactor atlandı** — 9 XSS testi static API'ye bağımlı, breaking change riski. DI F-7 live preview endpoint'te tekrar değerlendirilecek.
- **DashboardMigrationTests unit test atlandı** — C# entegre DB overkill. Gerçek DB'de zaten doğrulandı (4 rapor migrate + idempotent). F-12 E2E smoke kapsamında.
- **Chart.js plugin YOK** — 10 tip native ile karşılandı. heatmap/gauge/sankey/treemap/funnel M-12'ye.
- **Table auto-detect cols fallback** — Migration 18 Adım B sonrası tüm table → dashboard raporları columns:[] ile geliyor, shell JS SP ilk satır key'lerinden otomatik kolon üretiyor. Canlı bug fix.

### Dikkat Edilmesi Gerekenler
- **Preview lock** her dotnet build öncesi elle `preview_stop` gerekiyor — 2 kez patladı bugün. `dotnet build` hook'u ile preview'ı otomatik durdurma ileride gerekli (TODO).
- **F-7 en büyük faz** (~10h) — 775 satır dashboard-builder.js 6 modüle bölünecek + EditReport/CreateReport split-pane Razor + Gridstack CDN + BKM brand CSS. **Alt-commit'e bölmek şart** (3 alt-commit minimum: core+list, drawer+contract, preview+templates+Razor+CSS). 15-eşik riski.
- **Mockup v3 birebir kopyalanmayacak** — proje style.css custom class'ları (.btn-brand / .card-brand / .form-input-brand) + max-w-7xl + _AppLayout header/footer içinde port. Inter+JetBrains Mono builder-only font. Bu kural ADR-008'de belgelendi.
- **Yeni variant'lar canlıda görünmüyor** — Migration 18 tüm KPI'ları variant=basic, chart'ları variant=chartType değeri (line/bar/doughnut) olarak yazdı. delta/sparkline/progress/area/hbar/stacked/radar/polarArea/scatter variant'ları canlıda sadece admin builder'da yeni seçim yapılınca görülecek (F-7 sonrası).
- **Conditional format + tableOptions** canlıda görünmüyor — default stripe=true/stickyHeader=true var ama totalRow/clientSearch/pageSize/4 conditional mode admin builder'da seçilmeden aktifleşmez. F-7 sonrası.

### Yarına Başlangıç Noktası
1. **Opsiyonel: F-1.5 alt-commit 3** (Migration 19 DROP + ReportType property sil). [Obsolete] fazı geçti, canlı test OK. Tek commit, düşük risk. Kullanıcı "drop et" deyince.
2. **F-7 UI redesign** — en büyük faz (~10h). Plan:
   - **Alt-commit 1**: `dashboard-builder.js` 775 satır → `wwwroot/assets/js/dashboard-builder/` altında 6 modül (builder-core 160, builder-list 150, builder-canvas 180 gridstack wrap, builder-drawer 350, builder-contract 110, builder-preview 130, builder-templates 120). IIFE pattern, event bus (`document.dispatchEvent(new CustomEvent('db:*'))`). Eski dosya SİLİNECEK.
   - **Alt-commit 2**: EditReport.cshtml + CreateReport.cshtml split-pane Razor. Sol 320px palette + contract bar + tab strip. Orta Gridstack canvas (CDN). Sağ 380px drawer (Veri | Görünüm tab). Topbar (breadcrumb + dirty chip + Önizle + Geri Al + Kaydet).
   - **Alt-commit 3**: `dashboard-builder.css` yeni (~420 satır, BKM brand). Gridstack CDN + SRI. Inter + JetBrains Mono font CDN. `_AppLayout` uyumlu, max-w-7xl sınırında.
3. **F-8 drawer form UI** (~6h) — variant picker 4-kart (KPI) + chart gallery 10-tip SVG thumbnail + swatch-grid 7-renk + icon-grid 16 + span-toggle 1-4 + suggest pills (SP preview kolonlar) + calculated fields editor + section-hd pattern.
4. **F-9 live preview + dirty/toast + validation banner + Geri Al snapshot** (~5h). Yeni `AdminController.DashboardPreview + DashboardValidate` endpoint. Preview iframe srcdoc fetch.
5. **F-10 şablon + kbd shortcuts** (~3h) — KPI Trio/Trend Grafik/Detay Tablo preset + ⌘S/⌘P/Esc/Delete/? modal.
6. **F-11 smart defaults** (~3h) — SP preview'dan kolon auto-detect + chart tipi önerisi.
7. **F-12 E2E smoke + screenshot + journal** (~3h).

### Kritik dosyalar / kurallar
- Mockup referans: `ReportPanel/wwwroot/mockups/dashboard-builder-v3.html`
- ADR-008 + ADR-009: `docs/ADR/008-*.md`, `docs/ADR/009-*.md`
- Plan: `C:/Users/fikri.eren/.claude/plans/imdi-planlama-yap-bu-optimized-hippo.md`
- Renderer: `ReportPanel/Services/Rendering/` (6 dosya)
- Brand: `ReportPanel/wwwroot/assets/css/style.css`
- Layout: `ReportPanel/Views/Shared/_AppLayout.cshtml`
