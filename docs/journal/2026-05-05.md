# Oturum Günlüğü — 2026-05-05

## Ana Konu
Plan 07 Faz 2-5 + 5b uçtan uca implement: FilterDefinition EF + service, FilterOptionsService DB-driven, UserDataFilterInjector deny-by-default + Export inject güvenlik gap kapatma, kullanıcı formu dinamik filter UI, Sube canonical master + SubeMapping translate. (Oturum dünden — 4 Mayıs gecesinden — devam edip gece yarısını geçti.)

## Tamamlananlar
- **Plan 07 Faz 2** — `Models/FilterDefinition.cs`, `ReportPanelContext.FilterDefinitions` DbSet, `Services/FilterDefinitionService.cs` CRUD + audit + `IsSafeOptionsQuery` (SELECT-only guard). Commit `4df24d7`.
- **Plan 07 Faz 3** — `FilterOptionsService` switch-case kalktı, FilterDefinition lookup → Scope dispatch → NativeSources registry (reportAccess) + spInjection exec. `AdminController.SpExplorer.FilterOptions` dataSourceKey parametresi kaldırıldı (frontend zaten göndermiyordu). 7 yeni unit test. Commit `122d8af`.
- **Plan 07 Faz 4** — Aktif FilterDefinition'lar üzerinden deny-by-default check + `*` magic için injection skip + `UserDataFilterDeniedException`. `ReportsController` 5 caller (Run, Export, RunJsonV2, RunJsonV2Preview, PreviewDashboardV2) try/catch + audit `data_filter_denied` + 403. **Bonus:** Export action'a InjectAsync eklendi (mevcut multi-tenant gap kapandı). 5 yeni unit test. Commit `6056546`.
- **Plan 07 Faz 5** — `_AdminUserDataFilterPanel.cshtml` rewrite: her aktif FilterDefinition için kalıcı section + "Hepsi" toggle + multi-select. `AdminUserFormViewModel.FilterDefinitions` + AdminController.Users 3 yerde inject. Default boş (deny-by-default tutarlı). Commit `3f5f2d9`.
- **Plan 07 Faz 5b (yeni)** — Kullanıcı kritik mimari nokta açtı: 3 sistem (PDKS/DER/IK=Zirve) için ayrı sube listeleri. Çözüm: canonical Sube master + SubeMapping (per-DataSource external code). Migration 21, `Models/Sube.cs` (Sube + SubeMapping), DbSet + OnModelCreating, FilterOptionsService.NativeSources['sube'], FilterDefinitionService.ValidateAsync esnetildi (canonical native source kabul), UserDataFilterInjector translate logic + sessiz drop, 3 yeni test. Migration kullanıcı tarafından PortalHUB'da çalıştırıldı (Sube=5, SubeMapping_PDKS=5, DER/IK=0). Commit `241eeb0`.
- **HTML attribute parse fix** — Alpine.js x-data attribute içindeki JSON çıktısı `"` HTML attribute'unu kırıyordu. `<script>window.__userFilterSections=...</script>` global variable pattern'a geçildi (Faz 5 commit'inde).
- **Plan dosyası genişletildi** — `plans/07-yetki-filter-revizyon.md` Faz 5b adım listesiyle güncel.

## Build / Test Durumu
- Build: yeşil (son commit'te `241eeb0`)
- Test: 232 yeşil (217 + 15 yeni Plan 07 testleri)
- Smoke test (browser): UI render OK, Sube + Rapor Kategorisi 2 section görünüyor, ama EditUser POST submit kırıldı — sebep aşağıda

## Commit Durumu
- Uncommitted: 1 dosya (`ReportPanel/Controllers/AdminController.Users.cs` — bug fix, dev server kilit yüzünden derlenemedi)
- Bu oturum yeni commit'leri (4 Mayıs sonu + 5 Mayıs erken):
  - `4df24d7` feat(filter): EF model + FilterDefinitionService
  - `122d8af` refactor(filter): FilterOptionsService DB-driven
  - `6056546` feat(filter): deny-by-default + Export inject
  - `3f5f2d9` feat(filter): user form dinamik field
  - `241eeb0` feat(filter): Sube canonical + SubeMapping translate

## Yarım Kalan / Yarına Bırakılan İşler
- **EditUser POST handler bug fix commit** — `User.UserId` üzerinde `[BindNever]` (M-07 mass assignment koruması) → form'dan UserId hidden gelse bile binder atmıyor → `user.UserId=0` → UpdateAsync(0,...) → "Kullanıcı bulunamadı". Fix yapıldı: route'tan `int id` parametresi alınıyor (`EditUser(int id, User user)`), UpdateAsync(id, input) çağrılıyor. **Dev server (PID 38828) dll'i kilitliyor**, build edilemedi → commit yapılamadı. Yarın server durdurulup build + test + commit. Geçici DEBUG kodu (Console.WriteLine + Message zenginleştirme) kaldırıldı.
- **DER SubeMapping seed boş** — Faz 5b migration'da DER (DerinSIS) için mapping eklenmedi. Sebep: PDKS sube isimleri ile DerinSIS posMagaza isimleri otomatik eşleşmiyor (FSM vs FSM Mğz vs ÖZLÜCE Mğz). Manuel eşleştirme gerekiyor — Faz 6 Admin UI'da yapılacak veya kullanıcı doğrudan SQL ile.
- **IK (Zirve) SubeMapping** — Kullanıcı kararı gereği Plan 07 kapsamı dışı. Sonra eklenir.
- **GENEL MÜDÜRLÜK semantiği** — kullanıcı notu: GM çalışanı tüm şubeleri görebilmeli. Implementation değişikliği gereksiz; admin "Hepsi" toggle ile karşılayabilir, kullanım kararı.
- **Faz 6** — Admin "Filtreler" + "Şubeler" CRUD UI (Şubeler tab'ı + EditSube + SubeMapping yönetimi). ~3-4h.
- **Faz 7** — Reports/Index kategori filtresi (reportAccess scope), ReportsController.Index sorgusunda kategori join. ~1.5h.
- **Faz 8** — Test + arşivle (smoke senaryoları + plan'ı archive'a taşı). ~1.5h.
- **Mimari bütünlük analizi** — Plan 07 bittiğinde bilinçli sapmaları (FilterOptionsService DRY, IsSafeOptionsQuery paylaşım, vs) gözden geçir (memory: project_mimari_butunluk_analizi.md).
- **Plan 08 Faz 7 smoke test** — wiki-keeper field test, dünden bekliyor.

## Kararlar
- **Sube canonical pattern (4 Mayıs onayı):** Master `Sube` tablosu + `SubeMapping` (DataSourceKey, ExternalCode). UserDataFilters SubeId tutar; UserDataFilterInjector translate eder. Eksik mapping = sessiz drop (kullanıcı kararı).
- **Sadece Sube master, generic değil:** Bolum/Kategori vb. başka entity'ler için aynı pattern ihtiyaç çıkınca eklenir; şimdi prematüre değil.
- **IK (Zirve) Plan 07 kapsamı dışı:** mapping sonra eklenir.
- **FilterDefinitionService.ValidateAsync esnek:** spInjection + DataSourceKey/OptionsQuery NULL kombinasyonu canonical native source ise kabul (FilterOptionsService.HasNativeSource ile runtime kontrol).
- **HTML attribute pattern:** JSON içeren x-data için `<script>window.__var = ...</script>` global variable pattern; inline `@Html.Raw(json)` HTML parse'ı kırıyor.
- **EditUser POST signature düzeltmesi:** `[BindNever]` PK'lar için route parametresi explicit alınmalı (`EditUser(int id, User user)`). Pattern not.

## Dikkat Edilmesi Gerekenler
- **Dev server kilit pattern'ı kronik:** ReportPanel.exe çalışırken `dotnet build` apphost copy fail; `-p:UseAppHost=false` ile bypass edilebilir ama ben her seferinde takıldım. Bilinen workaround.
- **PortalHUB DB instance ayrımı:** Kullanıcının SSMS'i farklı bir SQL instance'a default bağlanmış olabilir (PortalHUB orada yok). Migration 21'in ilk denemesi default DB'de yetim Sube tablosu oluşturmuş olabilir — kullanıcı SSMS'te kontrol etsin (sonradan temizlemeli).
- **Mevcut "sube" testleri "bolum"'a taşındı** — Faz 5b'de sube translate logic mevcut raw value testlerini kırdı. Eski semantik için yeni FilterKey kullanıldı.
- **DEBUG kod zaten yer aldı ve kaldırıldı**: AdminController.Users.cs içindeki Console.WriteLine + Message debug formdump kodu temizlendi son edit'te.
- **Browser SSMS yanlış instance** — kullanıcının "PortalHUB veritabanı yok" hatası; sonradan doğru instance'a geçince migration tamamlandı.

## Yarına Başlangıç Noktası
1. **Dev server'ı durdur** (kullanıcı tarafından — PID 38828) → `dotnet test --nologo -p:UseAppHost=false` ile build + test → AdminController.Users.cs commit (`fix(filter): EditUser POST route id binding (plan: 07)`).
2. **Browser smoke** — `/Admin/EditUser/{id}` aç, bir Sube seç + güncelle, "Kullanıcı güncellendi" mesajı görmeli; DB'de UserDataFilters'ta SubeId kayıtları olmalı; admin GM çalışanı seçince "Hepsi" toggle ile tümünü görmeli.
3. **DER SubeMapping seed** — Manuel SQL ile veya Faz 6 Admin UI bekle. Geçici manuel: `INSERT INTO dbo.SubeMapping (SubeId, DataSourceKey, ExternalCode) SELECT s.SubeId, 'DER', '<mekanID>' FROM dbo.Sube s WHERE SubeAd = '<...>'` × N.
4. **Plan 07 Faz 6 başlat** — Admin "Filtreler" + "Şubeler" + "SubeMapping" CRUD ekranları (~3-4h).
