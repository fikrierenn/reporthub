# Oturum Günlüğü — 2026-05-04

## Ana Konu

Yoğun bir tek-oturum: ~7 saat, **19 commit**, üç paralel akış:
1. **Aktif sıra sentezi + housekeeping** — TODO.md tek senteze toplandı, 3 tamamlanan plan arşivlendi
2. **Kalite borcu refactor** — `js-conventions` + `csharp-conventions` hard-limit aşan 4 dosya partial class/modül split
3. **Yetki revizyonu konuşması + Plan 07 onayı** — kullanıcı veri filter sistemi UX/güvenlik problemini açtı, plan dosyası yazıldı + onaylandı

## Tamamlananlar

### Yeni özellikler / fix
- **Tablo widget CSV indir butonu** (`143e1d9`) — Dashboard tablo widget header'ına CSV İndir butonu, UTF-8 BOM + `;` delimiter (Türk Excel uyumlu), iframe sandbox `allow-downloads`
- **Hesaplı kolon autocomplete suggest pills** (`36dd2f3`) — V2 builder formula textarea altına aktif tablo widget kolon önerileri, tıkla → formula'ya iliştir
- **Mobile responsive dashboard grid** (`0eacfe9`) — `DashboardShellRenderer.GridColsClass` `grid-cols-4` sabit → `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`
- **PreviewDashboardV2 invalid config audit** — Run path ile parite, `dashboard_config_invalid` audit
- **M-08 Async tutarlılık** (`f71c6ec`) — `AdminController.CreateUser` GET sync→async (DB hit'li tek sync action)
- **M-09 AsNoTracking sweep** (`f71c6ec`) — 14 read query AsNoTracking eklendi (AdminController.Index/CreateUser/EditReport/EditUser/BuildCreateUserFormAsync, TestController)
- **M-07 [BindNever] mass assignment defansif** (`12367e0`) — User/ReportCatalog/Role/ReportCategory/DataSource models kritik field'lara
- **F-05 Türkçe UTF-8 normalize** (`326e651`) — JS dosyalarında ASCII kalıntılar (`Duzenle`/`Bilesen`/`Iptal`/`Once`/`secin`/...) UTF-8'e
- **Tailwind Play CDN local serve** (`762720f`) — `wwwroot/lib/tailwindcss/tailwindcss.js` (407KB), `_AppLayout.cshtml` script src güncellendi

### Refactor (hard-limit aşımı split — 4 dosya)
- **builder-v2/builder-drawer.js** 511 → 269+259 (`10bf631`) — yeni `builder-table.js` (Plan 05 tablo Setup tab + hesaplı kolon mixin)
- **DashboardClientScripts.cs** 551 → 116+107+120+257 (`5649dab`) — `Kpi.cs` + `Chart.cs` + `Table.cs` partial
- **ReportsController.cs** 907 → 337+234+369 (`4404fd5`) — `Run.cs` + `V2Preview.cs` partial
- **AdminController.cs** 984 → 292+97+144+207+175+144 (`2fd043e`) — `SpExplorer.cs` + `DataSources.cs` + `Reports.cs` + `Users.cs` + `RolesCategories.cs` partial

### M-03 Faz C — User.Roles CSV kolon DROP (`18e7e25`)
- `Database/19_DropUserRolesCsv.sql` (idempotent + default constraint handler)
- `User.cs`/`ReportPanelContext`/`UserManagementService` pragma + Roles field temizliği
- DB'de drop edildi (kullanıcı çalıştırdı, idempotent doğrulama yaptı)
- ADR-003 Faz C "TAMAMLANDI" notu

### Audit-only kapanışlar (zaten yapılmıştı, sentez yanılgısıydı)
- **G-08** DashboardRenderer JSON escape — `DashboardRendererTests.cs` 6 escape testi zaten kapsıyor
- **G-07** iframe policy — Run.cshtml temiz, V2 preview admin-only kabul edilebilir risk
- **F-03** dashboard-builder.js memory leak — F-7 split'te zaten çözüldü (event delegation + dragDropBound guard)
- **F-02** admin override UI — commit `816c8c2`'de zaten kapalı (`sp-helper.js:34-89`)
- **M-02** son grep — 5 `ex.Message` kullanımı tarandı, hepsi kabul (audit log alanı + suppress + parser)
- **CreateUser P0** veri filtresi — `CreateUser.cshtml:133`'te zaten partial çağrısı var
- **Dashboard P0** config deserialize try/catch — Run path zaten kapsıyor (line 591-612)
- **Dashboard P1** RS index validation — validator + resolver + 6 ResolveResultSet_* test zaten kapsıyor
- **ReportParamValidator Run path kök fix** — commit `2b62d39` PreviewV2 düzeltirken tüm parse `ParseSchema()` ortak helper'a yönlendirildi

### Belge
- **ADR-001 data-access** (`f194b39`) — SP rapor/dashboard + EF metadata hibrit kararı yazılı
- **TODO sentez** (`20d1243`) — 5 plan + FAZ 1-3 + journal taranarak ~50 açık iş kolaydan zora kategorize
- **Plan 03+04+06.B arşivlendi** — `plans/archive/`
- **M-13 PLAN 03 detay çizelgesi TODO.md'den temizlendi** — duplikasyon, arşive yönlendirildi

### Plan 07 — Yetki + Veri Filter Revizyonu (yazıldı + onaylandı)
- `plans/07-yetki-filter-revizyon.md` (`50b96e3` + `7a47baa` + `321cbfe`)
- 8 fazlı, ~15 saat, ~25 dosya, Tier 3
- 4 karar onaylı:
  1. 0 kayıt → 403 reddet (audit `data_filter_denied`)
  2. Migration backfill: mevcut 7 user × FilterDefinition tüm `*` (yumuşak geçiş)
  3. Yeni user formunda default **boş** (admin mutlaka karar versin)
  4. Faz 7 dahili — Reports/Index `raporKategori` filtresi
- Naming: `sube` (Scope=spInjection, PDKS/DerinSIS), `raporKategori` (Scope=reportAccess, native EF), `urunKategori` (admin sonradan ekler)

## Build / Test Durumu

- Build: yeşil (her commit sonrası compile-only check, 0 uyarı 0 hata)
- Test: **217 yeşil**, 0 kırmızı (her major refactor sonrası tekrar koşuldu)
- Smoke test: yapılmadı (auth-gerekli sayfalar; kullanıcı tarafında manuel)

## Commit Durumu

- **Yeni commit'ler: 19** (tarih sırası):
  - `143e1d9` CSV İndir butonu
  - `20d1243` TODO sentez + M-13 arşivleme
  - `18e7e25` M-03 Faz C User.Roles drop
  - `f194b39` ADR-001 + F-03 audit
  - `36dd2f3` Hesaplı kolon autocomplete suggest pills
  - `31201af` küçük-iş kategorisi audit kapanışları
  - `0eacfe9` Dashboard P0/P1 (mobile grid + PreviewV2 audit)
  - `f71c6ec` M-08 async + M-09 AsNoTracking
  - `cdb7f84` V1 builder split audit + V2 drawer aşım not
  - `10bf631` V2 builder-drawer.js split (511→269+259)
  - `5649dab` DashboardClientScripts split (551→4 partial)
  - `4404fd5` ReportsController split (907→3 partial)
  - `2fd043e` AdminController split (984→6 partial)
  - `12367e0` M-07 [BindNever]
  - `326e651` F-05 Türkçe UTF-8 normalize
  - `762720f` Tailwind Play CDN local serve
  - `50b96e3` Plan 07 oluşturuldu
  - `7a47baa` Plan 07 kullanıcı onayı + 4 karar
  - `321cbfe` Plan 07 native EF source registry netleştirme
- Uncommitted: 1 dosya (`TODO.md` — yetki konuşması sırasında muhtemelen küçük edit, bu handoff'ta dokunulmaz)

## Yarım Kalan / Yarın'a Bırakılan İşler

- **Plan 07 implementasyonu** — onaylı, 8 faz, ~15 saat. Faz 1 (DB migration + backfill) ile başla:
  - `Database/20_AddFilterDefinition.sql` yaz (idempotent, FilterDefinition tablosu + 2 seed: `sube` external SQL, `raporKategori` native EF — OptionsQuery NULL)
  - Backfill: mevcut 7 user × 2 FilterDefinition → `*` kayıt (~13 yeni satır)
  - DB'de çalıştır + idempotency doğrula
- **TODO.md uncommitted edit** — yetki konuşması sırasında küçük değişiklik, kontrol et + commit (içerik bilinmiyor, muhtemelen önceki sentez işaretleme)
- **Agent worktree temizliği** — `.claude/worktrees/agent-{a76f5c75f4bd71bb3,ab49a38d121720c78}` lock'lı, `git worktree remove --force` gerekli (bugün ReportsController split agent worktree'sinde başarılı oldu ama eski main fork ettiği için kullanılmadı; AdminController agent durduruldu, worktree clean)
- **NotebookLM brain push** — bu oturumun özetini Brain notebook'una push etmek için `notebooklm source add` gerekli (auth durumu belirsiz)
- **Bağımlı işler kuyruğu** (TODO sentez sıralı):
  - Inline RS lazy-load (3h)
  - DateTime Faz D DB DEFAULT GETDATE→GETUTCDATE (2-3h)
  - SP mimarisi sp_PdksPano → inline TVF (3h)
  - M-10 Faz 4-6 Named Result Contract (4h)
  - **G-09 SP read-only login** ⚠️ canlıya çıkmadan zorunlu (en sona) (2h)

## Kararlar

- **Hard-limit ihlali = derhal split** (kullanıcı kuralı netleştirdi 4 Mayıs): "uymuyorsa mantıklı şekilde bölmelisin tüm elden geçirmelerde geçerli". 3 C# dosyası + 1 JS dosyası anında splitlendi. csharp-conventions hard 500 + js-conventions hard 350 zorunlu kabul.
- **Agent worktree güvenilmez** — Cowork'te `isolation: worktree` parameter'ı eski main commit'ten fork ediyor, güncel feature branch görmüyor → 1028 vs 907 satır + 66 vs 217 test farkı. Manuel split daha güvenli sonuç verdi. Bu deneyim ileride `.claude/rules/`'e yazılabilir.
- **Plan 07 onayı:** 0 kayıt = 403, default boş, backfill var, raporKategori native EF (OptionsQuery yazma yok). Implement yarına bırakıldı (Tier 3 + uzun oturum).
- **Plan 03 (M-11 DashboardV2 standalone)** retro arşivlendi — Done criteria zaten karşılıydı (commit'ler `730d5ef` `eec48ab` `bb6cb1f` `5f8d20b`), sadece `plans/archive/`'a taşınmamıştı.
- **TODO.md M-13 detay çizelgesi temizliği** — kapalı planın 30+ unchecked çizelgesi `plans/archive/`'a yönlendirildi (CLAUDE.md "aynı bilgi iki yerde yaşamaz" kuralı).
- **Yetki revizyonu UX kararları (kullanıcı, 4 Mayıs):**
  - "filter satırı ekle" soyut UX terk edildi → User formunda **dinamik field** (her aktif FilterDefinition için kalıcı alan)
  - "0 satır = tümü" eski semantiği güvensiz, deny-by-default → "Hepsi" explicit `FilterValue='*'` kayıt
  - 300-500 user ölçeğinde manuel atama imkansız → ileride RoleDataFilters (Plan 08'e bırakıldı)

## Dikkat Edilmesi Gerekenler

- **Sentez yanılgıları** — yeni eklediğim "AKTIF SIRA" bölümünde 4-5 maddeyi yanlış "açık" göstermişim, audit ile kapatıldı (F-02, F-03, G-07, G-08, M-02, CreateUser P0, Dashboard P0/P1, ReportParamValidator Run path). İleride sentez yazarken **her madde için kod doğrulaması yap** — TODO başlığına güvenme.
- **Compile sorunu** — `dotnet build` her seferinde `ReportPanel.exe` (running instance) kilidi nedeniyle MSB3027 error veriyor; **C# compile başarılı** olduğu için `dotnet build -t:Compile` ile bypass ediyorum. Test'ler `--no-build` ile bin klasöründeki binary üzerinden çalışıyor → değişiklikleri test etmek için running instance'ı kapatmak gerek.
- **Agent worktree iz bırakıyor** — durdurulan/biten agent'lar `.claude/worktrees/agent-*` altında lock'lı kalıyor, manuel `git worktree remove --force` gerekli. Yarın temizle.
- **Plan 06 vNext (şirket içi portal)** Plan 07 üzerine oturur — granular role + permission sistemi geldiğinde FilterDefinition + UserDataFilters infrastructure değişmez, sadece üzerine eklenir.

## Yarına Başlangıç Noktası

1. **Uncommitted TODO.md kontrol** — `git diff TODO.md` ile bak, anlamlıysa commit, değilse `git checkout` (önemsiz edit ise)
2. **Agent worktree temizliği** — `git worktree remove --force .claude/worktrees/agent-a76f5c75f4bd71bb3 .claude/worktrees/agent-ab49a38d121720c78`
3. **Plan 07 Faz 1 başlat:**
   - `Database/20_AddFilterDefinition.sql` yaz (FilterDefinition tablo CREATE + 2 seed)
   - Backfill SQL: mevcut 7 user × 2 FilterDefinition `*` insert
   - DB'de çalıştır (idempotent doğrula)
   - Commit: `feat(filter): Migration 20 FilterDefinition + backfill (plan: 07)`
4. Faz 2-8 sırasıyla (toplam ~13h kalan implement süresi)

---

# Oturum 2 — Plan 07 Faz 1 + Plan 08 LLM Wiki / Obsidian Brain

## Ana Konu

İki büyük iş paralel: (1) Plan 07 Faz 1 DB migration (FilterDefinition altyapısı), (2) Karpathy "LLM Wiki" pattern'iyle yeni cross-project Obsidian Brain vault kurulumu (Plan 08, 7 faz). NotebookLM Brain push da sıralı işlerden biriydi.

## Tamamlananlar

### Plan 07 Faz 1 — Migration 20 (FilterDefinition + backfill) ✓

- `ReportPanel/Database/20_AddFilterDefinition.sql` yazıldı (idempotent CREATE + UQ + FK + CHECK Scope + 2 seed + backfill)
- İlk denemede FK uzunluk uyumsuzluğu (script `NVARCHAR(100)`, gerçek tablo `nvarchar(50)`) — `INFORMATION_SCHEMA.COLUMNS` ile doğrulandı, MCP `describe_table` yanıltmıştı
- Düzeltme: `NVARCHAR(50)` (DataSources/UserDataFilters/ReportCatalog ile aynı)
- DB'de çalıştırıldı, sonuç: FilterDefinition=2, UserDataFilters_total=15 (önceki 2 + 13 backfill `*`), aritmetik tutarlı
- Commit `08e058a` `feat(filter): Migration 20 FilterDefinition + backfill (plan: 07)`

### Plan 08 — LLM Wiki / Obsidian Brain (Faz 1-6 ✓, Faz 7 sonraki oturum)

**Plan dosyası:** `plans/08-llm-wiki-obsidian-brain.md` — Karpathy gist (https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) referansıyla, çoklu vault yaklaşımı (alternative C), 7 faz ~10h, GitHub push yok local-only (kullanıcı kararı).

**Faz 1 ✓ Vault iskelet:**
- `D:/Dev/brain/` git init local-only
- Klasör yapısı: `entities/{people,projects,companies,systems}`, `concepts/{architecture,patterns,principles}`, `synthesis/`, `raw/`
- `CLAUDE.md` (vault schema — Claude için talimatlar), `README.md`, `log.md` (changelog), `index.md` (Dataview master katalog), `.gitignore`

**Faz 2 (kısmen, kullanıcı tarafında):**
- Obsidian indirildi + kuruldu, vault olarak `D:/Dev/brain` bağlandı
- Plugin'ler yüklü (Dataview 0.5.68, Periodic Notes 0.0.17, Templater 2.20.0) — toggle açma kullanıcının sorumluluğunda
- Web Clipper Firefox extension henüz kurulmadı (link sağlandı: https://addons.mozilla.org/en-US/firefox/addon/obsidian-web-clipper/)

**Faz 3 ✓ 12 initial seed:**
- `entities/people/fikri.md` (auto-memory + skill referansları)
- `entities/projects/{reporthub,bkm-kitap,belinza}.md` (BKM Kitap stub: kapsam yok; Belinza skill referanslarıyla)
- `entities/companies/{bkm,anthropic}.md`
- `entities/systems/{claude-code,sql-server,obsidian,notebooklm}.md`
- `concepts/patterns/llm-wiki.md` (Karpathy gist tam özet)
- `concepts/principles/context-management.md` (`docs/CONTEXT_MANAGEMENT.md` cross-project versiyonu)
- `concepts/architecture/plan-first-tier-system.md` (ADR-010 özet)
- Her sayfada frontmatter (type, tags, created, updated, related, status) + cross-link + Update Log
- Brain commit (lokal): `seed: 12 initial entities + concepts (plan: 08 faz 3)`

**Faz 4 ✓ wiki-keeper skill:**
- `.claude/skills/wiki-keeper/SKILL.md` + 3 template (`entity.md`, `concept.md`, `synthesis-week.md`)
- 2 mod: **Update** (handoff sonrası — journal parse → entity update → log.md append → synthesis weekly), **Lint** (stale/orphan/broken-link/duplicate/missing-cross-ref tarar, `raw/vault-lint-YYYY-MM-DD.md` raporu)
- **Commit ETMEZ** — staged bırakır, kullanıcı review eder (Karpathy review-first)
- Harness skill'i yükledi, "wiki-keeper" listede görünüyor
- Commit `7fedeb2` `feat(skill): wiki-keeper for brain auto-update (plan: 08)`

**Faz 5 ✓ NotebookLM senkron script:**
- `D:/Dev/brain/scripts/sync-notebooklm.sh` — manuel haftalık, `entities/projects/*` ve `synthesis/*` Brain notebook'a push
- Mantık: aynı title source varsa delete + re-add (markdown refresh desteklenmiyor, sadece URL/Drive)
- Encoding fix dahil (`PYTHONIOENCODING=utf-8`, Windows cp1254 emoji bypass)
- Brain commit (lokal): `feat: notebooklm sync script + log.md Faz 4-6 (plan: 08)`

**Faz 6 ✓ ReportHub entegrasyonu:**
- `CLAUDE.md` başına Cross-project brain referansı (D:/Dev/brain, Karpathy LLM Wiki, NotebookLM ile paralel, sentez brain'de yaşar)
- Auto-memory: `reference_brain_obsidian.md` yeni entry + `MEMORY.md` index güncellendi
- Commit `56e1f23` `docs(brain): cross-project Obsidian brain reference (plan: 08)`

**Plan 08 status update:**
- Plan dosyası "Taslak" → "Uygulamada", Faz 1-6 commit serisi referanslandı, Faz 7 (smoke) sonraki oturum
- Commit `a926c19` `docs(plan-08): Faz 1-6 implement edildi — durum güncelle`

### Diğer

- **TODO.md audit** — sabah handoff'unda atlanmış olan CreateUser P0 audit kapanışı `[ ]` → `[x]` işaretlendi (zaten audit kapanmıştı, sadece TODO.md güncel değildi). Commit `1283f1e`.
- **Agent worktree temizlik** — `agent-a76f5c75f4bd71bb3` zaten yoktu, `agent-ab49a38d121720c78` lock'lı (pid 27904 = bu oturumun claude.exe'si), kullanıcı onayıyla `git worktree remove -f -f` ile silindi. Geriye 4 `claude/*` worktree (Cowork agent'ları) kaldı, dokunulmadı.
- **NotebookLM Brain push** (`f2407372`) — 2 yeni journal eklendi (2026-05-01.md, 2026-05-04.md), eski CLAUDE.md/TODO.md silinip güncel haller yeniden eklendi (markdown refresh desteklenmediği için delete + re-add), encoding fix gerekti (`PYTHONIOENCODING=utf-8 PYTHONUTF8=1`). Brain'de toplam 13 source senkron.

## Build / Test Durumu

- Build: yapılmadı (DB migration ve markdown ağırlıklı oturum)
- Test: yapılmadı (kod değişikliği yok)
- Smoke: DB migration sonucu validation query ile doğrulandı (FilterDefinition=2, UserDataFilters_with_*=13). Obsidian smoke kullanıcı tarafında (plugin toggle + index.md preview).

## Commit Durumu

- **Yeni commit'ler (ReportHub, 5):**
  - `1283f1e` docs(todo): CreateUser P0 audit kapanışı işaretle
  - `08e058a` feat(filter): Migration 20 FilterDefinition + backfill (plan: 07)
  - `56e1f23` docs(brain): cross-project Obsidian brain reference (plan: 08)
  - `7fedeb2` feat(skill): wiki-keeper for brain auto-update (plan: 08)
  - `a926c19` docs(plan-08): Faz 1-6 implement edildi — durum güncelle
- **Yeni commit'ler (Brain vault local, 3):**
  - `init: brain vault iskelet (plan: 08)`
  - `seed: 12 initial entities + concepts (plan: 08 faz 3)`
  - `feat: notebooklm sync script + log.md Faz 4-6 (plan: 08)`
- Uncommitted (ReportHub): 0 dosya
- Brain vault: temiz

## Yarım Kalan / Yarın'a Bırakılan İşler

### Plan 08 (vault implement)

- **Faz 7 — Smoke test** — wiki-keeper'ı handoff sonrası tetikleyip vault'a güncelleme yazdığını doğrula, sonra plan dosyasını arşivle (`git mv plans/08-*.md plans/archive/`).
- **Obsidian plugin toggle (Faz 2 kullanıcı kısmı)** — Dataview/Periodic Notes/Templater enable + reload + index.md verify (Dataview blok'lar liste üretmeli).
- **Web Clipper Firefox extension** kurulum — kullanıcı tarafında, manuel.
- **Vault seed iterasyonu** — Fikri Obsidian'da gez, eksik/yanlış gördüğü yerleri söyleyince genişletilir (compounding).

### Plan 07 (yetki+filter, ~13h kalan)

- **Faz 2 — EF model + FilterDefinitionService (~2h)**
- **Faz 3 — FilterOptionsService refactor DB-driven (~1h)**
- **Faz 4 — UserDataFilterInjector deny-by-default (~1h)**
- **Faz 5 — UI dinamik field user formunda (~3h)**
- **Faz 6 — Admin Filtreler CRUD (~3h)**
- **Faz 7 — Reports/Index kategori filtresi (~1.5h)**
- **Faz 8 — Test + smoke + arşivle (~1.5h)**

### Diğer borçlar (öncelik sıralı)

- **G-09 SP read-only login** ⚠️ canlıya çıkmadan zorunlu (~2h)
- **DateTime Faz D** — DB DEFAULT GETDATE → GETUTCDATE (2-3h)
- **SP mimarisi** — `sp_PdksPano` → inline TVF refactor (3h, ADR-004 adayı)
- **M-10 Faz 4-6** — Named Result Contract (4h)
- **Inline RS lazy-load** — 10K satır limit (3h)

## Kararlar

### Plan 08 ana mimari kararlar (kullanıcı 2026-05-04)

- **GitHub push YOK** — brain vault local-only kalır. Disk/makine kaybı kabul edilen risk; ileride OneDrive/external disk opsiyonel.
- **Çoklu vault yaklaşımı (alternatif C)** — brain ayrı vault, repo'lar ayrı, markdown link/file:/// URL ile bağlı. Symlink yok (Windows kırılganlık).
- **Web Clipper Firefox** — kullanıcı default browser.
- **BKM Kitap repo yok** — sıfırdan stub seed, somut iş çıktığında doldurulur.
- **NotebookLM paralel kalır** — bırakılmıyor. Obsidian = primary maintained, NotebookLM = Q&A endpoint + mobil okuma. Haftalık manuel sync script.

### Karpathy LLM Wiki adaptasyonu

- **Sentez brain'de, ham bilgi repo'da** — duplikasyon yok, kaynak ayrımı net.
- **wiki-keeper commit etmez** — staged bırakır, kullanıcı review eder. session-handoff'taki auto-commit istisnası burada **geçerli değil** çünkü ayrı repo + Karpathy'nin review-first prensibi.
- **Çelişkiler silinmez, flag'lenir** — `## Contradiction` başlığı altında, eski + yeni tarihlerle.

### Plan 07 Faz 1 (DB migration kullanıcı kararı)

- **DB describe yanıltabilir** — gerçekten emin olmak için `INFORMATION_SCHEMA.COLUMNS` kullan. MCP `describe_table` yanlış DataSourceKey uzunluğu döndürdü (`100` ama gerçek `50`).

## Dikkat Edilmesi Gerekenler

- **wiki-keeper henüz "field test" edilmedi.** Skill yazıldı ama gerçek bir handoff sonrası tetiklenip vault'a yazma davranışı doğrulanmadı (Faz 7 = smoke). İlk gerçek run'da bug çıkabilir, kullanıcı review etmeli.
- **`describe_table` güvenmemek** — INFORMATION_SCHEMA daha güvenilir. Plan 07 Faz 1'de bu yanılgıdan dolayı CREATE TABLE patladı, sonradan düzeltildi.
- **Brain vault local-only backup yok** — disk failure = veri kaybı. Kullanıcı kabul etti, ileride çözüm gerek.
- **Plan 08 dosyası uzun (335 satır)** — 200 satır eşiği plan dosyaları için zorunlu değil ama bilgi yoğunluğu yüksek. Arşivlenmeden önce gerekirse refactor.
- **Obsidian'da `.obsidian/plugins/*/main.js` git'e girdi** — gitignore sadece `data.json` hariç tutuyordu, plugin kodu (300KB+) brain repo boyutunu büyütüyor. İhtiyaç olursa gitignore daha sıkı yapılabilir (`.obsidian/plugins/` tamamen exclude).

## Yarına Başlangıç Noktası

1. **Plan 08 Faz 7 smoke test** — bir sonraki oturum sonu (yarın `/handoff` çağırınca) wiki-keeper otomatik tetiklensin → brain vault'a staged değişiklik yazsın → kullanıcı review etsin → çalışıyorsa plan arşivle (`git mv plans/08-*.md plans/archive/`)
2. **Obsidian'da Faz 2 kontrol** — Dataview/Periodic Notes/Templater toggle açık mı, `index.md` Dataview blok'ları liste üretiyor mu
3. **Plan 07 Faz 2 başlat** — `Models/FilterDefinition.cs` EF model + `Services/FilterDefinitionService.cs` CRUD + `ReportPanelContext.FilterDefinitions` DbSet (~2h)
4. **Vault seed iterasyonu** — Fikri Obsidian'da gez, BKM Kitap/Belinza/diğer sayfalarda eksik/yanlış gördüğü yerleri söyleyince genişletilir
