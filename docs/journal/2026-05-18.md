# Oturum Günlüğü — 2026-05-18

## Ana Konu
Plan 33 Faz 2 devamı: C-03 TamimReminderJob tamamlandı. Ardından caveman tool ekosistemi kuruldu ve cavemem ilham alınarak turn-logger Stop hook yazıldı.

## Tamamlananlar
- ✅ **C-03 TamimReminderJob** (`Mosaik.Modules.Circular/Services/TamimReminderJob.cs`) — günlük 09:00 cron, dün yayınlanan tamimler için AuditLog'dan okumamış kullanıcıları bulup `INotificationService.CreateBulkAsync` ile bildirim gönderiyor. EF Core `Database.SqlQuery<T>($"...")` FormattableString pattern (SqlClient bağımlılığı olmadan).
- ✅ **C-06 ObligationsController.Edit** (3a28fa0) — önceki oturumda tamamlandı, bu oturum doğrulandı.
- ✅ **caveman tool kurulumu** — `~/.claude/settings.json` hooks (SessionStart caveman-activate.js, UserPromptSubmit caveman-mode-tracker.js) + commands (caveman.md, caveman-commit.md, caveman-review.md, caveman-compress.md) + skills (caveman/SKILL.md, caveman-compress/SKILL.md) + statusline (caveman-statusline.ps1).
- ✅ **cavemem-inspired turn-logger** (`~/.claude/hooks/turn-logger.js`) — Stop hook: her tur sonunda `turn_summary` veya `last_assistant_message` alıp caveman sıkıştırması uygular, `~/.claude/projects/D--Dev-reporthub/turn-log.jsonl`'a append eder.
- ✅ **settings.json Stop hook** eklendi — `node turn-logger.js`, timeout 5s.
- ✅ **session-start.sh turn-log injection** — turn-log.jsonl mevcutsa son 5 giriş "Son tur ozetleri" bölümüne enjekte edilir.
- ✅ **Memory compression sweep** — 72 memory dosyası tarandı, hepsi zaten terse yazılmış (<3% savings), compression gerekmedi.
- ✅ **ARCHITECTURE_MAP refresh** (c1ac8dd) — test=263, migration 60 dosya.

## Build / Test Durumu
- Build: çalıştırılmadı (bu oturumda kod değişikliği sadece TamimReminderJob — önceki oturumda 340/340 geçmişti)
- Test: önceki oturum 340/340 yeşil (37b6de0 commit'inde doğrulandı)
- Smoke test: yapılmadı

## Commit Durumu
- Uncommitted dosya sayısı: 4 (M `ReportParamValidator.cs`, M `Run.cshtml`, ?? `docs/security-findings-2026-05-12.md`, ?? `kod_toplayici.ps1`)
- Yeni commit'ler:
  - `37b6de0` feat(circular): C-03 TamimReminderJob
  - `c1ac8dd` docs(map): ARCHITECTURE_MAP auto-refresh 2026-05-18
- Not: caveman hook dosyaları `~/.claude/` altında — bu repo'ya commit edilmiyor.

## Yarım Kalan / Yarına Bırakılan İşler
- **C-01** DailyReminderJob (3-4h) — Calendar + Compliance + Notification + Plan 31 caller. En yakın.
- **C-05** ContractsController partial split (1h) — Files.cs + Ai.cs. En hızlı.
- **C-02** Plan 22 Holidays entity (10-14h) — ADR-016 öncesi yazılmalı.
- **C-04** AI Wizard Faz 1 (8-10h)
- **C-07** Plan 27 Faz A eksikleri (4-6h)
- **C-08** Plan 25.1 Faz 5+6 (5-7h)
- `ReportParamValidator.cs` + `Run.cshtml` uncommitted kalıyor — ne değiştiği bilinmiyor, commit öncesi diff okunmalı.

## Kararlar
- **cavemem kurma yerine ilham al** — SQLite + npm bağımlılığı reddedildi. Turn logging bash/node ile native, proje-spesifik jsonl dosyasına gidiyor.
- **Remote script çalıştırma güvenlik kuralı** — `irm | iex` pattern yasak. Doğru akış: WebFetch → içeriği göster → kullanıcı onayı → çalıştır.

## Dikkat Edilmesi Gerekenler
- `ReportParamValidator.cs` ve `Run.cshtml`'deki uncommitted değişiklikler: ne amaçla yapıldığı bilinmiyor — `git diff` ile okunmalı, commit edilmeli veya revert edilmeli.
- `kod_toplayici.ps1` untracked — kullanıcının kendi scripti, dokunma.
- TODO.md'de C-03 ve C-06 hâlâ `[ ]` — yarın başında kapatılmalı.

## Yarına Başlangıç Noktası
1. `git diff Mosaik/Services/ReportParamValidator.cs Mosaik/Views/Reports/Run.cshtml` — ne değişmiş? Commit et veya revert.
2. TODO.md: C-03 ve C-06'yı `[x]` yap.
3. C-05 ContractsController partial split (1h) — sıcak başlangıç.
4. C-01 DailyReminderJob (3-4h) — C-05 ardından.

## Compact Snapshot --- 12:39

### Son 5 Commit

- cf5fd44 fix(calendar): migration 58 SQL syntax — RESTRICT→NO ACTION, Firmas(FirmaId), Status int values
- 7d2b930 docs(journal): 2026-05-18 handoff
- c1ac8dd docs(map): ARCHITECTURE_MAP auto-refresh 2026-05-18
- 37b6de0 feat(circular): C-03 TamimReminderJob — günlük tamim okunmama hatırlatması (09:00 cron)
- 3a28fa0 feat(obligations): C-06 ObligationsController.Edit + Edit view

### Uncommitted Dosya Sayisi

- 9 uncommitted dosya

### Aktif TODO (ilk 15 acik madde)

- - [ ] **C-04** AI Wizard Faz 1 tamamla (8-10h) — WizardStart/Status endpoint + UI + BUGFIX-4 üzerine
- - [ ] **C-07** Plan 27 Faz A eksikleri (4-6h) — ContractExtractionValidator + Stage 3 + 10 field prompt
- - [ ] **C-08** Plan 25.1 Faz 5+6 (5-7h) — Queue migration + admin inline style + smoke + hook enable
- - [ ] **ADR-016** Calendar Unified Event Source (Plan 22'den önce)
- - [ ] **ADR-017** Compliance Scope Sınırı
- - [ ] **VISION update** — AI Generation karar notu (scope tut, modüllerde çıkar)
- - [ ] **D-01** Documents Plan 27 Faz C (20-28h) — versioning + FTS + metadata + permission + audit
- - [ ] **D-02** AI altyapı iyileştirme (8-10h) — audit log + token bütçe + rate limiting + FallbackLlmService cleanup **(SOP öncesi önerilir)**
- - [ ] **D-03** Vision çoklaştırma (4-6h) — Gemini + OpenAI vision fallback
- - [ ] **D-04** Test coverage (6-8h) — Documents/Wizard/multi-firma integration
- - [ ] **D-05** Plan 16.7 Tag Sistemi Faz A (8-10h) — bağımsız (AI önerici 16.5 Faz C bekliyor)
- - [ ] **[Plan 34 · SOP / Prosedür Yönetimi](plans/34-sop-prosedur-yonetimi.md)** ✅ **ONAYLANDI 2026-05-14 (varsayılan kabul)** — Plan 33 Faz 1+2 (+ önerilen D2) bittikten sonra Faz A başlar. Tamim altyapısı %80 reuse + AI Danışman MVP. 38-60 saat (2.5-3 hafta). Bağımlılık yok — bağımsız başlayabilir.
- - [ ] **Comment / Mention sistemi** (1-2 hafta + modül başına 1 gün) — Cross-cutting. Polymorphic `Comments` tablo + `EntityType`/`EntityId` + `@user` mention + `INotificationService` callback. Tamim/Doküman/Sözleşme/OrgChart entegrasyonu. **Bağımlılık:** Plan 31 SMTP caller (Plan 32 bekliyor). Plan yazılacak (Plan 35 adayı).
- - [ ] **Workflow Designer + Onay Akışları** (3-4 hafta + 1 hafta entegrasyon) — `IWorkflow` Core abstraction üzerine designer UI + chain config + assignment + reminder. SOP onayı + sözleşme onayı + satın alma onayı + izin talebi onayı **hepsi aynı engine**. **En yüksek leverage iş** — kalan modüllerin bağımlılığı. Plan yazılacak (Plan 36 adayı).
- - [ ] **Plan 18B · HR Sync** (büyük, ~16-24h) — Hangfire + Mosaik.User ek kolonlar + UserSyncService + Email/TC kararları + UserDataFilter otomatik atama. Haftada 1-2 gün, blok değil. (Kullanıcı 8 May "şimdilik durdur" demişti, vNext sıralaması sonrası yeniden değerlendir.)


---

## Oturum 3

## Ana Konu
C-04 AI Wizard tamamlandı: takvim modülüne compliance şablonları eklendi (migration 49+60), ComplianceController bugları düzeltildi, Türk vergi takvimi seed edildi, WizardExtractionService claude-for-legal-turkish esinlenerek 5 yeni alan + hukuki risk bayrağı tespiti ile genişletildi.

## Tamamlananlar
- ✅ **Takvim-Compliance bütünleşmesi** — Migration 49 (ComplianceTemplate tablo) + 60 (yeni paketler: tasit, emlak, belediye) DB'ye uygulandı. `ReminderSentAt` kolonu da eklendi.
- ✅ **ComplianceController.cs bugfix** (`Mosaik/Controllers/ComplianceController.cs`) — `[HttpGet("Compliance/Preview/{package}")]` route attribute eklendi (convention routing `finans` → `id` bağlıyordu, 400 fix). Quarterly recurrence `_` default'a düşüyordu — düzeltildi. Hafta sonu → Pazartesi kaydırma eklendi.
- ✅ **ContractObligationGenerator.SafeDate** (`Mosaik/Services/Contracts/ContractObligationGenerator.cs`) — hafta sonu → Pazartesi kaydırma eklendi.
- ✅ **Vergi takvimi seed düzeltme** (`Mosaik/Database/49_CreateComplianceTemplates.sql`) — KDV 26→28, Kurumlar Vergisi 25→30 Nisan, Geçici Vergi DayOfMonth=17 (sabit değil, hafta sonu logic halleder).
- ✅ **Migration 60** (`Mosaik/Database/60_SeedComplianceTemplatesV2.sql`) — 4 yeni paket: tasit (MTV 1+2), emlak (Emlak/Arazi 1+2 Taksit), belediye (İlan Reklam), vergi eki (Damga, KDV Tevkifat, GV 2. Taksit), ik eki (SGK Prim Ödemesi). Toplam ~25 şablon, 8 paket.
- ✅ **AI Wizard UI** (`Mosaik/Views/Contracts/Create.cshtml`) — collapsible panel, drag-drop upload, polling, form doldurma + yeni `riskFlags` state + uyarı listesi render.
- ✅ **WizardExtractionResult** (`Mosaik/Services/Ai/WizardExtractionResult.cs`) — 5 yeni alan: `ContractValue`, `GoverningLaw`, `AutoRenewal`, `KvkkInvolved`, `RiskFlags`.
- ✅ **WizardExtractionService** (`Mosaik/Services/Ai/WizardExtractionService.cs`) — `WizardJsonShape` 8 kategori hukuki risk bayrağı tespiti + Türkçe açıklamalarla genişletildi (TBK m.182, sorumluluk sınırı, tek taraflı fesih, otomatik yenileme ihbar, KVKK eki, yurt dışı tahkim, fikri mülkiyet devri, gizlilik süresi). `ParseResult` 5 yeni alan okuyor.
- ✅ **components.css** — `.risk-flag-list` utility eklendi.

## Build / Test Durumu
- Build: BAŞARISIZ (dosya kilidi — pid 17000 server çalışıyor, C# derleme hatası yok)
- Test: çalıştırılmadı
- Smoke test: yapılmadı

## Commit Durumu
- Uncommitted dosya sayısı: 17 (eşik üzerinde — yarın commit-split şart)
- Yeni commit'ler (bu oturum): 44f57c1 docs(map) CLAUDE.md migration range 60_→61
- Commit beklemede: AiController.cs, AiController.Wizard.cs, ComplianceController.cs, WizardExtractionResult.cs, WizardExtractionService.cs, ContractObligationGenerator.cs, DailyReminderJob.cs, Create.cshtml, components.css, migration 49+60, TODO.md

## Yarım Kalan / Yarına Bırakılan İşler
- **dotnet test** — build/test sonucu olmadan C-04 kapatılamaz. Server durdurulup `dotnet build && dotnet test` çalıştırılmalı.
- **commit-split** — 17 uncommitted, 15 eşik aşıldı. Önce commit-split, sonra yeni iş.
- **Create.cshtml ContractValue/GoverningLaw form alanları** — wizard `populate()` bu id'leri set etmeye çalışıyor ama form'da alanlar yok. Eklenebilir veya `set()` no-op olarak bırakılabilir (şimdilik zararsız).
- **C-07** Plan 27 Faz A — ContractExtractionValidator + Stage 3 + 10 field prompt. WizardExtractionService genişletmesi buradaki 10 field ile overlap ediyor, birlikte kapatılabilir.

## Kararlar
- **claude-for-legal-turkish esinlenme yaklaşımı** — plugin'i direkt çağırmak yerine içeriği (12 kırmızı bayrak, KVKK 12-kapı, SaaS alanları) `WizardJsonShape` prompt'una entegre edildi. Kullanıcı kararı: "yok direk kullanma esinlen kullan sisteme entegre et".
- **Türk vergi takvimi referansı** — GİB + EY Türkiye + PwC kaynakları. KDV=28, Muhtasar=26, Geçici Vergi=17 (Q+2 ayı), Kurumlar=30 Nisan, Gelir=31 Mart kanun dayanağı VUK.

## Dikkat Edilmesi Gerekenler
- `ReminderSentAt` SqlException başında yakalandı — migration 57 DB'de yoktu. `ALTER TABLE ContractObligations ADD ReminderSentAt DATETIME2 NULL;` sqlcli ile uygulandı. Migration zinciri kontrol edilmeli.
- `ComplianceTemplate` tablosu yoktu (migration 49 uygulanmamıştı). Uygulandı.
- Geçici Vergi ilk yüklemede Haziran 18 görünüyordu (Quarterly case düşüyordu); düzeltildi, DB de güncellendi.
- Duplicate KDV kaydı oluştu (önce yanlış tarihle import, sonra düzeltildi). Temizlendi.
- Server çalışırken build yapılamıyor — `Mosaik (17000)` exe kilidi. Ctrl+C + restart gerekiyor.

## Yarına Başlangıç Noktası
1. Server durdur → `dotnet build --nologo && dotnet test --nologo` — sıfır hata doğrula.
2. `commit-splitter` agent — 17 dosyayı bucket'lara böl, commit-split yap (15 eşik geçildi).
3. C-07 / WizardExtractionService Stage 3 + 10 field — C-04 ile overlap, birlikte kapat.
4. `ContractValue` / `GoverningLaw` alanlarını Create.cshtml form'una ekle (opsiyonel — wizard zaten doldurur, kullanıcı kaydetmeden önce görebilir).

## Compact Snapshot --- 23:12

### Son 5 Commit

- cf5fd44 fix(calendar): migration 58 SQL syntax — RESTRICT→NO ACTION, Firmas(FirmaId), Status int values
- 7d2b930 docs(journal): 2026-05-18 handoff
- c1ac8dd docs(map): ARCHITECTURE_MAP auto-refresh 2026-05-18
- 37b6de0 feat(circular): C-03 TamimReminderJob — günlük tamim okunmama hatırlatması (09:00 cron)
- 3a28fa0 feat(obligations): C-06 ObligationsController.Edit + Edit view

### Uncommitted Dosya Sayisi

- 14 uncommitted dosya

### Aktif TODO (ilk 15 acik madde)

- - [ ] **C-04** AI Wizard Faz 1 tamamla (8-10h) — WizardStart/Status endpoint + UI + BUGFIX-4 üzerine
- - [ ] **C-07** Plan 27 Faz A eksikleri (4-6h) — ContractExtractionValidator + Stage 3 + 10 field prompt
- - [ ] **C-08** Plan 25.1 Faz 5+6 (5-7h) — Queue migration + admin inline style + smoke + hook enable
- - [ ] **ADR-016** Calendar Unified Event Source (Plan 22'den önce)
- - [ ] **ADR-017** Compliance Scope Sınırı
- - [ ] **VISION update** — AI Generation karar notu (scope tut, modüllerde çıkar)
- - [ ] **D-01** Documents Plan 27 Faz C (20-28h) — versioning + FTS + metadata + permission + audit
- - [ ] **D-02** AI altyapı iyileştirme (8-10h) — audit log + token bütçe + rate limiting + FallbackLlmService cleanup **(SOP öncesi önerilir)**
- - [ ] **D-03** Vision çoklaştırma (4-6h) — Gemini + OpenAI vision fallback
- - [ ] **D-04** Test coverage (6-8h) — Documents/Wizard/multi-firma integration
- - [ ] **D-05** Plan 16.7 Tag Sistemi Faz A (8-10h) — bağımsız (AI önerici 16.5 Faz C bekliyor)
- - [ ] **[Plan 34 · SOP / Prosedür Yönetimi](plans/34-sop-prosedur-yonetimi.md)** ✅ **ONAYLANDI 2026-05-14 (varsayılan kabul)** — Plan 33 Faz 1+2 (+ önerilen D2) bittikten sonra Faz A başlar. Tamim altyapısı %80 reuse + AI Danışman MVP. 38-60 saat (2.5-3 hafta). Bağımlılık yok — bağımsız başlayabilir.
- - [ ] **Comment / Mention sistemi** (1-2 hafta + modül başına 1 gün) — Cross-cutting. Polymorphic `Comments` tablo + `EntityType`/`EntityId` + `@user` mention + `INotificationService` callback. Tamim/Doküman/Sözleşme/OrgChart entegrasyonu. **Bağımlılık:** Plan 31 SMTP caller (Plan 32 bekliyor). Plan yazılacak (Plan 35 adayı).
- - [ ] **Workflow Designer + Onay Akışları** (3-4 hafta + 1 hafta entegrasyon) — `IWorkflow` Core abstraction üzerine designer UI + chain config + assignment + reminder. SOP onayı + sözleşme onayı + satın alma onayı + izin talebi onayı **hepsi aynı engine**. **En yüksek leverage iş** — kalan modüllerin bağımlılığı. Plan yazılacak (Plan 36 adayı).
- - [ ] **Plan 18B · HR Sync** (büyük, ~16-24h) — Hangfire + Mosaik.User ek kolonlar + UserSyncService + Email/TC kararları + UserDataFilter otomatik atama. Haftada 1-2 gün, blok değil. (Kullanıcı 8 May "şimdilik durdur" demişti, vNext sıralaması sonrası yeniden değerlendir.)

