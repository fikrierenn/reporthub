# Oturum Günlüğü — 2026-07-04

> Not: bu oturum 2026-07-03 gündüz başladı, gece yarısını aşıp 07-04'e taştı. Çok uzun tek oturum.

## Ana Konu
İki büyük iş: (1) **Plan 55** BKM org yapısını Mosaik'e omurga olarak import + `/GorevTanimlari/Sema` interaktif chart (bkm `index.html` birebir port, DB-driven, admin sağ-tık düzenleme) — **Faz 1-6 KAPANDI**. (2) **20-modül tamamlanmışlık audit** → **Plan 56** modül-tamamlama programı → **Forms modülü M-A** (şifreleme + submission admin) gerçek İhbar formuyla E2E test.

## Tamamlananlar
- **Plan 55 (arşivde)** — `035f467`/`5471e0d` (Faz 1-2: OrgPositions +HolderName/ZirveMatchKey, MatchIncumbents), `c6f14ed` (Faz 6: sidebar Şema + subnav), `d7e4139` (arşiv). org.json → 57 pozisyon omurga; eski 62 IsActive=0 (backup). `/OrgChart` (dabeng) korundu (kullanıcı kararı B).
- **Grounding (atlanamaz)** `70d9bd1` — `.claude/rules/gorev-org-yapisi.md` (core, paths yok) + CLAUDE.md pointer + bkm `DECISIONS.md` **I** (OrgPositions=bizim tasarım omurga, G supersede).
- **20-modül audit** (7 paralel agent) — **18 TAM / 2 YARIM** (Documents: DocumentPermission ölü kod + versiyon write-only; Forms: G1-5 eksik). Vizyonun "Comment yok / Search stub / Inbox boş" iddiaları **YANLIŞ** (hepsi canlı). Ölü kod: ApprovalService (Workflow), KVKK XlsxImporter orphan.
- **Plan 56** — modül-tamamlama programı. Her önemli modül: kılcal-dalış → `mosaik-<x>-expert` skill → danış → geliştir → 4-agent scan → preview → commit (~8 modül).
- **mosaik-forms-expert skill** — ilk per-modül uzman skill (kılcal dalıştan; tam mimari + 3 gap insertion noktası + auto-trigger).
- **Forms M-A G1 şifreleme** `aaff92f` — DataProtection field-encrypt (IsEncrypted → ValueText şifreli blob). danış(mosaik-security) → scan(security-reviewer) → **3 fix**: H-1 key-persist blocker (ephemeral memory → App_Data/dp-keys, restart-safe), H-2 boş guard, M-1 CHECK constraint (migration 76).
- **Forms M-A G2 submission admin/export/decrypt-gate** `a9f824d` — FormSubmissionQueryService (list/detail/export ClosedXML). Decrypt **yetki-gated** (ihbar-komitesi/admin → çöz, yoksa 🔒 maske; ciphertext client'a gitmez). Export şifreli alanı hep maskeler. danış → scan(security+code, **0 CRIT/0 HIGH**) → M-1(audit "ne oldu")/M-2(export sayı)/pagination-guard/A11y fix.
- **Gerçek İhbar formu E2E** `60a5c48` — 7 alanlı whistleblower formu (Select/Date/TextArea/File/Radio) UI'dan tasarlandı, gerçekçi ihbar submit edildi, DOĞRULANDI: tüm hassas alan DB'de şifreli (plaintext yok), boş alan şifrelenmez (H-2), admin decrypt (yetkili) çalıştı, **key preview-restart'ı atlattı (H-1 canlı kanıt)**. 2 real-world bug fix: (a) survey-core UI İngilizce → TR sözlük override ("Gönder"), (b) Forms `_ViewImports.cshtml` YOK → asp-append-version ÖLÜ → cache-bust çalışmıyordu (deploy sonrası eski asset).

## Build / Test Durumu
- Build: **yeşil** (0 hata; 4 pre-existing uyarı — Forms CS8620/CS8604, SOP NU1903).
- Test: **706 yeşil** (koştu, Plan 55 sonrası).
- Preview/smoke: **E2E doğrulandı** — /Sema 57-düğüm + admin düzenleme; Forms gerçek İhbar formu encrypt→decrypt→export.

## Commit Durumu
- Uncommitted: **3** (kod-dışı: .codex/, .claude/_archive/, + bu journal). Eşik altı.
- Bu oturum ~10 commit (yukarıda). Branch `feature/gorev-tanimlari-org`, hepsi push'lu.

## Yarım Kalan / Yarına
- **Plan 56 M-A Forms kalan:** G3 conditional-logic (FormField.ConditionalLogic ölü alan → survey-core visibleIf + server required-if) · G4 submit-bildirim (SMTP köprü — submit sessiz) · G5 template seed (İhbar formu **id=6 DB'de** → seed migration'a çevir).
- **Sonra:** M-B Documents (permission ölü kod karar + versiyon UI), M-C ölü kod temizliği, M-E belge/vizyon hijyeni.
- **Notlar:** şifreli formda **dosya ekleri şifrelenmiyor** (App_Data düz — whistleblower kanıtı için değerlendir); file "Select" caption survey-core locale key farklı (minör).

## Kararlar
- **Advisor mimarisi:** her önemli modül kendi `mosaik-<x>-expert` skill'i (~8, per-modül skill değil 60). Skill auto-trigger + kılcal bağlam. Plan 56'da.
- **Şifreleme:** ASP.NET DataProtection (AES manuel değil — secret-out-of-source), key App_Data/dp-keys persist. Decrypt yetki-gated.
- **DECISIONS I** (bkm): OrgPositions = bizim tasarım omurga; Zirve maps-in. G supersede.

## Dikkat Edilmesi Gerekenler (SÜREÇ DERSİ — kritik)
- **Kullanıcı sert uyardı:** kod-öncesi **DANIŞ** ve **full-scan'i COMMIT'ten ÖNCE** atlıyordum (işi yapıp disiplini sonra dolduruyordum). G1/G2'de düzeltildi: **danış → kod → scan → fix → commit** sırası. Bir daha atlarsam iş geri-alınıp baştan (ceza = rework).
- **Uzman skill her gap sonrası senkronlanır** (Gap 1'de stale kaldı, doğrulama agent yakaladı).
- **Gerçek form testi > sentetik:** tek-alanlı sentetik test 2 bug'ı (locale, cache-bust) göstermedi; gerçek çok-alanlı İhbar formu gösterdi. Kullanıcı içgörüsü.
- **Preview:** Qwen warmup boot'u yavaşlatır; `sleep` bazen preview process'ini düşürüyor (SIGTERM) — poll ile bekle. HTML-entity encoding (İ→`&#x130;`) decrypt-doğrulama check'lerinde false-negative yapar — ASCII substring kullan.

## Yarına Başlangıç Noktası
1. **ÖNCE oku:** `.claude/rules/gorev-org-yapisi.md` (org işi çıkarsa) + Plan 56 M-A. Forms'a dokununca `mosaik-forms-expert` skill auto-yüklenir (G1-2 kapalı işaretli, G3-5 insertion noktaları hazır).
2. **G3 conditional-logic:** danış(forms-expert + mosaik-js-expert) → FormSchemaBuilder `visibleIf` + FormFieldValidator required-if → scan → preview → commit → **skill senkronla**.
3. Sonra G4 (bildirim) → G5 (İhbar formu id=6 → template seed). Her adım: danış→kod→scan→preview→commit sırası ZORUNLU.
