# 2026-05-07 — Mosaik Foundation (Plan 11)

## Ana konu

ReportPanel/ReportHub → **Mosaik** rebrand + DB reset + UTC seed baseline. vNext "şirket içi portal" vizyonu için modüler brand metaforuyla yeni kimlik. Plan 11 Faz 1+2+5+6+7+8 tamamlandı; Faz 3+4 (Brand parametric + Module switchable infrastructure) Plan 12'ye ertelendi.

## Tamamlananlar

**Plan dosyaları:**
- Plan 10 (DateTime Faz E) yazıldı + onaylanmadı + Plan 11 supersed → archive
- Plan 11 (Mosaik Foundation) yazıldı + onaylandı + 6 commit chain ile uygulandı + closure notu

**L1 Canonical kod rebrand (Faz 2):**
- `reporthub.sln` → `Mosaik.sln`
- `ReportPanel/ReportPanel.csproj` → `Mosaik/Mosaik.csproj`
- `ReportPanel.Tests/` → `Mosaik.Tests/`
- Namespace `ReportPanel.*` → `Mosaik.*` (133 dosyada sed replace)
- `ReportPanelContext` → `MosaikContext`
- csproj `<RootNamespace>Mosaik</RootNamespace>` + `<AssemblyName>Mosaik</AssemblyName>`
- Doc path/namespace replace: CLAUDE.md, TODO.md, .claude/rules/, docs/ADR/, INSTALL.md, README.md, vb.
- CLAUDE.md kimlik bölümü Mosaik vizyonu (L1/L2/L3 katmanlar, vNext modül listesi)
- Bonus: `FilterDefinitionService` `DateTime.Now` → `DateTime.UtcNow` (ADR-006 Faz C eksik kalmış)

**DB reset (Faz 5+6):**
- 14 migration dosyasında `USE [PortalHUB]` → `USE [Mosaik]`
- `01_CreateDatabase.sql` Mosaik adıyla yeniden, FILENAME kaldırıldı (default SQL Server data path)
- `03_SeedData.sql` 6 yerde `GETDATE()` → `GETUTCDATE()` (UTC baseline)
- Yeni `00_DevReset.sql` master script — SQLCMD :r ile 26 migration chain + master cleanup
- SSMS'te master DB'ye bağlı çalıştırıldı: PortalHUB DROP → Mosaik CREATE → migration zinciri replay → seed UTC

**Veri kurtarma (kısmi):**
- Klasördeki `D:\Dev\reporthub\PortalHUB.mdf` SQL Server restart sonrası attach edildi (`PortalHUB_Old`)
- `ReportHUB` DB'si zaten online'dı (eski snapshot, M-09 öncesi yapı)
- DataSources (4 satır): IK / DER / MAIN / PDKS — gerçek ConnString'lerle Mosaik'e aktarıldı (`sqlsrv:` prefix kaldırıldı)
- ReportCatalog (2 satır): Günlük Satış + PDKS Pano — ReportHUB'dan Mosaik'e aktarıldı
- Manuel eklenmiş gerçek IK raporları (PersonelKarsilastirma_Ozet/Detay) kayıp — admin GUI'sinde yazıldığı için git'e/SQL dump'a hiç yansımamış. Acı ders.
- Yeni 2 user oluşturuldu (admin, ik) — PowerShell ile `123456` plaintext PBKDF2 hash üretildi
- 5 Roles + UserRoles junction sync
- ReportAllowedRoles junction sync (CSV'den) — Reports/Index junction tablosu kullanıyor

**Browser smoke (Faz 7):**
- Login admin/123456 ✅
- Reports listesi ✅ (junction sync sonrası 2 rapor admin'e: PDKS + Personel)
- Run sayfası ✅ (`/Reports/Run?reportId=10` query string format)
- V2 builder save fail ❌ (kullanıcı "bulunmadı" hatası, route confusion — Plan 12'ye)

## Build / Test Durumu

- Build: yeşil (0w 0e), output `Mosaik.dll`
- Test: 246/246 yeşil
- Dev server (PID 28848) çalışır durumda

## Commit Durumu

- 6 commit Plan 11 kapsamında: `7588fe4` (taslak), `72002ae` (canonical rebrand), `5fbc2c5` (iç klasör + doc), `70c8429` (migration update), `e54559b` (master script), `418b268` (reset düzeltmeler)
- Plus session içinde: `744f4cc` (dashboard role-based), `c76c87c` (validator coverage), `77525d3` (F-12 closure)

## Kararlar

- **Plan 10 superseded by Plan 11** — dev ortamı + sıfırlanabilir data ile karmaşık veri shift gereksiz, reset baseline daha temiz çözüm.
- **Repo klasörü `D:\Dev\reporthub` korundu** — IDE workspace + git remote bağlamayı kırmamak için. İç klasörler Mosaik/, sadece dış klasör adı tarihsel.
- **Faz 3+4 Plan 12'ye ertelendi** — kullanıcı "parametrik kısmını geç" dedi, scope'u küçültmek + odaklanmak için. Brand/module parametric infrastructure ayrı bir plan'da admin GUI ile birlikte yapılacak.
- **SP'ler Mosaik DB'sine deploy edilmedi** — sp_PdksPano (PDKS linked server) + sp_SatisPano (DerinSISBkm) external DB'lerde duruyor, Mosaik portal DB'sinde yok.
- **MCP write izni açılmadı** — Claude config restart gerekirdi, kullanıcı SSMS'te çalıştırdı. MCP allowlist'inde Mosaik yok (sadece master + PortalHUB), ileride eklenebilir.
- **Plan 14 (install/deploy script) öğretisi**: rapor metadata dump kapsama dahil edilmeli — manuel admin GUI'de yazılan tanımlar git'e yansımıyor, kayıp riski.

## Dikkat Edilmesi Gerekenler

- **Manuel admin GUI eklemeleri SQL dump'a yansımıyor** — ileride Plan 14'te ReportCatalog SQL backup automation gerekli.
- **V2 builder dashboard odaklı** — plain table rapor için karmaşık. Kullanıcı save fail yaşadı. Plan 12'de V1 fallback link veya basit form gerekli.
- **Reports/Run route confusion** — `/Reports/Run/{id}` route attribute eksik (default {id?} parametresi `reportId`'ye match etmiyor). Sadece `?reportId=` query string çalışıyor. Plan 12 fix.
- **DataSource ConnString'leri eski snapshot'tan** — IK/MAIN/MALI local SQL Express'te bağlanamayacak (IKDB/MainDB/MaliDB DB'leri yok). PDKS enterprise prod'a bağlı (192.168.40.201). Çalıştırma test'i sadece PDKS'de geçerli olur.
- **`bkm` schema Mosaik'te yok** — sp_SatisPano deploy edilmedi (zaten external DB'de duruyor).

## Yarına Başlangıç Noktası

1. **Plan 12 yaz** — Admin GUI + Brand+Module parametric infrastructure (Faz 3+4 Plan 11'den taşıyor) + V2 builder save bug fix + plain table form fallback + Reports/Run route fix. ~6-8h.
2. **IK rapor metadata** — admin GUI'den manuel ekleme (PersonelKarsilastirma_Ozet/Detay/Listesi/etc.) **veya** SQL ile direkt INSERT. SP body'leri zaten external IKDB / DerinSISBkm'de duruyor.
3. **MCP allowlist Mosaik ekle** — `claude_desktop_config.json` portalhub MCP env: `ALLOWED_DATABASES=master,PortalHUB,Mosaik` + `ALLOW_WRITE=true` + `MSSQL_DATABASE=Mosaik`. Sonraki seans MCP write açık.
4. **G-09 SP read-only login** (canlı blocker, ~2h).
5. **Plan 14 install script** — vNext öncesi otomasyon (DB CREATE + migration + brand prompt + admin user + ReportCatalog dump backup).
