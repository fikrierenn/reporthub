# 2026-05-07 — PDKS+Satış Pano canlandırma + Filter sistemi planlama

## Ana konu
PDKS Pano + Satış Pano kodlarından "ayağa kaldırma". Yol boyunca dashboard config mimarisini yanlış anlayıp ezdiğim için onarım. Sonra FilterDefinition sistemi aktivasyonu + Plan 14 hazırlık.

---

## Tamamlananlar

### Dashboard config mimarisi (3 commit)

- **`d0a60d2`** — fix(db): named contract + no hardcoded IDs
  - PDKS + Satış Pano DashboardConfigJson'u `result: "kpi"`, `"subeOzet"` named binding'e çevrildi (rsN pattern değil — V2 builder default'u, seed yolu değil)
  - resultContract block restore (migration 26+27 chain'inden ezilmişti)
  - Widget `id` alanları silindi (validator regex strict, builder GUI üretir)
  - Migration 30 doğru pattern'e çekildi (`WHERE DashboardConfigJson IS NULL` + ProcName LIKE suffix match)
  - **Yeni rule:** `.claude/rules/dashboard-config-seed.md` (paths-scoped)
  - **Yeni memory:** `feedback_db_update_where_zorunlu.md`, `feedback_migration_chain_oku.md`, `project_dashboard_config_named_contract.md` + MEMORY.md güncellendi

- **`9aa20b1`** — fix(db): ProcName schema fix
  - PDKS Pano `bkm.sp_PdksPano` → `dbo.sp_PdksPano` (BKM DB)
  - Satış Pano `dbo.sp_SatisPano` → `bkm.sp_SatisPano` (DerinSISBkm DB)
  - Audit log'da "saklı yordam bulunamadı" hatası buradan geliyordu
  - Migration 30'a idempotent ProcName fix bloğu eklendi

- **`7eef591`** — fix(db): PDKS Pano ParamSchema'ya 4 parametre
  - Audit log: "TOP veya OFFSET yan tümcesi geçersiz değer içeriyor"
  - Sebep: ParamSchema sadece `Tarih` içeriyordu, `@FmTop`/`@GecTop` gönderilmiyordu, NULL kullanılınca `TOP(NULL)` hatası
  - Çözüm: ParamSchema'ya FmTop/GecTop/sube_Filtre/bolum_Filtre eklendi (default 10)
  - Kullanıcı SP source'unu paylaştı, default'lar var ama `sys.parameters.has_default_value=false` yanıltıcı (T-SQL prosedür default'larını sistem view'ı düzgün göstermiyor)

### FilterDefinition aktivasyonu

- **`5c3788f`** — feat(db): migration 32 — FilterDefinition aktive + UserDataFilter backfill
  - 3 FilterDefinition: PDKS sube, DER sube, DER urunKategori → `IsActive=1`
  - 6 UserDataFilter: admin × 3, ik × 3, hepsi `*` (tümü)
  - UserDataFilterInjector deny-by-default devrede

### Plan 14 hazırlandı

- `plans/14-filter-production-readiness.md` — Tier 3, 4 faz incremental
- code-explorer subagent ile derinlemesine sistem analizi
- Faz A (audit + bug fix) sabah ilk iş

---

## Build / test durumu

- Build: lokal kilitli (running app port 5197)
- Test: çalıştırılmadı
- Smoke: PDKS Pano + Satış Pano kullanıcıdan onaylandı ("tamam bu da oldu")

---

## Commit durumu

- Uncommitted dosya sayısı: 0 ✓
- Yeni commit'ler: `d0a60d2`, `9aa20b1`, `7eef591`, `5c3788f` + plan dosyası (commit edilmedi, taslak)
- Bekleyen: plan 14 commit'i (sabah onay sonrası)

---

## Yarım kalan / yarına bırakılan işler

- **Plan 14 onay** — sabah kullanıcıya 4 faz yaklaşımı + Faz A'dan başlama kararını onaylat
- **Plan 14 Faz A** — `UserManagementService.SyncDataFiltersAsync` audit log + `BuildEditUserFormAsync` helper + EditUser error recovery (~60 dk)
- **Plan 14 Faz B** — IK sube FilterDefinition.OptionsQuery (BKM_GENEL DB keşif gerek) + ik kullanıcısı kapsam daraltma
- **G-09 (Plan 13)** — read-only SP login, canlı öncesi blocker (kullanıcı "en son" dedi, Plan 14 sonrası)
- **Plan 12 closure** — Brand+Modules smoke test + arşiv (4 oturumdur erteleniyor)
- **Plan 08 Faz 7** — Obsidian Brain wiki-keeper smoke test + arşiv (5 oturumdur erteleniyor)

---

## Kararlar

- **Mimari hatamı 3 yerde kalıcılaştırdım:** rule + 3 memory dosyası + MEMORY.md index. Bir daha aynı hatayı yapmamak için. Kullanıcı "doğru mimariyi öğrenince de unutmaman için gerekeni yapmayı unutma" dedi → uygulandı.
- **ParamSchema fix yolu seçildi (canlı SP güncelleme yerine):** Geri dönülebilir, prod-risk yok. Kalıcı doğru çözüm canlı SP'yi `CREATE OR ALTER` ile lokal source'la eşitlemek (`@FmTop int = 10` default).
- **FilterDefinition Plan 14 olarak ayrıştı** — bugün aktivasyon + backfill yeterli. Production-readiness eksikleri Plan 14'te incremental.

---

## Dikkat edilmesi gerekenler

- **MCP UPDATE WHERE şart!** — Bugün PDKS Pano'nun migration 12+26+27 zincirinden geçmiş contract'lı V2 config'ini WHERE'siz UPDATE ile ezdim. Onarmak 3 commit + 4 dosya değişiklik aldı. Bir daha mevcut metadata kolonu UPDATE etmeden önce önce SELECT, sonra WHERE.
- **Migration zinciri tarama:** Yeni seed yazmadan `Glob Database/*.sql` + Grep tablo adı ile mevcut tüm migration'ları oku. V1→V2 upgrade chain (12+14 → 26 → 27) gibi serileri ortadan ezme.
- **`sys.parameters.has_default_value=false` yanıltıcı:** T-SQL prosedür default'larını sistem view'ı düzgün göstermiyor. Asıl source'a bak.
- **Plan 14 Faz C riski:** `raporGrubu` deny-by-default kontrolü eklenirse mevcut user'ların backfill'i zorunlu, yoksa rapor listesi boşalır.

---

## D:/Dev envanteri — sabah deep scan için

`D:/Dev/` altında ~60 yarım proje var. vNext modüllerle direkt eşleşmesi muhtemel olanlar:

| Klasör | Son değişiklik | Tahmini eşleşme |
|---|---|---|
| **`tamim/`** | Sep 10 2025 | **Tamim/sirküler modülü** — birebir |
| `hrreport/`, `hrrepo/`, `hrreport_electron/`, `hrreport_backup_*/` | Aug-Sep 2025 | **HR portal + onboarding altyapısı** — Plan 16 HR sync için kritik |
| `ebordro/` | Aug 7 2025 | HR/bordro |
| `talep/` | Dec 6 2024 | Form/anket veya onay akışı |
| `meet/`, `toplanti/` | Feb 17/20 | Takvim/toplantı modülü |
| `katalog/` | Apr 21 | Doküman paylaşımı |
| `esign/` | Aug 7 2025 | Onay akışı / imza |
| `butce/`, `cashflow/`, `tahmin/`, `tahminleme/` | varied | KPI/OKR / forecast |
| `icdenetim/` | Mar 29 | SOP / iç denetim |
| `RiskAnaliz/` | Mar 29 | Dashboard / risk |
| `pivotera/`, `pivoterahub/`, `pivoterasite/` | varied | Rapor pano (önceki iterasyon olabilir) |
| `spendme*` (5 versiyon!) | varied | Finans tracker |
| `enflasyonsepet/` | Sep 26 2025 | Finans/raporlama |
| `CrewOps/`, `Operax/`, `BkmArgus/`, `yonet/` | varied | Operasyon yönetimi (içerik bilinmiyor) |
| `DikkatIQ/`, `FLYX/`, `tower/`, `FrameOS/`, `HommLink/` | varied | İçerik bilinmiyor |

**Diğer projeler (Mosaik dışı kategori — vNext'le ilgisiz):**
- `claude-context-template/`, `brain/`, `AtlasOPS/` — meta/dev tooling, zaten kullanılıyor
- `chat-app/`, `eticaret-panel/`, `etsy/`, `aracsatis/` — ayrı domain'ler
- `arsatarlacomtr/`, `olimposce/`, `homm/`, `hommlinklead/`, `katalog/` — site/landing
- `gemini/`, `ai/`, `data/`, `TrendScopeAI/` — AI deneyleri
- `qtrace/`, `mmm/`, `sınav/`, `bel/`, `cetingursel*/`, `fikrieren/` — bilinmiyor
- `sqlserver-mcp-server/`, `sqlcli/`, `NodeExpressPgDemo/`, `tsoft-rest-api-test-app-main/` — tooling/test
- `namaz/`, `fifo/`, `hal/` — bilinmiyor

### IQ-suffix + sln tabanlı kurumsal projeler (KULLANICI VURGUSU — birinci öncelik)

Hızlı peek (gece quick check):

| Proje | Stack / Açıklama | Mosaik için değer |
|---|---|---|
| **`yonet/` (YonetIQ)** | .NET 10 Blazor Server kurumsal yönetim platformu | **Mosaik vizyonuyla birebir** — entity model + view'lar + auth doğrudan port adayı |
| **`tower/` (Control Tower)** | Next.js, AI-native KPI + Görev + Not Yönetim Paneli | **KPI/OKR modülü için doğrudan referans** — UI + entity şema |
| **`BkmArgus/`** | .NET sln, Audit & Risk Intelligence (BKMKitap iç denetim + risk + AI) | **İç denetim/Risk modülü iskelet referansı** |
| `DikkatIQ/` | .NET sln + .mdf, içerik bilinmiyor | İçerik analiz et |
| `FLYX/` | FSL — low-code platform (FSL DSL ile is uygulama tanımı) | Builder mantığı için referans, port değil |
| `Operax/` | .NET sln + mdf, içerik bilinmiyor | İçerik analiz et |
| `CrewOps/` | AI takım orkestratörü (sektörel) | Mosaik dışı (orchestrator) — referans değer düşük |
| `FrameOS/` | AI Video Director | Mosaik dışı (video) |

**Sabah deep scan'de kritik:**
- **YonetIQ** ilk önce — Mosaik'in büyük abisi olabilir. Modül mimarisi nasıl, hangi entity'ler var, hangi UI pattern'ler kullanılıyor, ne kadarı reusable?
- **Control Tower** ikinci — KPI/OKR için doğrudan UI + DB şema port adayı
- **BkmArgus** üçüncü — iç denetim modülü iskeleti
- DikkatIQ + Operax içerik kontrolü (sln var, ne yapıyor?)

**Sabah scan stratejisi (subagent paralel):**
1. `tamim/` README + entity model + UI çıkar — Mosaik vNext Tamim modülü baştan yazma yerine port
2. `hrreport*` + `hrrepo/` HR sync + user onboarding mantığı çıkar — Plan 16 hızlanır
3. `meet/` + `toplanti/` → Calendar modülü iskeleti
4. `talep/` → Form/Approval iskeleti
5. `katalog/` → Doküman modülü iskeleti
6. `esign/` → Onay akışı
7. `butce/` + `cashflow/` + `tahmin/` → KPI modülü

Her klasör için:
- README/package.json/csproj/EntityModels oku
- "Bu Mosaik'te ne kadar reuse edilebilir?" notu
- Eğer monolitik prototip → entity şema + UI ekran tasarımı çıkar (referans olarak)
- Eğer modular → service/controller direkt port

**Zaman tahmini:** 7 modül × 15 dk = ~2 saat (subagent paralel). Sonuç: vNext Plan 17+ için temel iskelet referansları.

---

## Yarına başlangıç noktası

1. **Plan 14 onay** — kullanıcıya 4 faz incremental yaklaşımını sun, Faz A onayı al
2. **Faz A başla:**
   - `Mosaik/Services/UserManagementService.cs:204-222` SyncDataFiltersAsync audit log
   - `Mosaik/Controllers/AdminController.Users.cs:156-194` BuildEditUserFormAsync helper
   - `Mosaik/Controllers/AdminController.Users.cs:103-127` EditUser POST error recovery güncelle
3. **Smoke test:** `/Admin/EditUser/7` → filter değiştir → audit log'da `user_data_filter_sync` event görünmeli
4. **Commit:** `feat(admin): UserDataFilter sync audit log + EditUser error recovery (plan: 14)`

Faz B IK sube için BKM_GENEL DB keşfi:
```sql
SELECT TOP 30 OBJECT_SCHEMA_NAME(object_id)+'.'+name FROM sys.tables WHERE name LIKE '%Sube%' OR name LIKE '%Magaza%'
```

**SABAH ÖNCELİK SIRASI:**
1. Plan 14 + Plan 15 notları + D:/Dev envanteri kullanıcıyla konuş (5 dk)
2. **D:/Dev derin tarama** (subagent paralel, ~2 saat) — vNext modül adayları (tamim, hr*, meet/toplanti, talep, katalog, esign, butce/cashflow)
3. Tarama sonucu Plan 17+ vNext modül planları formalize
4. Plan 14 Faz A ya hemen ya tarama sonrası
5. Karar: önce Plan 14 (audit blocker, ~60dk) mı yoksa önce vNext keşif mi?

---

## Oturum 2 — D:/Dev derin keşif (sabah)

**Ana konu:** 8 paralel subagent ile D:/Dev altındaki kurumsal + vNext modül adayı klasörlerin port stratejisi taraması.

### Subagent batch (8 paralel Explore + 1 ek DikkatIQ)

1. **YonetIQ** (`yonet/`) — .NET 10 Blazor Server kurumsal yönetim platformu, 16 entity + 40+ AI servisi. **Reuse 3.5/5.** Karar: **Referans al, port etme** (Blazor↔MVC). Alınacak parça: BaseEntity, ServiceResult<T>, Lookup, SystemLog.
2. **Tower** (`tower/`) — Next.js + raw MSSQL, KPI/Dashboard/Tasks/Notes. **Reuse 4/5.** Karar: **Entity port + UI yeniden çiz.** SQL şeması direkt port, KPIService.ts → C# KpiService.
3. **BkmArgus** (`BkmArgus/`) — .NET 10 Razor Pages + Dapper + 4-katmanlı AI. **Reuse 3.5/5.** Karar: **Audit + Risk + DOF entity'lerini port et (5/5)**, UI/auth skip.
4. **Tamim** (`tamim/`) — Next.js 15 + Prisma. **Reuse 5/5 — birebir port.** Eksik: ReadLog tablosu eklenir. **8-12 saat solo dev.**
5. **HR sync** (`hrreport/`+`hrrepo/`+`hrreport_electron/`) — En olgun: hrreport (Node Express). hrrepo'da `GetCurrentWindowsUserAsync()` AD entegrasyonu var. **Plan 16'ya entegre.**
6. **Meet+Toplanti** (`meet/`+`toplanti/`) — Meet'te 600 LoC Türkçe NLP regex motoru var (action/decision/blocker çıkarımı, in-memory mock). Toplanti boş.
7. **Form/Doc/Approval** (`talep/`+`katalog/`+`esign/`) — Katalog 4/5 (full port adayı, AI extraction), Esign 2/5 (UI sadece), Talep 0/5 (boş).
8. **KPI/Finans** (`butce/`+`cashflow/`+`tahmin/`+`tahminleme/`) — Cashflow olgun (.NET 10 Blazor + EF, 13 tablo + 6 SP, multi-tenant). Bütçe/tahmin Python. Karar: **modül değil dashboard widget tipi**.
9. **DikkatIQ** (`DikkatIQ/`) — kullanıcı sordu, ek keşif. ASP.NET Core 10 Razor Pages + EF + Hangfire + **3-layer LLM fallback** (Ollama→Gemini→Claude) + PdfPig + OCRmyPDF Türkçe. Domain: AI-powered sözleşme/yükümlülük yönetimi. **İki ayrı kullanım:** (a) AI layer Plan 16.5 `Mosaik.Core.AI` shared kit (LLM fallback + PDF + suggestion pattern, ~7h port) — tüm vNext modüllere katma değer. (b) Plan 25 Sözleşme/Yükümlülük modülü (yeni, en sonda, ~20-24h).

### Ana çıktı

**`plans/16-vnext-module-roadmap.md`** yazıldı. Tier 3 plan, 10 bölüm:

- Sentez (8 subagent bulguları)
- Sıralama tablosu (8 modül × reuse + effort + plan no)
- 5 ortak pattern bulgusu (BaseEntity, ServiceResult<T>, IWorkflow, AttachmentService, ReadLog<T>)
- Plan 16.5 shared kit önerisi
- 5-lens alternatif değerlendirme
- Riskler + done criteria

### Sıralama önerisi

| # | Modül | Plan | Effort |
|---|---|---|---|
| 17 | Tamim | yeni | 8-12h |
| 18 | HR Sync | **Plan 16 (mevcut)** | 12-16h |
| 19 | Doküman | yeni | 16-20h |
| 20 | KPI Widget | yeni | 12-16h |
| 21 | Audit/Risk/DOF | yeni (büyük) | 24-32h |
| 22 | Calendar | yeni | 16-20h |
| 23 | Approval/E-imza | yeni | 20-24h |
| 24 | Form Builder | yeni | 24-32h |

### Build / test

Çalıştırılmadı (sadece keşif + dokümantasyon).

### Commit durumu

Uncommitted: 1 dosya (`plans/16-vnext-module-roadmap.md` taslak — onay bekliyor).

### Yarına başlangıç noktası

1. **Kullanıcıya 4 karar noktası sun:**
   - Sıralama onay?
   - Plan 16.5 shared kit ayrı plan mı yoksa Plan 17 içinde mi?
   - Plan 14 Faz A bu modüllerden önce mi sonra mı?
   - Sürpriz öncelik var mı (BkmArgus risk, KPI önce gelsin mi)?
2. Onay sonrası Plan 17 (Tamim) yazılır.
3. Memory'e `project_vnext_module_roadmap.md` eklenir.

### Dikkat edilmesi gerekenler

- **Auth uyumsuzluğu kritik:** Tüm kaynak klasörlerin auth'ı farklı (NextAuth, custom HMACSHA256, BCrypt, Identity). Mosaik Identity'ye standartlaşma her modülde maliyet. Plan 16.5 `IUserContext` interface'i bu sancıyı ortak kılar.
- **DB connection bağımlılığı:** BkmArgus DerinSISBkm bağımlı, Cashflow multi-tenant. Mosaik tek DB. Plan 12 datasource sistemi mevcut, modül-bazlı `DbConnection` registry kullanılabilir.
- **Scope creep tehlikesi:** 8 modül = 6+ ay. Mevcut filter/audit/G-09 işleri unutulmamalı.

---

## Oturum 3 — DikkatIQ + YonetIQ derin keşif + Plan 14 Faz A implementasyonu

**Ana konu:** DikkatIQ ek derin keşif (kullanıcı sordu) → AI Core kritik bulgu. YonetIQ derin keşif (yüzeysel kalmıştı) → 6-katman AI Core + MetricEngine + domain pattern hazinesi. Plan 14 Faz A implementasyon (10 öneri ile genişletildi). Commit-split.

### Tamamlananlar

**Keşif:**
- **DikkatIQ keşfi** (`D:/Dev/DikkatIQ/`) — AI Core hazinesi (3-layer LLM fallback Ollama→Gemini→Claude + PdfPig+OCRmyPDF Türkçe + 2-stage prompts + suggestion-approval pattern). 13 entity. Plan 16.5'a AI layer port + Plan 25 Sözleşme/Yükümlülük modülü eklendi.
- **YonetIQ derin keşif** (code-explorer subagent) — 6 AI katmanı: AiProviderService (multi-provider fallback + SSE), SkillRegistry (DB override), PromptEngine (.md hot-reload + global prefix), AiOrchestrationService (routing + self-correction), **MetricEngineService** (deterministik NL→SQL, Plan 26 adayı), LearningSignalService (Levenshtein + signal weights + decay). Domain hazineleri: ServiceResult<T>, Lookup, ApprovalRequest+Step, TaskItem.SourceEntity, ConversationContextService, MetaSkill+SkillStudio.

**Plan dokümantasyonu:**
- **`plans/16-vnext-module-roadmap.md`** — yeni Tier 3 plan, 9 vNext modül (Plan 17→26 + Plan 25 Sözleşme + Plan 26 NL-to-SQL/MetricEngine)
- **`plans/16.5-shared-kit.md`** — yeni Mosaik.Core shared kit (4 faz, ~18h, hibrit AI Core: YonetIQ üst + DikkatIQ üstüne, 9 modül paylaşımlı)
- **Plan 14 revize 1** — 10 öneri Faz A'ya entegre, Faz C IFilterScope→Plan 16.5 IUserDataScope'a taşındı, Faz D dropped (Plan 18 sonrasına)

**Plan 14 Faz A implementasyonu** (commit `27714c5`):
- `Mosaik/Services/UserManagementService.cs:204-251` — SyncDataFiltersAsync diff audit (FilterTuple record, oldSet/newSet karşılaştırması, no-change short-circuit, EventType="user_data_filter_sync")
- `Mosaik/Controllers/AdminController.Users.cs` — BuildCreateUserFormAsync → BuildAdminUserFormAsync (4 çağrı noktası: CreateUser GET/POST + EditUser GET/POST), ReadPostedFilters helper, ModelState.IsValid kontrolü
- `Mosaik.Tests/UserManagementServiceTests.cs` — yeni test dosyası, 4 senaryo (diff event, no-change skip, DataSourceKey değişikliği, bos value ignore). EF InMemory + DefaultHttpContext pattern
- 246 → 250 test yeşil, 0 build warning

### Build / test
- Build: ✓ 0 hata, 0 warning
- Test: 250/250 yeşil

### Commit'ler (6)
- `27714c5` feat(admin) — UserDataFilter diff audit + ortak helper + ModelState
- `2d4801e` docs(plan) — Plan 14 revize + Plan 16 + Plan 16.5
- `cb74471` docs(meta) — TODO + journal
- `e39c7de` fix(views) — PortalHUB → Mosaik (DataSource form + INSTALL)
- `0c28813` docs(rules) — subagent ana prensip + Mosaik DB adı
- `536b3ec` docs(plan) — Plan 14 Faz B BKM heterojen + TODO 2026-05-07 senkron

### Memory eklendi/güncellendi (5)
- `project_vnext_module_roadmap.md` — Plan 16 sıralaması
- `project_dikkatiq_ai_layer.md` — AI layer port detayı
- `project_yonetiq_deep_findings.md` — 6 katman + domain pattern
- `project_mosaik_core_shared_kit.md` — Plan 16.5 özet
- `project_bkm_sube_heterogen.md` — BKM şube heterojenliği (FilterDefinition.DataSourceKey zorunlu, IUserDataScope imzası)
- `feedback_subagent_skill_ana_prensip.md` — Subagent + skill default, hatırlatma bekleme

### Kararlar
- **Plan 16.5 = hibrit AI Core:** YonetIQ üst (provider+prompt+conversation+learning), DikkatIQ üstüne (PDF + suggestion-approval). DikkatIQ FallbackLlmService → YonetIQ AiProviderService ile değiştirilir.
- **Plan 26 MetricEngine ayrı plan** (NL-to-SQL kavramsal port, BKM hardcoded refactor şart).
- **Plan 14 Faz D dropped** — bulk assign UI 2-3 user için overkill, Plan 18 sonrasına.
- **Subagent + skill ana çalışma prensibi** — kullanıcı 2026-05-07'de açıkça söyledi, CLAUDE.md + session-protocol.md + feedback memory'e işlendi. Hatırlatma beklemeden proaktif tetikle.

### Dikkat
- **MCP allowlist:** Mosaik DB allowlist DIŞINDA (master, DerinSIS*, BKMDATA, EncoreMerkez, BKM). Uygulama içi DB sorgusu için MCP yerine SSMS/sqlcmd. Tanı sorgusu (raporGrubu coverage) bu yüzden manuel.
- **BKM şube heterojenliği** — Plan 14 Faz B + Plan 18 + Plan 16.5 hepsini etkiler. PDKS/IK/DER şube ID'leri farklı, FilterDefinition.DataSourceKey zorunlu, IUserDataScope imzası `(userId, dataSourceKey, value)`.
- **YonetIQ port düzeltmeleri:** `BaseService.cs:35` ex.Message expose → security #7 ihlali, port'ta düzelt. `AiProviderService` HttpClient Dispose eksik → IHttpClientFactory pattern. Dapper → EF/ADO.NET her port'ta.

---

## Oturum 4 — Agent token yönetimi + skill ekosistem genişletme

**Ana konu:** Agent model frontmatter optimizasyonu (token+performans) + eksik kritik skill'ler. Kullanıcı: "Agent tarafında token yönetimi yap, skill yoksa araştır bul implemente et."

### Tamamlananlar

**Subagent envanter (Explore + claude-code-guide paralel):**
- 9 agent durumu çıkarıldı, model frontmatter best-practice rehberi alındı
- Anthropic resmi: `model:` field haiku/sonnet/opus, override: env CLAUDE_CODE_SUBAGENT_MODEL veya frontmatter (Agent tool param yok)

**Agent model atamaları:**
| Agent | Eski | Yeni | Gerekçe |
|---|---|---|---|
| code-architect | sonnet | sonnet | denge optimal |
| code-explorer | sonnet | sonnet | derin keşif |
| code-reviewer | opus | opus | false-positive maliyeti yüksek |
| code-simplifier | **opus** | **sonnet** | mechanical refactor opus overkill, ~5x cost saving |
| comment-analyzer | inherit | sonnet | subtlety, medium-high precision |
| commit-splitter | yok | **haiku** | mechanical, hız kritik |
| pr-test-analyzer | inherit | sonnet | edge case detection |
| silent-failure-hunter | inherit | **opus** | error handling güvenlik kritik |
| type-design-analyzer | inherit | sonnet | niche domain |

**3 yeni domain skill yazıldı:**
- **bkm-db-explorer** — BKM kurumsal DB keşif standardı. Şube heterojenliği kuralı, MCP allowlist, INFORMATION_SCHEMA > describe_table best practice, OptionsQuery taslak şablonu, yaygın yanılgılar listesi.
- **vnext-entity-port** — Plan 17-25 modül implementasyon şablonu. 8 standart adım (Entity→EF migration→Controller→ViewModel→Views→Module reg→Test→Smoke). 9 modül için kaynak + özel dikkat tablosu.
- **sql-migration-writer** — 5 idempotent migration template (yeni tablo, kolon ekleme, data UPDATE WHERE zorunlu, SP CREATE OR ALTER, FilterDefinition seed). Yedek almadan silme YASAK + migration zinciri oku kuralları entegre.

**Commit:**
- `f6971b3` chore(agents+skills) — 9 dosya, +462 satır (3 yeni skill ~440 satır + 6 agent frontmatter düzeltme)

### Build / test
- Test çalıştırılmadı (sadece skill/agent dosyaları eklendi, kod değişmedi)
- Build doğrulandı önceki commit'te ✓

### Yarım kalan / yarına bırakılanlar

1. **Plan 14 Faz B** — IK sube OptionsQuery (`bkm-db-explorer` skill ile BKM_GENEL keşfi yap, migration 33 yaz). Yeni skill kullanım fırsatı.
2. **Plan 14 Faz C** — Plan 16.5 Faz A+B sonrası, IUserDataScope ile reportAccess deny-by-default + migration 34 backfill (Faz A tanı sorgusu sonucuna göre).
3. **Plan 16.5 Faz A+B** — Mosaik.Core domain primitives (BaseEntity, ServiceResult, ApprovalRequest) + Lookup + IUserDataScope. ~7h.
4. **Tanı sorgusu** — uygulama çalışırken SSMS/sqlcmd ile:
   ```sql
   SELECT COUNT(DISTINCT UserId) FROM UserDataFilters WHERE FilterKey='raporGrubu';
   SELECT COUNT(*) FROM Users WHERE IsActive=1;
   ```
5. **Plan 14 Faz A smoke test** — uygulama çalışan oturum gerekli (audit diff JSON UI doğrulama).
6. **Plan 12 closure** + **Plan 08 Faz 7** (5 oturumdur erteleniyor).

### Kararlar

- **Subagent + skill = ana çalışma prensibi** — `CLAUDE.md` + `.claude/rules/session-protocol.md` + `memory/feedback_subagent_skill_ana_prensip.md` üçüne işlendi.
- **Token optimizasyonu uygulandı** — code-simplifier opus→sonnet ile en büyük tasarruf, silent-failure-hunter opus ile kritik koruma. Ortalama %25-30 maliyet azalma beklenir (claude-code-guide tahminin).
- **Yeni skill'ler "tetikle, kullan" hazır** — bkm-db-explorer Plan 14 Faz B'de hemen kullanılacak.

### Dikkat

- **`sql-migration-writer` skill registry'de görünmedi henüz** — yeni eklendi, sonraki oturumda skill list'e gelecek (system-reminder cache timing). bkm-db-explorer + vnext-entity-port görünüyor.
- **Agent model override yolu:** Frontmatter zorunlu, runtime param yok. `CLAUDE_CODE_SUBAGENT_MODEL` env var session-wide override.

### Yarına başlangıç noktası

1. **`bash .claude/hooks/session-start.sh`** + son 2 journal oku (zorunlu ritüel)
2. **Plan 14 Faz B'ye başla** — `bkm-db-explorer` skill çağır, BKM_GENEL DB'sinde IK şube/personel master tablolarını keşfet (subagent paralel + skill kullanım örneği)
3. **Tanı sorgusu** çalıştır (SSMS/sqlcmd ile, Mosaik DB)
4. **Plan 14 Faz A smoke test** (uygulama açıkken)
5. Faz B sonrası: **Plan 16.5 Faz A** (Mosaik.Core domain primitives)

---

## Bu oturumun özet metriklerı

- Toplam commit: **7** (Faz A 3 + view düzeltme 1 + rules 1 + Plan 14 senkron 1 + agent/skill 1)
- Toplam test: 246 → **250** (4 yeni unit test, hepsi yeşil)
- Yeni plan dosyası: **2** (Plan 16 + Plan 16.5)
- Plan revize: **1** (Plan 14)
- Yeni memory: **5** (vNext roadmap + DikkatIQ + YonetIQ + 16.5 shared kit + BKM heterojen)
- Yeni feedback memory: **1** (subagent ana prensip)
- Yeni skill: **3** (bkm-db-explorer + vnext-entity-port + sql-migration-writer)
- Agent model frontmatter: **6 güncelleme** (1 downgrade + 1 upgrade + 4 inherit→spesifik)
- Subagent çağrısı: **~13** (ilk 8 paralel + DikkatIQ + YonetIQ + Operax + FrameOS + FLYX + envanter + claude-code-guide)
- D:/Dev keşif klasörü: **11** (yonet, tower, BkmArgus, tamim, hr*, meet/toplanti, talep+katalog+esign, butce+cashflow+, DikkatIQ, Operax, FrameOS, FLYX)
- vNext modül roadmap: **9 modül** sıralandı (Plan 17→26)
