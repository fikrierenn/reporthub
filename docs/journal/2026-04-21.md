# Oturum Günlüğü — 21 Nisan 2026

_Kaynak: CLAUDE.md §5'ten taşındı. Tarihli, referans niteliğinde._

## Tamamlananlar

### 1. Dashboard builder UX fix'leri
- "Düzenle" bug'ı: `_dbTypeChange` editIndex'i reset ediyordu → korunuyor artık.
- Smooth scroll + ring flash feedback + mavi arkaplan + "Düzenleniyor: {title}" banner + iptal linki.
- Drag-drop component sıralama (HTML5 native, grip handle, dragover highlight, editIndex korumalı).
- **Yan-yana layout:** liste sol (5/12), form sağ (7/12, `lg:sticky lg:top-4`). Form içi grid'ler responsive (sm:2, xl:4/5 sütun).

### 2. CreateUser → veri filtresi entegrasyonu
- CreateUser.cshtml'e veri filtresi bloğu eklendi.
- AdminController POST'ta `SyncUserDataFilters` çağrısı eklendi.
- `BuildCreateUserFormAsync` helper'ı ile form hata dönüşlerinde filtre korunuyor.

### 3. Dashboard P0 güvenlik fix'leri (hepsi tamam)
- `ReportsController.cs:238-267` → `JsonSerializer.Deserialize<DashboardConfig>` try/catch, `JsonException` yakalanıyor, `dashboard_config_invalid` audit log + UYARI metni + fallback `new DashboardConfig()`.
- `DashboardRenderer.cs:58-69` → RS'ler artık tek `window.__RS = [...]` array, 3 yerde `eval('RS_'+rs)` → `window.__RS[rs] ?? []`.
- `DashboardRenderer.cs:138-163` modal XSS fix → `innerHTML` + string concat yerine `createElement` + `textContent`.
- `DashboardRenderer.cs:223-268` table XSS fix → `onclick="showDetail(JSON)"` inline attribute yerine `addEventListener` + closure.
- Ek: `</script>` break-out case-insensitive regex ile, `<!--` HTML comment pattern de kaçırıldı.

### 4. SP kolon auto-detect backend (yeni, `AdminController.cs`)
- `[HttpGet] /Admin/SpList?dataSourceKey=xxx` → `sys.procedures`'tan SP adlarını JSON döndürür.
- `[HttpGet] /Admin/SpPreview?dataSourceKey=xxx&procName=yyy&maxRows=10` → SP'yi bulunabildiğinde parametreleri NULL ile, yoksa parametresiz çalıştırır; her result set için `{ index, columns, rows, rowCount, truncated }` döner. Hata varsa `{ success: false, error }`.

### 5. EditReport'a SP önizleme UI
- ProcName input'una `list="spList"` + `<datalist>` + "SP Önizle" mor butonu + `#spPreviewPanel`.
- `admin-report-form.js` içinde: data source değişince SP listesi datalist'e yükleniyor; önizle butonu result set'leri kart olarak gösteriyor (her biri: `RS N` rozet, kolon chip'leri, ilk N satır tablo).
- `window.__spPreview` global ve `document.dispatchEvent(new CustomEvent('spPreviewReady'))` event yayını ile builder tarafına veri aktarımı hazır.

### 6. Dokümantasyon organizasyonu
- CLAUDE.md ve TODO.md organize edildi: proje bazlı memory + 4 ana plan (DASHBOARD, USER MGMT, MIMARI TUTARSIZLIKLAR + kullanıcı direktifleri).

### 7. Kaspersky EBADF teşhisi
- BKM AV real-time scanning `.tmp-*` dosyalarını kilitliyor.
- Kalıcı çözüm: IT'den exclusion istemek.
- Teşhis komutu: `Get-CimInstance -Namespace "root\SecurityCenter2" -ClassName AntiVirusProduct`.

### 8. Security review
- `main` branch uncommitted değişiklikler security-review skill'i ile denetlendi.
- Yüksek güvenilirlikli açık yok (dashboard `<iframe sandbox="allow-scripts">` XSS'i büyük ölçüde izole ediyor).

## Build durumu
- `dotnet build` yeşil (0 uyarı, 0 hata, .NET 10.0).
- `admin-report-form.js` ve `dashboard-builder.js` syntax check OK.
- **Commit edilmedi** (kullanıcı açıkça istemedi). 32+ yeni dosyalık uncommitted mass, TODO.md'deki 7 fazlı plana göre parçalanacak.

## Yarıda / Yarına Bırakılan İşler

- **Dashboard builder kolon auto-detect JS tarafı** (yarım): Backend `/Admin/SpPreview` hazır, `window.__spPreview` global'i ve `spPreviewReady` event'i yayılıyor. **Eksik:** `dashboard-builder.js` tarafında event'i dinleyip ilgili result set'in kolonlarını "Kolon adı" input'larında `<datalist>` olarak sunmak. Ayrıca "Result Set" input'u preview yapıldıktan sonra max değerini/seçenekleri güncellemeli.
- **Dashboard UX — Canlı önizleme iframe** (hiç başlamadı): builder'ın sağ sütunu altında gerçek DashboardRenderer çıktısını iframe'de göstermek. Her değişiklikte throttle ile tazele. Test data için `window.__spPreview.resultSets[N].rows` kullanılabilir.
- **User P1** — Admin listesi arama + filtre + son giriş (hiç başlamadı).
- **SP mimarisi tartışması** — "her şeyi SP yapmaktan vazgeçmek mi" sorusu. TODO.md'de iki karar yazıldı: (1) Hibrit kal — SP rapor için, EF Core metadata için. (2) Monolitik sp_PdksPano'yu inline TVF'lere parçala (reuse + test).

## Düzeltme Notları (memory hatası)
- Explore agent mimari raporunda "net8.0 + net10.0 çift target" dedi → **yanlış**, proje sadece `net10.0`. `.csproj` kontrol edildi.
- `View/Auth/AGENT.md` "Razor Pages + Dapper" manifestosu → proje ile çelişiyor, yanıltıcı. TODO.md'de silinme/güncelleme planlı.

## Gece Son Çıktılar (uyumadan önce)
- **`docs/CLAUDE_TOOLING_PROPOSAL.md`** — 3 paralel agent ile derlenmiş detaylı rapor (10 mimari öneri + 7 skill + 7 subagent + 3 hook + 3 sprint planı).
