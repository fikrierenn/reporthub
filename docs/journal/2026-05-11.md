# Oturum Günlüğü — 2026-05-11

## Ana Konu
Plan 25.1 Faz 2-6 tamamlandı: AiSettings.ApiKey at-rest encryption, ContractEditViewModel mass-assignment koruması, ContractObligationGenerator MaxIterations dinamik, AiSettingsTest restore try/finally + DataProtection key ring not'u. Yanıt özlülüğü kuralı CLAUDE.md'ye eklendi.

## Tamamlananlar

### Plan 25.1 Faz 2 — AiSettings.ApiKey at-rest encryption (commit `dccf6b1` önceki oturum, bugün doğrulama)
- `Mosaik/Services/AiSettingsProvider.cs` — IDataProtector ile decrypt, CryptographicException guard, SemaphoreSlim cache lock
- `Mosaik/Controllers/AdminController.AiSettings.cs:48` — POST'ta `[FromServices] IDataProtectionProvider dpProvider` + `Protect()` çağrısı
- `Mosaik/Program.cs:41` — `builder.Services.AddDataProtection()`
- `Mosaik/Database/45_NullAiSettingsApiKeys.sql` — mevcut plaintext key'leri NULL'a sıfırla

### Plan 25.1 Faz 3 — ContractEditViewModel + Edit POST hardening (commit `8d63be9`)
- `Mosaik/ViewModels/ContractEditViewModel.cs` (yeni) — sadece Title/Counterparty/Category/Status/StartDate/EndDate/Notes alanları
- `Mosaik/Controllers/ContractsController.cs` Edit GET/POST — model.Id route'tan, audit log (`contract_update`)
- `Mosaik/Views/Contracts/Edit.cshtml` — `@model Mosaik.ViewModels.ContractEditViewModel`

### Plan 25.1 Faz 4 — ContractObligationGenerator MaxIterations dinamik (commit `f55c09f`)
- `Mosaik/Services/Contracts/ContractObligationGenerator.cs` — `GetMaxIterations(type, intervalValue)` metodu (Yearly:10 / Quarterly:40 / Monthly:120 / Custom: 120/intervalValue+10)
- Hata mesajı kullanıcı dostu: "Yükümlülük 'X' için tarih aralığı çok uzun. Maksimum: 10 yıl (40 dönem). Bitiş tarihini kısaltın."

### Plan 25.1 Faz 5 — Wizard queue migration (NOOP, önceden implement edilmiş)
- `AiPipelineQueue` (Channel<int> bounded 100) + `AiExtractionWorker` (BackgroundService) zaten Faz 1'de yapılmış
- `Program.cs:67-69` registrations doğrulandı (singleton + hosted service)

### Plan 25.1 Faz 6 — code-reviewer audit fix'leri (commit `a56fae9`)
- **Kritik 1 (95/100):** `AdminController.AiSettings.cs` `AiSettingsTest` action — diğer profilleri toggle disable edip AI çağrısı yapıyordu, exception fırlatırsa restore satırı çalışmıyordu → `try/finally` bloğuna alındı
- **Kritik 2 (80/100):** `Program.cs` DataProtection comment netleştirildi (dev memory key ring + restart sonrası NULL sıfırlama notu)
- code-reviewer'ın tespit ettiği orta-risk maddeler ertelendi: ContractObligationGenerator Custom formülü (intent/comment uyumsuzluğu), Edit POST `model.Id != id` BadRequest (FirmaId scope check zaten var)

### Pre-commit hook Windows fix
- `.git/hooks/pre-commit` — `bash` çağrısı WSL gerektiriyordu → `sh` ile değiştirildi (Git for Windows dahili)
- Doğrulama: temp file ile commit testi başarılı (sonra `git reset HEAD~1` ile geri alındı)

### Yanıt stili kuralı eklendi
- `.claude/rules/response-style.md` (yeni) — uzun açıklama yasak, özet/iltifat/adım duyurusu yok
- `CLAUDE.md` 10. madde: "Yanıt özlülüğü" → response-style.md referansı

## Build / Test Durumu
- Build: yeşil (0 error, NU1902 HtmlSanitizer warning eski)
- Test: 321/321 yeşil (Mosaik.Tests)
- Smoke test: dev server başlatıldı (port 5197), login ekranına kadar gidildi — Contracts/Obligations görsel doğrulama yarına bırakıldı (kullanıcıya login bilgisi soruldu, oturum kapatıldı)

## Commit Durumu
- Bu oturum yeni commit'ler:
  - `8d63be9` Faz 3
  - `f55c09f` Faz 4
  - `a56fae9` Faz 6
- Uncommitted (handoff anında):
  - `M CLAUDE.md` — yanıt özlülüğü kuralı (10. madde) eklendi
  - `?? .claude/rules/response-style.md` — yeni kural dosyası
- Bu iki dosya bir sonraki oturumda commit edilebilir (`docs(rules):` veya bu handoff sonrası elle)

## Yarım Kalan / Yarına Bırakılan İşler

1. **Ekran doğrulama (smoke test) yarım** — login bilgisi alınamadığından Contracts/Details/Obligations ekranları görsel doğrulanmadı. Login sonrası kullanıcı kendi ekranlarını gözden geçirebilir veya Claude Preview ile yeniden denenebilir.
2. **Migration 45 DB'ye uygulanmadı** — `UPDATE AiSettings SET ApiKey = NULL` çalıştırılmadı. Encryption sonrası ilk admin login'de profil keys NULL göstermeli, admin yeniden girer. Komut: `cd D:/Dev/sqlcli && dotnet run -- script D:/Dev/reporthub/Mosaik/Database/45_NullAiSettingsApiKeys.sql --profile mosaik`
3. **AI Wizard Faz 2** — `ContractsController.Wizard.cs` partial (5 endpoint: GET Wizard, UploadTemp, ExtractAi, ExtractionStatus, Save). z.ai vision provider hâlâ canlı test edilmedi.
4. **AI Wizard Faz 3** — Frontend Alpine state machine + drop zone + step indicator + ai-suggested badge.
5. **Plan 18A — IK Quick Reports + Dashboard** ⭐ kullanıcı tercihi: "kolaydan zora" sıralamada bir sonraki kazanım (~4-6h, read-only).
6. **CLAUDE.md + response-style.md commit** — bugün uncommitted bıraktığımız 2 dosya.

## Kararlar

### 2026-05-11 (kullanıcı) — Yanıt özlülüğü
"Çok uzun açıklamalar yapmamak ile ilgili kural yaz Claude içine." Yeni `.claude/rules/response-style.md` dosyası + CLAUDE.md 10. madde. Yasak: özet, iltifat, adım duyurusu, "şimdi X yapıyorum"; izin: tek cümle durum, kritik bulgu, onay sorusu, handoff özeti.

### 2026-05-11 — Pre-commit hook Windows-uyumlu
`bash` → `sh` değişikliği yapıldı. Git for Windows zaten `sh.exe` getiriyor, WSL gereksinimi kalktı. Hook artık her commit'te çalışıyor.

### 2026-05-11 — DataProtection dev key ring kabul
`SetApplicationName` extension'ı `IDataProtectionBuilder`'da yok (referenced assembly farklı). Sade `AddDataProtection()` bırakıldı. Production'da `.PersistKeysToFileSystem` veya DB persistence ekleneceği yorum olarak yazıldı.

## Dikkat Edilmesi Gerekenler

- **DataProtection key ring restart sonrası kaybolur** (memory-only): Mevcut şifreli ApiKey'ler bir sonraki uygulama restart'ında decrypt edilemez → admin yeniden girmek zorunda. Production'a çıkmadan `.PersistKeysToFileSystem(@"D:\secrets\keys")` veya DB persistence eklenmeli.
- **AiSettingsTest hâlâ tüm diğer profilleri DB'ye yazıyor** (geçici disable + restore). Concurrency riski (iki admin aynı anda Test'e basarsa interleaving). `ITestableAi` ile per-call config override daha temiz olurdu — Faz 7 adayı, şimdilik try/finally ile en kötü senaryo (exception → restore'suz) kapatıldı.
- **Yanıt özlülüğü kuralı bu handoff dosyasına uygulanmadı** — handoff template özet gerektiriyor, kural istisnası (response-style.md'de "handoff dışında özet yok" yazıyor).
- **Bugün journal dosyası 2026-05-11.md** — sistem `date` 2026-05-09 dönüyor ama önceki oturumlar 2026-05-10 yazmış (sıralama: 5-9, 5-10, 5-11). Tarih sıralamasını koruyorum.

## Yarına Başlangıç Noktası

1. **Migration 45 DB'ye uygula** + admin AI Settings ekranında bir profil için key yeniden gir → encryption end-to-end smoke test.
2. **Ekran turu** — Contracts list / Details / Edit / Obligations Index / Create — login bilgisi al, Claude Preview ile screenshot.
3. **Plan 18A IK Quick Reports** ⭐ kullanıcı tercih ettiyse (~4-6h, hızlı kazanım) **veya** AI Wizard Faz 2 (ContractsController.Wizard.cs partial + 5 endpoint).
4. **CLAUDE.md + response-style.md commit** — `docs(rules): yanıt özlülüğü kuralı` tek commit.

---

## Oturum 2 — Plan 26 (OCR Fallback) + Plan 27 Faz A & B

### Ana Konu
Bir günde iki büyük plan: Plan 26 OCR fallback yazıldı+implemente edildi, Plan 27 Faz A komple bitti, Faz B'nin 3 quick win'i de bitti. Turkcell 22 sayfalık taranmış sözleşmesi başarıyla AI analiz edildi (Stage 1 65sn, full pipeline ~3 dk, 16 öneri).

### Tamamlananlar

**Plan 26 — OCR Fallback (taranmış PDF)**
- `plans/26-ocr-fallback.md` yazıldı + onaylandı
- `Mosaik/Services/Ai/TesseractOcrExtractor.cs` (yeni) — PDF → SKBitmap → PNG → Tesseract
- NuGet: Tesseract 5.2.0 + PDFtoImage 4.1.0; `tessdata/tur.traineddata` gitignored
- `Mosaik.csproj` MSBuild target — native DLL kopyalama
- `AiExtractionWorker` fallback zinciri: PdfPig → Tesseract → z.ai vision
- `PdfPigTextExtractor` path fix: ContentRootPath → WebRootPath
- Migration 50 (singular→plural rename) + 51 (audit kolonları)

**Plan 27 — Roadmap + 3 paralel araştırma**
- `plans/27-documents-ai-roadmap.md` (5 faz, ~4-6 hafta)
- Araştırma: CUAD/OpenContracts/Vanna 2.0/RAGFlow/Onyx/Letta/Dify/OpenWebUI + Türk legal MCP (yargi-mcp, mevzuat-mcp ücretsiz MIT) + RAG mimarisi (SQL Server 2025 VECTOR + BGE-M3)

**Plan 27 Faz A — Akıllı Sözleşme Extraction (KOMPLE BİTTİ)**
- A-01 `PageImportanceScorer.cs` — regex skorlu sayfa önceliklendirme
- A-02 Otsu binarization eklendi → performans (GetPixel O(n²)) nedeniyle devre dışı
- A-03 MaxPages 8→30, DPI 200→300, dil `tur+eng`
- A-04 `TesseractOcrExtractor.ExtractPages` — per-page MeanConfidence + OcrPageResult
- A-05 Worker smart page seçimi + low-conf vision rescue (threshold 0.60, max 5 sayfa)
- A-06 Stage 1 prompt: adres halüsinasyon yasağı, vergi/sicil ayrımı, sözleşme/abonelik no eşanlamlıları, kritik bölüm zorunluluk, sayfa konum
- A-08 `CreateContract` endpoint — AI çıkarımdan Contract + Obligation taslakları, redirect /Contracts/Edit
- Migration 52 (Stage2ResultText) + 53 (ProgressTimestampsJson)
- `Views/Ai/Review.cshtml` — Sözleşme Bilgileri/Taraflar/Şartlar section'lar
- `Views/Ai/Detail.cshtml` (yeni) — raw text + Stage1 + Stage2 tab'lar + adım süreleri timeline
- `Views/Ai/Index.cshtml` — stepper UI (4 adım × icon × canlı süre), polling, İptal butonu
- `AiController.Cancel` endpoint
- Startup recovery — orphan Processing extraction'lar app start'ta queue'ya geri
- HTTP timeout 45→180sn (AiSummaryProvider)
- Hata mesajları DB'ye detaylı (Tesseract LastDiagnostic + z.ai HTTP body)

**Plan 27 Faz B — Quick AI Wins (3 quick win bitti)**
- Migration 54 (AiSummary + AiTagsJson + AiClassifiedAt)
- B-01+B-03 `DocumentInsightService.cs` — tek z.ai çağrısı: summary + tags + documentType + year + department
- B-02 Upload akışına fire-and-forget background insight (~30sn'de dolu)
- B-04 Documents Index tag pill'ler + dosya adı tooltip AI özet
- B-05 `DocumentChatService.cs` + `AiController.AskDocument` endpoint + sağ alt chat drawer (native JS, RAG'siz, token <30K)

**Diğer**
- Serilog file logging — `logs/mosaik-YYYY-MM-DD.log` daily rolling 7 gün
- Documents Index UI refactor — TopActions Yeni Belge (native JS toggle), filter-bar standart, table-card, row-link, empty-state__icon
- Contracts/Details UI — dosya satırlarında akıllı butonlar (PDF önizleme + AI Detail link / StartAiExtraction); AI Geçmişi Onayla/Reddet → Detay/İncele
- Yeni utility'ler (components.css): `tab-btn`, `code-block`, `kv-grid`, `party-card`, `term-block`, `ai-stepper`+`ai-step` (pulse), `doc-chat-drawer`, `doc-tags`, `row-link`, `data-link`, `pill.ghost`, `text-err`, `text-right`, `timeline-table`, `stack-12/16`
- DocumentsController.Delete — FK_ContractAiExt_Files temizliği

### Build / Test Durumu
- Build: yeşil ✓ (CS0 hata, CA1416 + NU1902 warning'leri mevcut)
- Test: çalıştırılmadı
- Smoke test: Turkcell 22 sayfa PDF AI analiz başarılı → Status=AwaitingReview, 16 öneri (6 obligation + 3 event + 7 risk), adres "Bursa Osmangazi" doğru, Sözleşme No 5320380620 yakalandı, toplam ~3 dakika

### Commit Durumu
- Uncommitted: **33 dosya** (15 eşiği aşıldı — commit-split zorunlu)
- Yeni commit: yok (oturum boyunca commit yapılmadı, kullanıcı isteğiyle)

### Yarım Kalan / Yarın'a Bırakılan
- **Commit-split 7 commit**: db migrations, OCR fallback, Faz A extraction, Detail+stepper+Serilog, A-08 CreateContract, Faz B insight+chat, UI refactor
- **A-07** JSON schema validation + retry — ertelendi (opsiyonel)
- **Otsu performance** — Span\<byte\> ile yeniden yaz (şu an devre dışı)
- **z.ai HTTP timeout bug** — 180sn vurmuyor, response stream 14 dk sürebiliyor (page selection ile maskelendi ama gerçek fix gerekli)
- **CA1416** PDFtoImage Windows-only → csproj `<SupportedOSPlatform>windows</SupportedOSPlatform>`
- **NU1902** HtmlSanitizer 9.0.886 güvenlik açığı → NuGet upgrade
- **Plan 27 Faz C+D+E** — DMS Foundation, Compliance (e-imza/KEP), Advanced AI RAG backlog'da
- **Plan 28 — Skill System** tasarımı yazıldı (NL-to-SQL + Sözleşme Hukuku + Doc Chat skill'leri), implement bekliyor

### Kararlar
- **Page importance scoring** — top-12 sayfa seçimi devrede (Vanna 2.0 Function RAG pattern). 15 dk → 65 sn iyileşme.
- **z.ai uluslararası API'de embedding endpoint YOK** (`docs.z.ai/llms.txt` doğrulandı) → Faz E'de BGE-M3 local + SQL Server 2025 VECTOR + DiskANN tek deployment.
- **Türk legal MCP entegrasyonu** — yargi-mcp + mevzuat-mcp ücretsiz MIT, hosted endpoint, Plan 28-F Sözleşme Hukuku Skill'in canlı veri kaynağı.
- **Plan 28 ayrı plan** — Plan 27 DMS odaklı, Plan 28 AI Chat & Skill System (~3 hafta 5 faz).
- **CreateContract akışı:** AI çıkarım → kullanıcı "Sözleşme Olarak Kaydet" butonu → Contract (Status=Draft) + Obligation'lar → redirect /Contracts/Edit. Auto-trigger değil, kullanıcı kontrolü.
- **AiPipelineQueue restart recovery** — startup'ta orphan Processing → queue'ya geri push (DB-backed queue Plan 25.1 hardening).
- **Sözleşme detay sayfası** — AI'ı tekrar çalıştırma butonu kaldırıldı; mevcut çıkarım varsa Detail link (re-run yok), yoksa kullanıcı isterse StartAiExtraction tetikler. PDF önizleme butonu eklendi.

### Dikkat Edilmesi Gerekenler
- **`.req` class** sadece form zorunluluk işareti (accent kırmızı), PDF ikon rengi için `text-err` kullan.
- **`x-data` scope'u TopActions section'a uzanmıyor** — Razor section'ları body x-data scope dışında render edilir, Alpine state paylaşmak zor. Native JS toggle veya `Alpine.store` kullan.
- **Anonymous type ViewBag** Razor'da `dynamic` ile cross-assembly erişilemiyor (.NET 10) → `ValueTuple<int,string>` kullan.
- **Tesseract Otsu** GetPixel/SetPixel virtual call O(n²) — 22 sayfa × 8M pixel = 191M call (5+ dk). `SKBitmap.GetPixelSpan()` + unsafe span ile yeniden yaz.
- **taxId Stage 1 prompt'ta hâlâ Ticaret Sicil No'ya karışıyor** — daha sıkı format kontrolü gerekli.
- **Confidence parse case-sensitive** — "High"/"Medium"/"Low" string'i hep Medium kalıyor.

### Yarına Başlangıç Noktası
1. **Commit-split 7 commit** — `commit-splitter` agent ile veya manuel. 33 dosya/15 eşik aşıldı.
2. **`.tmp-sms/` sil + `app.log`'u .gitignore'a ekle** (logs/ zaten var ama root app.log farklı).
3. **CA1416 + NU1902 warning temizliği** — csproj SupportedOSPlatform + HtmlSanitizer upgrade.
4. **Plan 28-A başla** (Skill System DB şeması + Registry + Tool framework) **veya** Plan 27 Faz C (DMS Foundation: versiyonlama + FT search + metadata + permission + audit extend).
5. **Otsu performans rewrite** — `SKBitmap.GetPixelSpan()` ile span-based.
