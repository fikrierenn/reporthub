# Build outputs
**/bin/
**/obj/

# User-specific files
**/.vs/
**/.vscode/

# Logs
**/logs/
app.log

# Local DB files
*.mdf
*.ldf

# Secrets
**/*secrets*.json
**/appsettings.Development.json

# MCP server config (plain-text DB credentials for local dev — not shared)
.mcp.json

# Cowork sandbox worktrees (isolated copies, not tracked)
.claude/worktrees/

# Klonlanan skill'ler (git clone ile kurulan, gitignored — README'de install komutu)
.claude/skills/htmx-expert/

# Per-user Claude Code permissions (not shared)
.claude/settings.local.json

# sqlcli local connection config (plain-text password — not shared)
sqlcli.json

# Claude scheduled tasks runtime lock (PID kaydı, oturum bazlı)
.claude/scheduled_tasks.lock

# Codex sandbox (boş skill dizinleri, ReportHub'a ait değil)
.agents/

# Codex agent dokümanı (deneme — CLAUDE.md tek source-of-truth)
AGENTS.md

# Code aggregation utility output (39K+ satır dump)
TUM_PROJE_KODLARI.txt
kod_toplayici_v*.ps1

# Kişisel dev utility script'leri (per-user, paylaşılmaz)
ReportPanel/start.bat
kodlari_topla_v2.bat

# Python virtual environments (NotebookLM CLI vb.)
.venv/

# Geçici cleanup/migration script'leri (Plan 17 vb. tek kullanımlık utility'ler)
.tmp/

# OS junk
.DS_Store
Thumbs.db

# Tesseract dil dosyaları (büyük binary — ayrıca indir)
Mosaik/tessdata/*.traineddata

# Plan 34.1 SOP AI Advisor — model dosyaları (GGUF/ONNX/LoRA, BKM proxy pre-bundle)
# README.md'ler tracked, model binary'leri (.gguf .onnx) ignored.
Mosaik/App_Data/models/**/*.gguf
Mosaik/App_Data/models/**/*.onnx
Mosaik/App_Data/models/**/*.bin
Mosaik/App_Data/models/**/*.safetensors

# Plan 56 M-A G1 — DataProtection anahtar halkası (Forms alan-şifreleme). ASLA commit'lenmez
# (şifreleme anahtarı = sır). Prod'da güvenli persist (Key Vault/DPAPI) ayrı ele alınır.
Mosaik/App_Data/dp-keys/
